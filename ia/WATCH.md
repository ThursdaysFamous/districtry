# WATCH.md — redistricting watch calendar

The one place the dates live: *when to look* for boundary and roster changes in this
instance's sources. The repo's `docs/REDISTRICTING_RUNBOOK.md` is *what to do* when a
boundary changes. Update the "Last done" column each time you complete a row — a
checkpoint with a stale date is a checkpoint that didn't happen.

---

## Standing (automated — verify, don't perform)

| Cadence | What | Where | You do |
|---|---|---|---|
| Weekly (Mon 14:30 UTC) | U.S. House (IA) roster refresh | `.github/workflows/update-ia-congress-roster.yml` → PR on change | Review + merge the PR; a week with a surprise diff is worth a look at the source |
| Weekly (Tue 14:30 UTC) | Iowa Senate + House roster refresh (Open States ia.csv + legis.iowa.gov office enrichment) | `.github/workflows/update-ia-legislature-roster.yml` → PR on change | Review + merge the PR — especially after an Iowa general (November, even years) and each January seating |
| Weekly (Wed 14:30 UTC) | County auditor roster refresh (iowaauditors.org's own association directory) | `.github/workflows/update-ia-county-auditor-roster.yml` → PR on change | Review + merge the PR — auditors are elected countywide and can turn over between the Iowa generals above |
| Weekly (Thu 17:30 UTC) | Des Moines city council roster refresh, PLUS a `--check` re-verification of the ward geometry, its tiling of the city, and the city's service still answering | `.github/workflows/update-ia-dsm-council-roster.yml` → PR on change | Review + merge the PR. Des Moines elects on odd Novembers (city elections), so a diff outside that cycle is a resignation or an appointment and worth reading |
| Weekly (Thu 19:30 UTC) | Cedar Rapids city council roster refresh, PLUS a `--check` that re-proves LINN COUNTY's township code still means Cedar Rapids — in BOTH directions | `.github/workflows/update-ia-cedar-rapids-council-roster.yml` → PR on change | Review + merge the PR. Cedar Rapids elects on odd Novembers, so a diff outside that cycle is a resignation or an appointment and worth reading |

---

## Per-election — the seats above the boundaries

| When | What | Last done |
|---|---|---|
| After each Iowa general (November, even years) and any special election | The chamber rosters turn over; the weekly Open States refresh picks it up, but verify the first post-election PR against the Legislature's own directory before merging | 2026-08-27 (initial build: 49/50 Senate seats, 100/100 House seats) |
| After each U.S. general (November, even years) | The delegation turns over; the weekly congress-legislators refresh picks it up | 2026-08-27 (initial build: 4/4) |

---

## Per-decade — the census redistricting cycle

| When | What | Last done |
|---|---|---|
| After each decennial census (next: 2031–2032) | Congressional + legislative districts redraw: re-run `ia/scripts/build_legislative_boundaries.py` (all three targets — see its docstring for why the default differs from Wisconsin's), confirm the district counts against the apportioned delegation and the 50/100 chambers, and re-verify the smoke anchors against the rebuilt geometry | 2026-08-27 (all three built at 100.00% agreement) |
| Mid-decade — Iowa specifically | County-level SUPERVISOR districts move off the census cycle under Iowa's own law: **Senate File 75** (signed 2025-04-11) forced Story, Johnson and Black Hawk counties from at-large to district elections for November 2026. `ia/scripts/build_ia_supervisor_districts.py` pins the state aggregate's vintage (`LSA_LAST_EDIT_MS` = 2024-01-30) and refuses to build silently if it ever moves — re-run the builder and re-verify all three counties' PLANTYPE the day that check fires, not just the three named here | 2026-08-26 (Black Hawk shipped real geometry from its own hosted GIS; Story and Johnson shipped as county-level TRANSITIONING placeholders, both SOS-approved but no GIS service found for either) |
| Iowa specifically, no fixed cadence | **STORY IS BUILT — 2026-09-05.** Its three adopted districts now ship as real geometry; only Johnson remains a `TRANSITIONING`/`PENDING` placeholder, so re-check Johnson's own site and re-sweep the ArcGIS catalog periodically for a published district-map service (the Black Hawk route). **TWO CORRECTIONS TO WHAT THIS ROW SAID ON 2026-09-05, both of them mine.** (1) It said Story's map carries "**1,584 FILLED curves** — which is the Jackson precondition", and told the next reader to "read the FILL OBJECTS". *The filled curves are real and NONE OF THEM IS A DISTRICT*: the largest is 2.63% of the page and not one exceeds 5% — they are lakes, parks and city fills. Story's districts are **strokes** at linewidth 12, the only curves on the page at that weight out of 10,761 (**CORRECTED 2026-09-05: this row published 10,235 and it was wrong; pdfplumber 0.11.10 reports 10,761 page curves on the pinned PDF**). "This PDF has filled paths" and "this PDF's DISTRICTS are filled paths" are different claims and only the second licenses the Jackson method; the rule that survived is the one that was always the point — **read the path OBJECTS, never the pixels**. (2) It said Johnson's recorded source URL "had gone dead (`/supervisor-districts` 404s)". *It does not.* GET https://johnsoncountyiowa.gov/supervisor-districts with the pinned User-Agent, 2026-09-05 → **200, 45,908 bytes, title "Supervisor Districts \| Johnson County"**. The original measurement probed `/supervisors`, a different path, off a URL truncated in tool output; the shipped URL was never changed and needs no change. **A truncated string in tool output is not a URL — read it from the file.** A fresh `arcgis.com/sharing/rest/search` sweep for Story, 2026-09-05, still returns no supervisor-district service (1 unrelated result), which is why the map had to be read at all. **You do:** re-sweep the catalog for Johnson, and re-run `ia/scripts/build_story_supervisor_districts.py --check` if Story's map is re-printed — it pins the PDF's SHA-256 and refuses to re-derive silently. **TWO MORE CORRECTIONS, 2026-09-05, both from review of the merged build.** (3) This row's build claimed Story's districts SPLIT A PRECINCT. They do not — Iowa Code 49.3(2)(a) — lettered, and exempting supervisor districts in the fifteen counties on representation plan "two", which Story is not — makes district boundaries follow precinct boundaries, and LSA's First Plan lists Roland/Howard Twp whole in District 2. What the block sort divided is the SHIPPED precinct polygon, which still carries its 2020 census voting-district geometry (IoU 0.999573 against `HOWARD TWP W/O STORY CITY`, POP100 1,869 = 1,837 + 32); only 6 of 45 shipped Story precincts still match a voting district that closely, so **treat `ia-precincts.json`'s 2024 vintage as partly stale for this county**. (4) LSA published TWO plans and the build named neither: the First (2025-12-04, 32,783/32,894/32,860) which the Board REJECTED 2026-01-06, and the Second (2026-01-14, 32,940/32,793/32,804). The blocks match the First exactly and none of the Second, which is now gated both ways, alongside a second gate dissolving the First Plan's own precinct lists (worst IoU 0.99127) | 2026-09-05 |
| Iowa specifically, no fixed cadence | Jones County is entirely absent from the state's own CountySupervisorDistricts aggregate (measured by name and by FIPS 105) — re-check on each vintage change (see the row above) whether the state has added it, and separately whether the county's own vector PDF map (`jonescountyiowa.gov/files/board_of_supervisors/bos_districts_final_23073.pdf`) is worth a Jackson-County-IL-style extraction | 2026-08-27 (first build; recorded as a coverage-gaps.json gap) |
| Iowa specifically, every JANUARY | **The county board CHAIR roster** (`ia-county-board-chairs.json`, 35 of 99 counties, refreshed Fridays by `update-ia-county-chair-roster.yml`). Iowa boards elect their chair at the January reorganisation, so **January is when this file should churn and any other month is worth a second look** — an out-of-season change is more often a county rebuilding its page than a board voting again. Two things degrade it silently and neither trips a count guard. (1) **A county page that stops naming a chair simply leaves**, and the builder's floor of 30 is set to catch the parser or the network collapsing, not one county's redesign — so watch the *identity* of the 36 in the weekly PR diff, not only the number. (2) **A chair who leaves the board stops rendering rather than moving**, because the card joins by NAME against `ia-county-officers.json`; that is the safe failure, but it means a county can quietly go chairless on the card while both files look healthy. **The one failure mode no gate here can see** is a county board page that is simply out of date — the scrape is faithful to the page, and only an independent document catches the page being wrong, which is exactly how Des Moines County's boards-and-commissions entry was caught naming the vice-chair. **You do:** in January, spot-check a handful against the counties' own minutes; if a *class* of page starts producing wrong answers, the fix is an exclusion in `ia_county_chair_scraper.py`'s `OTHERBODYRX`, never a loosened `GAP_CAP` **A THIRD failure mode has a guard now and a fourth does not.** A chair QUALIFIED by another body (`Budget Committee Chair: X`, or an `<h3>Regional Planning Commission</h3>` above the name) passed every structural test and is now refused on a window cut from the PARENT, backwards only — the first attempt tested the whole container and dropped EIGHT real counties, because five of them print the supervisor's own representative appointments a line below their name. And a pairing whose own term dates have expired is refused, which is what removed Mahaska. **The one nothing here can see** is a page that is simply out of date without saying so; only an independent document catches that | 2026-09-05 (first build; 98 counties swept, 36 resolved, 0 ambiguous, 19 of 19 checkable against county minutes agreed; 35 after the qualified-chair and expired-term refusals) |
| Iowa specifically, no fixed cadence | `county-subdivision` is live-fetched (no builder script, no committed data/app file — `ia/index.html`'s `tigerStatewideLoader`), so TIGERweb's own vintage bumps (this layer's metadata reads "January 1, 2025 vintage" as of this build) reach the app with nothing to rebuild. What CAN silently go stale is the card's three-way type classification (`township`/`city`/`ut`, read from `LSADC` via the NAME-minus-BASENAME suffix) — periodically re-run the tally query (`WHERE STATE='19'`, group by `LSADC`) and confirm it still returns exactly the three values this build measured (44 ×1,600, 25 ×62, 46 ×1); a fourth value would render with no Type row rather than break, which is safe but worth a look | 2026-08-27 (first build; 1,663 records, 3 LSADC values) |
| Iowa specifically, no fixed cadence | `municipality` is likewise live-fetched (same `tigerStatewideLoader`) with nothing to rebuild — new incorporations/dissolutions reach the app automatically. Nothing to classify (its `LSADC` is uniformly 25 — measured 2026-08-27), so unlike `county-subdivision` there is no derived value that can drift; if a periodic re-check ever turns up a second `LSADC` value, that is the signal Iowa has grown a second incorporated-place class and the card would need one | 2026-08-27 (first build; 939 records, 1 LSADC value) |
| Iowa specifically, weekly (Fri 15:30 UTC) and on any change to the city count | **THE CITY CARD NOW CARRIES CONTACT, NEVER AN OFFICEHOLDER**, and the two must not be confused: `ia-city-contact.json` gives all 939 cities their own office phone (927) and website (532) from the Iowa League of Cities' own city table, which HAS NO COLUMN NAMING A PERSON — so the `ia-municipal-officeholders` gap stays open and the card says so in its own words. **What the weekly run is really watching is the JOIN, not the phone numbers**: `build_ia_city_contact.py` refuses to write unless ALL 939 TIGERweb places still get a League row, with one alias (the League's `Jewell` is TIGER's `Jewell Junction`), and unless the nine rows that correctly do not join keep their measured shape — two county entries, one header artifact, and six tiny places TIGERweb's incorporated-places layer does not carry. A city incorporating, dissolving or being renamed fails that gate loudly instead of leaving one card with no way to reach its city hall | 2026-09-04 (first build; 939/939 joined, 1 alias, 11 cities with neither phone nor site) |
| Iowa specifically, whenever the TIGERweb place count moves | **SIX LEAGUE CITIES ARE ABSENT FROM TIGERWEB AND THE CAUSE IS NOT ESTABLISHED** — Center Junction, Delphos, Hepburn, Millville, Mount Union and Pioneer (23-111 people each, none with a website). Whether they dissolved or TIGERweb lags was NOT determined, and the record says so rather than guessing. What WAS established is the absence itself: a prefix query returns zero for each **against a working control** — `Adel` returns one row and `Jewell` returns `Jewell Junction city` — so the zero is a measurement and not a broken query. **Run the control every time**, because a query that returns zero for everything looks exactly like six dissolved cities. Either way this app draws no polygon for them, so there is no card for contact to reach; the builder asserts the set and fails if a name enters or leaves it | 2026-09-04 (six measured absent, control passed) |
| Iowa specifically, whenever a workflow is added or retired | **THE WORKSHEET'S `workflows[]` LIST IS HAND-KEPT AND NOTHING GATES IT AGAINST DISK**, and it had silently gone two short: `update-ia-county-officers-roster.yml` and `update-ia-supervisor-roster.yml` shipped in phase 3 and were never added, so `ia/history.html`'s "scheduled refresh & audit jobs" tile read **6 against 8 on disk** until 2026-09-04. That tile is MEASURED from the worksheet, which is exactly the drift a measured tile is supposed to prevent — the measurement was just pointed at a hand-kept list rather than at the workflow files. `validate_workflow_deps.py` walks the real files but says nothing about this list. **A gate comparing `workflows[]` to `.github/workflows/*` would close it for all six instances at once and is not built here** — it is root machinery and would surface other instances' drift in a PR about Iowa's cities. Until it exists, add the entry in the same change as the workflow | 2026-09-04 (drift found and corrected, 6 -> 9; no gate) |
| Iowa specifically, per school year (consolidations effective each July 1) | School districts consolidate independently of any census: TIGERweb's `School/MapServer/0` (325 as of this build) already trailed the Dept. of Education's own current layer by one district — Orient-Macksburg dissolved into Nodaway Valley for 2026-2027, and TIGERweb's federal vintage hadn't caught up when this shipped. `ia/scripts/build_ia_school_districts.py`'s `DISSOLVED_INTO`/`EXPECT_TIGER`/`EXPECT_DE` constants gate on the counts it knows about and fail loudly the day either source's count moves again — re-derive the reconciliation (which district, into which neighbor) the same way this one was found: diff the DE org's newest and previous school-year-versioned layers (`IowaSchoolDistricts<YYYY>_<YYYY+1>`) by name, then spatially sample the dissolved district's old boundary against the new layer | 2026-08-27 (first build; DE layers for 2026-2027 and 2025-2026 both fetched and diffed the same day) |
| Iowa specifically, no fixed cadence | `zip-code` and `post-office` are both live-fetched by an Iowa bounding envelope, no builder script and nothing to rebuild — new ZCTAs and post offices reach the app automatically. Neither carries a derived classification that can drift (no `LSADC`-style field this app reads), and neither is a fixed count to gate on (envelope queries move as ZCTA boundaries and post offices near the state line change) — this row exists only so both live-fetched sources are on record, not because either needs periodic re-checking | 2026-08-27 (first build; 1,443 ZCTAs and 1,170 post offices measured in-envelope) |
| Iowa specifically, no fixed cadence | `police-station` and `fire-station` (phase 2 PR 1) are the same live-fetched-by-envelope shape as `zip-code`/`post-office` — no builder script, nothing to rebuild, no derived classification, envelope counts that legitimately move as USGS's structures dataset updates. The dataset is known (from Wisconsin's own build) to carry occasional stale/ghost records (a defunct department not yet removed) — the card's "nearest stations" framing is a proximity fact, never a jurisdictional claim, precisely so a ghost record degrades gracefully rather than misleading | 2026-08-28 (phase 2 PR 1; 449 police points and 1,262 fire points measured in-envelope, correcting the phase-2 roadmap's original 53/51 estimate) |
| Iowa specifically, no fixed cadence | `school-site` (phase 2 PR 3) is pre-built, not live-fetched — new/closed school buildings reach the app only on the next operator rebuild. Re-run `ia/scripts/build_ia_school_sites.py` periodically (no fixed cadence measured yet; DPI directories elsewhere in the fleet rotate roughly per school year) and watch for the naming trap the builder's own docstring records: the correct layer slug is `IowaSchoolBldgs`, never its internal title `PublicSchoolBldgs`, which names a different, stale 2018 service | 2026-08-28 (first build; 1,321 sites, all public, no placeless rows) |
| Iowa specifically, at each new statewide election file (the publisher stamps a fresh one in the spring of an election year) | **POLLING PLACES ARE MEASURED SHUT ON A DROPPED COLUMN, NOT ON A MISSING FILE** — and the one thing to re-check is cheap. The HOST (HSEMD; the Secretary of State SUPPLIES the data, which is why Ask 4 goes to both) puts out a current, public, unlicensed layer of all 1,658 Iowa polling places; its **2024** edition carried `Precinct_Name` and joins this app's precinct fabric at **98.0%**, and the **2026** edition dropped that column, leaving `Pre_Code`, which joins at **22.0%** and matches NOTHING in Polk, Linn, Scott or Black Hawk. A geometric join is not a substitute and was measured: all 1,658 points fall inside some precinct, but 449 precincts contain none and 288 contain several, because a polling place is a building. **You do:** when a new edition appears, re-run one unauthenticated query against its layer 0 and look at the FIELD LIST — if `Precinct_Name` is back, the whole route reopens and nothing else has to change. **Layer 0 is the right place to look for the precinct column and the WRONG place for the election columns**: `Election_Date` and `Election_Description` sat on LAYER 1, the 300 ft buffers, alongside `Combo` and `Split_Code`. An earlier version of this row sent the next reader to layer 0 for all three. Do NOT ship the 2024 file to get a join: it names the 5 November 2024 General, and the display contract retires a polling row once its election has passed. Ask 4 is rewritten to request that one column and is UNSENT | 2026-09-05 (first measurement; 1,658 points, 22.0% against 98.0%, gap `ia-polling-places`) |
| Iowa specifically, per-election (precincts move when a county re-precincts, most often after a redistricting cycle or a consolidation) | `precinct` (phase 2 PR 4) is pre-built from the same Legislature ArcGIS org and vintage (2024-01-30) as `county-supervisor` — re-run `ia/scripts/build_ia_precincts.py` on the same cadence you'd re-check that layer, and pin the item id (`d394edea208c4003ac1d6bd1ec78532f`) rather than a name-based search: two decoy services (`IaPrecincts` on the separate LSAFiscal org, a bare `Precincts` on the same org) sit at deceptively similar names. Re-verify the 2-record `PctNameOfficial` name gap (Warren County) hasn't grown, and never add `PollingPlace`/`PollingPlaceAddr`/`PPID` to `OUT_FIELDS` without first writing the display contract `docs/IA_EXPANSION_PLAN.md`'s phase 4 defers | 2026-08-28 (first build; 1,660 precincts, 99.95% point-agreement, 0 overlaps) |
| Iowa specifically, statutorily up to once per decade (Iowa Code SS602.6107 requires a Supreme Court review "beginning January 1, 2012, and at least every ten years thereafter"; no reorganization found enacted as of this build) | `ia-judicial-district` (phase 2 PR 5) is the one Iowa layer whose OWN weekly workflow re-verifies its geometry, not just its roster: `update-ia-judicial-district-roster.yml` runs `ia/scripts/build_ia_judicial_district.py --check` before every scrape, which re-fetches the LSAFiscal organization's own published district polygons and re-proves all 99 counties' crosswalk assignments against them — a moved county or a redrawn district fails that step loudly rather than drifting silently. If it ever fails, re-derive the crosswalk the same way this build did: iowacourts.gov's own per-district "District N Counties" page, cross-checked against Ballotpedia, both cited in the builder's own docstring | 2026-08-28 (first build; 99/99 counties spatially confirmed against the live LSAFiscal witness, 0 mismatches) |
| Iowa specifically, no fixed cadence (the Legislature's own ArcGIS org has already revised this boundary once, mid-2026, outside any obvious statutory cycle) | `community-college` (phase 2 PR 6) is pre-built, not live-fetched, and no weekly workflow re-checks it — re-run `ia/scripts/build_ia_community_colleges.py` periodically and watch its three build-time gates (15-name match against the CommColleges2020 witness layer, population summing to Iowa's exact 2020 census total 3,190,369, director-district count summing to 124): any one failing is the signal the source moved again. If Southeastern's own `NDISTRICT` ever reads anything other than 16, that specific assertion is this layer's redistricting tripwire. The deferred `cc-director-district` sub-layer is blocked on the LSA's `CC_DD2023` layer gaining Des Moines Area's missing District 2 polygon (confirmed absent 2026-08-28) — re-check that layer's per-college feature count against each college's own trustee count before attempting it | 2026-08-28 (first build; 15/15 colleges, all three witnesses exact) |

| Iowa specifically, per-election (all six county offices are elected on staggered four-year terms; supervisors turn over in halves) | `county` (phase 3 PR 1) now carries all six elected county offices, from FIVE publishers, and the thing to watch is not any one of them going down — it is the two DATED PDFs going stale while still answering 200. The ISSDA sheriff directory (April 2025) and the ICAA county-attorney roster (May 2026) are documents, not services: `ia/scripts/ia_county_officer_sources_scraper.py` pins each URL, so a NEW EDITION AT A NEW PATH looks exactly like no change at all. Re-check both associations' own pages for a newer file after each general election, and re-read the builder's divergence report — every line of it is a county where two publishers disagree, which is the earliest signal that one of them has stopped being maintained | 2026-08-28 (first build; recorder 99/99, sheriff 98/99, county attorney 97/99, treasurer 99/99, 92 of 99 boards) |
| Iowa specifically, no fixed cadence, and before any municipal build | **THE CITY CARD NAMES NOBODY IN 939 OF 940 CITIES**, and until 2026-09-03 Iowa's `coverage-gaps.json` held ONE record and did not mention it — the app's largest silent absence was invisible to the reader and to any audit. It is now `ia-municipal-officeholders`, measured across four routes, and **one of the four corrected this project's own record**: the Iowa League of Cities was on file as "a membership directory with no public officials export located", and `iowaleague.org/cities/` is NOT gated — it answers 200 to an ordinary browser request with 948 city rows carrying a PHONE for 935 and a WEBSITE for 536 (505 of those as bare hostnames with no scheme, 2 cells holding an e-mail instead of a URL). It names NO PERSON in any column, so the old wording was right about the people and wrong about the directory. **That is a shippable City-card improvement on its own and is deliberately not built here** — a card that names nobody and links nowhere could hand a reader their own city's phone and front door from one publisher, joined on name + county, which is a different change from a roster of people | 2026-09-03 (gap recorded; League directory measured, not built) |
| Iowa specifically, on each ask's send date and then at ~3 weeks and ~5 weeks | **THE WEC-SHAPED ASK IOWA HAD NEVER MADE IS NOW DRAFTED** (`docs/ASK_DRAFTS.md` Ask 8, to the Secretary of State's elections division). Wisconsin ships a clerk in all 608 of its cities and villages because ONE publisher holds all 1,848 municipalities in one file, sent in reply to a single e-mail answered in 22 minutes; Iowa's structural counterpart is the SoS, since city elections run under Iowa Code ch. 376 through the county commissioners of elections. Measured before drafting so the ask is not for something already published: sos.iowa.gov answers 200 to curl with a browser user-agent (WebFetch 403s), and neither its Schools & Cities page nor its Research & Data page links a clerk directory or any document of that kind; its contact page publishes a form and three phone numbers rather than an address, so sending may mean the form. **A pointer to the county auditors would be a good answer, not a refusal** — all 99 already ship with an e-mail apiece **RECIPIENT CORRECTED AND DRAFTED INTO THE MAILBOX 2026-09-04**: the ask's own note said the contact page publishes a form and phone numbers rather than an address. It publishes three, Cloudflare-obfuscated the same way the county auditors' are, and the Elections Division has its own — so the draft is addressed there rather than to a web form. The recorded contact paths had also 404'd since the site was rebuilt. **Still NOT SENT**: the ledger stays `NOT YET ASKED — DRAFTED` until the day it goes, and the send date is written the same day, never before. | 2026-09-03 (drafted; not sent) |
| Iowa specifically, no fixed cadence | **WATERLOO IS A SECOND CITY TIER SITTING READY**, found by the same ArcGIS catalog sweep that was looking for something else. `services1.arcgis.com/QOAXA4I2iTKKdBuy/.../Wards_view` publishes 5 wards, and each feature names its OWN ward councilperson, BOTH at-large members, and a link to the city's council page — the Des Moines shape with more in band. The Coles rule still applies and is the whole reason this is a note rather than a layer: a roster attached to a boundary refreshes on the boundary's schedule, so those names are the build-time WITNESS and cityofwaterlooiowa.com is the authority, exactly as `build_dsm_wards.py` treats the Des Moines service. Ames publishes precincts and a council StoryMap, which is prose, not data | 2026-09-03 (measured, not built) |
| Iowa specifically, on each ask's send date and then at ~3 weeks and ~5 weeks | **THREE OUTBOUND ASKS ARE DRAFTED AND UNSENT** (`docs/ASK_DRAFTS.md`), all three INSTITUTIONAL: HSEMD for statewide NG911 service boundaries, the Secretary of State AND HSEMD together for the precinct column the current polling-place file dropped (rewritten 2026-09-05 — it used to ask for a current edition, which measurement showed already exists), and the Department of Education about reading three aggregate columns from the "internal use only" `CommColleges2020` layer. **The operator sends; the ASKED date is recorded the day it goes and never before** (the Scott rule — two ask ledgers in this repo once said "held" about e-mails already sent). Follow up at ~3 weeks and again 2 weeks later before recording a route UNRESPONSIVE, which is a different claim from "no source exists". **A clean NO closes a question for good and is worth as much as a yes**, which is why each draft says declining is fine. **THE FIFTEEN-COUNTY TRANCHE WAS WITHDRAWN, NOT SENT AND NOT ANSWERED**, and the ledger has to keep those three states apart: it was drafted and filled on 2026-08-29 (19 questions across 15 counties) and withdrawn on 2026-09-03 in favour of the operator reading Iowa's county sites BY HAND, the exercise that worked for Wisconsin. That is the right trade because six of the fifteen were blocked by a site that refuses this client or sits behind a challenge — an access control this project does not route around and a person with a browser closes for free. **If the review does not close them, redraft from what it measured**; a page a person has read is a different starting point from one that was only probed | 2026-09-03 (tranche withdrawn unsent; three institutional asks still drafted, none sent) |
| Iowa specifically, before any officer-contact ask is sent, and before the by-hand county review | The withdrawn county asks were the RESIDUE of a probe, not a first move: each county's own site was read first, and an address was accepted only where the officeholder's own name vouched for it (`witnessed`) or its form made it an office mailbox (`office`). That gate is the whole method — **a page window is not a witness.** The first probe took the nearest address to the word "treasurer" and returned a DEPUTY's personal address in 4 of 7 counties (Appanoose, Boone, Bremer, Buchanan), which would have put a staffer's address under an elected officer's name. **THE SAME GATE BINDS A HUMAN READER**: an address found by hand ships only if the officeholder's own name is in it or its local part is the office's mailbox — a page window is not a witness whoever opened the page. Re-run the probe before re-drafting anything — a county that redesigns its site often starts publishing the address as a side effect | 2026-08-29 (first probe; 55 counties short) / 2026-09-03 (residue handed to the manual review: 34 treasurers and 11 sheriffs short, five names disputed, each with its own reason in `ia/scripts/.cache/ia_county_officer_emails.json`) |
| Iowa specifically, no fixed cadence | The ISAC member portal is the ONLY statewide source for the county TREASURER and for the BOARD OF SUPERVISORS, and it is the one source here with no second witness for the treasurer at all. It is also measurably imperfect: it publishes an APPOINTED chief deputy in the elected sheriff's row in at least three counties (Crawford, Page, Sioux) and an appointed assistant in Page's county-attorney row — detected automatically by `build_ia_county_officers.py` only because each office's own directory names the same person as a deputy. **A mislabel in the treasurer's row would be invisible**, because nothing else publishes Iowa's county treasurers. **THAT RECORD WAS RE-MEASURED ON 2026-08-29 AND ITS PARENTHESIS WAS WRONG.** `iowatreasurers.org` is not a payment portal that names nobody: its county selector resolves to `index.php?module=treashome&idCounty=<N>`, a per-county treasurer page that carries E-MAIL ADDRESSES the ISAC portal has no column for (ISAC re-checked the same day — zero `mailto:`, zero `@` anywhere, columns Name/Office/Address/City/State/Zip/Phone/Fax/Party). **It is still not a quick win, for two measured reasons, and neither may be guessed past.** (1) THE KEY IS UNRELIABLE: `idCounty=51` and `idCounty=77` return the same county, byte-identical Jefferson County addresses, so the id is not a 1..99 alphabetical index. (2) IT DISAGREES WITH ISAC ABOUT WHO THE TREASURER IS: `idCounty=1` (Adair) names Marilee Kerber where the shipped roster names Brenda Wallace. Build or rule out that route BEFORE any treasurer-address ask goes out — asking 48 counties for something a state site already publishes spends the operator's credibility on a question they can answer with a link (`docs/ASK_DRAFTS.md`, "What is NOT here, and why") | 2026-08-28 (first build; 4 mislabels detected and corrected, treasurer unwitnessed by construction) / 2026-08-29 (iowatreasurers.org re-measured; the treasurer ask is HELD, not sent) |
| Iowa specifically, per-election, and on any run whose e-mail count moves | **THE TREASURER'S AND SHERIFF'S E-MAIL ADDRESSES COME FROM THE COUNTIES' OWN SITES**, because no statewide directory carries them — the ISAC portal has no e-mail COLUMN at all (re-checked 2026-08-29: zero `mailto:`, zero `@` on a county page), and the ISSDA sheriff PDF has gaps. 65 of 99 treasurers and 87 of 98 sheriffs carry one as of the first run. **AN ADDRESS SHIPS TWO WAYS AND NO THIRD**: `witnessed` (the officeholder's own name is in the local part) or `office` (the local part is the office's mailbox, correct whoever holds it). **A page window is NOT a witness** — the first version of that probe took the address nearest the word "treasurer" and returned a DEPUTY's personal address in four of the first seven counties tried (Appanoose, Boone, Bremer, Buchanan). The builder RE-CHECKS a witnessed address against the name it actually ships, so an address gathered before a divergence resolution changed the name is dropped rather than paired with a stranger | 2026-08-29 (first run; 69 addresses added, 0 dropped) |
| Iowa specifically, before trusting anything from iowatreasurers.org | **THAT SITE SERVES THE WRONG COUNTY, ON A COMPLETE AND PLAUSIBLE PAGE, WITH NO ERROR AND NO 404** — the most dangerous shape a source can have, because no count guard would ever notice. Its per-county pages sit at `index.php?module=treashome&idCounty=<N>`, N in alphabetical county order, and a sweep of all 99 ids on 2026-08-29 found: EIGHT ids serving another county outright (Buchanan, Johnson, Linn, Montgomery and Poweshiek all get CLARKE; Floyd, Iowa and Polk get byte-identical JEFFERSON pages, 92,419 bytes each), and THREE MORE serving the right county's page while carrying Jefferson's address anyway (Dallas, Kossuth, Muscatine). **So the page-level county check is necessary and not sufficient, and the address's DOMAIN must also fit the county** — that second gate is the only thing that catches the last three. Linn is Iowa's second-largest county. NO NAME IS EVER READ FROM THIS SITE, only office-form mailboxes; re-run the sweep before widening that | 2026-08-29 (first sweep; 11 of 99 ids contaminated, all gated out) |
| Iowa specifically, no fixed cadence | Two extraction defects found by reading the output rather than the code, both of which would have shipped dead or wrong data. **A LEADING RUN OF DIGITS IS A PHONE NUMBER**: Taylor County's page yielded `712-523-2384treasurer@taylorcounty.iowa.gov`, an address that looks alive and is not; a run of 7+ digits is stripped and `treas1@` is untouched. **AND A PLACEHOLDER CAN LOOK LIKE AN ADDRESS**: Webster's page publishes `treasurer@mycounty.gov`, which the domain gate rejects. Neither would have failed a count | 2026-08-29 (both caught pre-ship) |
| Iowa specifically, per-election and on any board-size referendum | Seven counties ship NO board of supervisors, and the reason is a gate rather than a missing source: `build_ia_county_officers.py` refuses a board whose ISAC row count is not a legal 3 or 5 (Iowa Code 331.201) or that disagrees with the seat count read back from the shipped supervisor-district geometry. Adair (4), Floyd (2), Henry (4), Humboldt (6) and Tama (4) are impossible counts; Warren and Wright list a legal 5 against geometry seating 3. Re-check these seven on each refresh — a county genuinely moving from 3 to 5 members (Iowa Code 331.203 puts that to a referendum) would show up here first, and the fix is to rebuild the supervisor geometry, never to loosen the gate | 2026-08-28 (first build; 7 withheld, each with its reason on the card) |
| Iowa specifically, no fixed cadence | Five offices ship NO NAME because their two publishers name different people and no third witness resolved it (Davis sheriff, Sac→resolved, Henry and Keokuk county attorney, Humboldt and Jasper recorder). These are not errors to fix in code — they are two directories disagreeing, recorded on the card in those words. The way to close one is the way Sac's was closed: read the county's own office page, and add a `DIVERGENCE_RESOLVED` pin naming that page as the witness. The builder FAILS if a pin outlives the disagreement it settles, so the table cannot calcify | 2026-08-28 (first build; 5 withheld, 1 pinned) |


| Iowa specifically, per-election (supervisors serve staggered four-year terms, so roughly half of every board turns over each even year) | `county-supervisor` (phase 3 PR 2) now names WHO holds each district — but only for PLAN 3 counties, and only the ~17 of 40 that publish the pairing themselves. The thing to watch is not a county going down; it is the PROXIMITY RULE quietly losing its footing. The scraper reads no markup: it finds each known supervisor's surname and takes the nearest "District N" within 80 characters, which works because counties that publish this publish it adjacently (widest gap measured across 67 districts: **42 characters**). Every run prints its widest gap and flags any county above that measured maximum. **A run whose gaps creep toward the 80-character cap is the signal to re-measure rather than to widen the window** — Polk's page prints its five supervisors twice, once as a bare navigation run with no districts near it, and a window wide enough to reach from that run to an unrelated district mention would pair them confidently and wrongly | 2026-08-28 (first build; 17 counties, 67 districts, widest gap 42) |
| Iowa specifically, no fixed cadence | 23 of the 40 plan 3 counties ship NO keying, each with its reason in the scraper's own output: several refuse this client outright (403), two answer 202 behind a captcha — which is an access control this project does not route around — and the rest simply name no district on any page it can find. Those counties are not degraded: their supervisors are still named on the County card, unkeyed, which is the honest answer. Re-check them periodically; a county that redesigns its board page usually starts publishing the district as a side effect, and the scraper's link discovery is deliberately unpinned so it picks that up with no config edit | 2026-08-28 (first build; 23 skipped, every reason recorded) |
| Iowa specifically, on any board-size change or geometry rebuild | Two counties surfaced a conflict this pass rather than a gap, and both are worth re-reading whenever either side moves. **Humboldt**: ISAC lists 6 supervisors (illegal under Iowa Code 331.201), while the county's own page shows 5 districts and 5 of those 6 names — evidence the portal's 6 is the defect. **Warren**: the county's own page shows 5 districts and 5 names, but the shipped LSA geometry seats 3, so the keying is refused by the geometry gate and the likely stale party is the GEOMETRY, not the county. Warren is the opposite conclusion to Humboldt from the same shape of disagreement, which is exactly why neither is resolved by a rule | 2026-08-28 (first build; both refused, both recorded) |


| Iowa specifically, per school year (school districts consolidate every July 1, and a district may switch between at-large and director-district election at any time) | `school-director-district` (phase 3 PR 3) is pre-built, not live-fetched, and no weekly workflow re-checks it — it is geometry, not a roster. Re-run `ia/scripts/build_ia_school_director_districts.py` whenever `ia-school-districts.json` is rebuilt, because the two are joined by name and the builder REFUSES to write if any shipped district gains no director-district feature. Its own source is dated "as of 2023-12-18" and already trails: `LU VERNE` and `ORIENT-MACKSBURG` are still in it and correctly absent from ours, the second being the district this repo dissolved into Nodaway Valley for 2026-2027 — so the director layer is at least one school year behind the Department of Education's. **The gates to watch are the counted ones**: 728 published features, exactly 10 exact duplicates, exactly 2 stale names, and exactly one district (Kingsley-Pierson) carrying an AT-LARGE row alongside a numbered one. Any of those four moving is the source changing shape, and each fails loudly rather than shipping | 2026-08-28 (first build; 716 features, 193 at-large, 523 numbered, 99.85% agreement, 0 overlaps) |
| Iowa specifically, no fixed cadence | The director layer's `UID` looks like a primary key and is NOT one — WEBSTER CITY publishes districts 2 and 3 both carrying `UID 3063002`, with different populations and different geometry. The builder keys on `<GEOID>-<DISTRICT>` and asserts uniqueness, so a future build cannot silently drop a district to a key collision. If the county ever fixes that typo nothing here changes; if a SECOND collision appears, the uniqueness assertion is what will say so | 2026-08-28 (first build; one collision measured, keyed around) |
| Iowa specifically, per school year, and ALWAYS in the same pass that rebuilds `ia-school-districts.json` | `iowa-aea` (phase 3 PR 6) is the one Iowa layer whose geometry this repo BUILDS rather than copies, and the reason is a dated source. The Department of Education publishes an AEA polygon; its item is titled **IowaAEAs** while the layer calls itself **IdoeAeaFY20**, and its description says outright "for the 2019-2020 school year". So it is the build's WITNESS and never its geometry: what draws the line is the Department's own CURRENT district layer, which carries `AEA_NUM` in band on all 324 districts, dissolved over the school-district fabric this app already ships. **THE TWO FILES ARE THEREFORE COUPLED** — `ia/scripts/build_ia_aea.py` joins on `DistrictNCESCode` = Census `GEOID` and refuses to write unless that join is 324/324 with no leftovers in EITHER direction, so a consolidation this repo has not yet reconciled fails here loudly rather than shipping an agency short a district. Re-run it whenever the parent is rebuilt | 2026-09-03 (first build; 324/324, no alias table, 9 agencies) |
| Iowa specifically, on every rebuild of `iowa-aea` | **THE GATE THAT MATTERS IS NOT THE AGREEMENT PERCENTAGE.** An AEA line only moves when a member school district CHANGES AGENCY, so the builder asks that directly, of all 324 districts: five interior points each, and a majority must land inside that district's own agency in the published FY20 polygon. All 324 pass today, which is what makes a six-year-old boundary safe to trust as a witness. A first draft of this gate tried to infer the same thing from how far apart the disagreeing sample points fell — and that is not a property of the data: two independent points on one long shared boundary land close together as soon as the sample is big enough, and the gate duly fired at 4.3 km on a seam it had measured at 50.5 km one seed earlier. **WHEN A PROXY AND THE REAL QUESTION ARE BOTH CHEAP, ASK THE REAL QUESTION.** Two other measurements sit beside it: the dissolve must have ZERO self-overlaps, while the published layer's own overlaps are reported and merely capped — four of its pairs overlap each other across about 5.3 sq mi (01x07 alone is 2.989 sq mi in 124 slivers, the largest 171 acres at Polsby-Popper 0.042). The layer this build declined to ship does not close on itself; the one it built does | 2026-09-03 (first build; 99.8% agreement over 13,072 in-state points, 324/324 districts in their own agency, 0 dissolve overlaps) |
| Iowa specifically, no fixed cadence | The nine agencies' names, phone numbers and websites come from the Iowa AEA system's own **Find My AEA** page, and the join is a KEY rather than a name match: each agency's block carries the same two-digit code (`id="01"`…`id="15"`) the Department's geometry does. The builder requires all nine blocks, a website on each, and the two publishers' names to agree case-folded before it writes — so a page redesign fails the build instead of silently shipping eight agencies or a stale URL. Worth knowing if that page moves: the first guess at Keystone's site (`aea1.k12.ia.us`, from the agency's number) did not resolve at all, and the correct `keystoneaea.org` came off this page. **A HOSTNAME THAT LOOKS DERIVABLE IS STILL A GUESS** | 2026-09-03 (first build; 9/9 with a phone, 8 of 9 sites independently confirmed by their own page titles, mbaea.org answering a bot challenge rather than a title) |


| Iowa specifically, on any change touching the board — County card OR County Supervisor District card | The board sections obey two fleet rules that Iowa broke on first ship and now enforces mechanically (`docs/EXPANSION_GUIDE.md` Part 5). **The switchboard rule**: an identical phone on every member row is the board office, not five direct lines — `build_ia_county_officers.py` collects the distinct numbers per board and hoists the number to `boardPhone` when exactly one covers the whole board, which is currently ALL 92 boards ISAC publishes. If a county ever publishes real per-member lines the test stops firing for it and the numbers stay on the members, so **do not replace it with a per-county pin**. **The whole-board rule**: listing N names on the County card claims all N represent the reader, which is true under Iowa Code 331.206 plans 1 and 2 and FALSE under plan 3 — 35 of the listed boards are plan 3. Every list is therefore qualified by `supervisorPlan`, read back from the shipped district geometry, and a county whose plan this app does not know (Jones, absent from the state layer) claims NEITHER method rather than defaulting to one. **THE SWITCHBOARD RULE HAS TWO ENFORCEMENT POINTS, NOT ONE**, and the second was missed for a day: `ia-supervisor-members.json` carries the same people onto the County Supervisor District card, and it went on shipping the hoisted number per district — where ONE name renders, so the switchboard read as that supervisor's own line, which is worse than the County card's original form. `build_ia_supervisor_roster.py` now carries `boardPhone` at COUNTY level and REFUSES to write if a supervisor row ever arrives with a phone at all, so a genuine per-person line has to be admitted deliberately rather than inherited. When a rule is applied to a roster, check every card that reads that roster. **THE WHOLE-BOARD RULE THEN NEEDED A SECOND PASS OF ITS OWN, for the opposite reason: the qualification was correct and useless.** "One of these supervisors represents you, not the whole board" satisfies the rule and, in the 18 plan 3 counties whose districts are not keyed, tells a reader the list in front of them cannot answer their question and stops there. The note is now conditional on KEYING rather than on the plan alone — a keyed county explains its district badges, an unkeyed one adds a second row naming the limitation and the next step, since all 40 plan 3 counties ship numbered district geometry and the County Supervisor District card therefore always names the reader's district and links that county's board page. It also counts the board rather than assuming five. **When a card cannot answer the question, say what it cannot answer AND what in the app can** | 2026-08-28 (both applied; 92 switchboards hoisted, 350 redundant phone values removed) / 2026-08-28 (supervisor card fixed, 67 more removed) / 2026-08-28 (plan-3 note made keying-conditional; 17 keyed, 18 unkeyed) |


| Iowa specifically, per statutory decade (the plan is effective 2023-08-01 and runs to the next redistricting) | `cc-director-district` (phase 3 PR 4) is pre-built, not live-fetched, and no weekly workflow re-checks it — geometry, not a roster. Re-run `ia/scripts/build_ia_cc_director_districts.py` whenever `ia-community-colleges.json` is rebuilt: the two are joined on the NUMERIC key and the builder refuses to write if any college fails to join or if a college's polygon count stops matching the directors the parent seats. **The remap is the tripwire to watch**: this source numbers Southeastern 8 where the app ships 16, and the builder FAILS if `CCdist 8` ever stops appearing — because a retired remap left in place would silently mis-key a different college. The source's licence is EMPTY (terms unstated, NOT the CC0 the school-director layer carries), so a licence appearing is a change worth reading | 2026-08-28 (first build; 123 features, 15 colleges, 99.95% agreement, 0 overlaps) |
| Iowa specifically, no fixed cadence | **Des Moines Area is drawn as EIGHT director districts (numbered 1 and 3-9) where the parent layer seats NINE, and that is a count disagreement rather than a hole in the map.** The obvious reading was tested and rejected twice over: sampling puts the share of each merged area covered by none of its own director districts at 0.00-0.64% across all fifteen colleges, with Des Moines Area at **0.11% — lower than most, and lower than Southeastern's 0.64%**, so those slivers are digitisation differences between two independently drawn layers rather than missing territory; and the source's own `IDEAL` for Des Moines Area is 99,579 against a merged-area population of 794,895, which is the total over EIGHT (99,362) and not over nine (88,322), with every district's deviation computed against it and inside ±2%. Kirkwood is the control: its IDEAL implies nine for its nine polygons. **Nothing here resolves which publisher is right about the number of directors** — re-check if either layer's vintage moves | 2026-08-28 (first build; both measurements recorded) |
| Iowa specifically, no fixed cadence | `cc-director-district` ships identity-only and the reason is per-college rather than statewide: trustees ARE elected by district under Iowa Code 260C.11, but no statewide roster exists and the 15 colleges publish their boards 15 ways. Probed 2026-08-28: **six name a numbered district on a discoverable board page** (North Iowa, Iowa Valley, Eastern Iowa, Kirkwood, Western Iowa Tech, Iowa Western), Northwest Iowa answers 202 behind a captcha — an access control, not an obstacle to route around — and Indian Hills refuses this client with 403. Joining six of fifteen would leave a reader unable to tell a college with no published district from one this app has not read, so the layer names none. A future pass should key them per college with a name-set gate, the phase 3 PR 2 shape | 2026-08-28 (first build; 6 of 15 measured tractable) |
| Iowa specifically, on any Des Moines annexation or ward redraw | `city-ward`'s Des Moines entry (phase 3 PR 5, consolidated 2026-09-04) is the instance's first CITY tier, and its own weekly workflow re-verifies the geometry as well as the roster. **The load-bearing gate is the TILING one**: the four wards must still cover the city's own City Boundary layer, measured at first build to 0.0070% uncovered in 753 perimeter fragments (largest 3,482 m²). That shape is two independently digitised outlines, not a hole — a genuine hole is ONE LARGE COMPACT PART, which is why the largest single fragment is capped as well as the total. Des Moines annexes land regularly, and a ward layer that has not caught up would leave a reader inside the city where this layer answers nothing at all. **No roster check would notice**, which is the whole reason the gate runs weekly rather than at operator discretion | 2026-08-28 (first build; 4 wards, 90.66 sq mi city, 0.0070% uncovered) |
| Iowa specifically, no fixed cadence | **The city's own wards 1 and 2 OVERLAP, and it is recorded rather than smoothed**: 37,599 m² — 9.29 acres, 0.016% of the city — of which one part is 37,561 m² with a 5,278 m perimeter over a 1,958 × 1,235 m extent, a Polsby-Popper compactness of **0.0169** and a mean width of about 14 metres. That is a ribbon running ~2.6 km along the two wards' shared edge, the Richland-County digitisation-sliver shape, not a disagreement about which ward a place is in. The agreement gate therefore measures the SOURCE's own overlap and refuses only if simplification ADDS to it or if the source's own rises past a ceiling two orders of magnitude above the measurement. **A gate that fails on a defect present identically in its input is testing the publisher, not the build** — and it stops the build without recording anything | 2026-08-28 (first build; measured two ways, by area and by 40,000-point sampling, which agreed) |
| Iowa specifically, on any change to data.dsm.city's terms | The ward boundary ships under a CONDITION, not a permission: the city's Terms and Conditions of Use require that applications using its portal data "must include the following disclaimer", and that text ships verbatim from `dsm-wards.json` onto the card. **The item's `licenseInfo` opens "All rights reserved" and reading that as a refusal nearly kept this layer from shipping at all** — it is the text OF the required notice. The same terms carry a **Right to Discontinue Feeds** clause, so the weekly `--check` is also how a withdrawn service gets noticed rather than discovered months later. If the terms change, re-read them before the next rebuild: what ships on the card is quoted from them | 2026-08-28 (first build; terms read 2026-08-28, last updated by the city 2025-09-04) |
| Iowa specifically, per city election (odd Novembers) | The council page's roster is scoped by `<h2>` heading because **Appointed Staff and Department Directors render in IDENTICAL card markup to the elected members** — a scraper walking every `card-title` ships the City Manager and the Library Director as council members. The scrape refuses if a name appears under both an elected and an unelected heading, **and also refuses if the unelected sections vanish**, because those sections are the control that proves the heading split is still what keeps them out. The four ward members are additionally cross-witnessed against the Wards layer's own in-band names and e-mails, which is the check that catches a page rebuilt into a different shape that still parses | 2026-08-28 (first build; 7 elected seats, 4 unelected cards correctly excluded) |
| Iowa specifically, no fixed cadence | The Wards feature service carries a member name and e-mail IN BAND and this app does not read them as the roster — the Coles County rule, and it is measured here rather than assumed: ward 2's feature still carried its **2024-02-16** edit while wards 1, 3 and 4 were edited across **2025-12-29..31** for the November 2025 cycle, so the in-band roster is refreshed on the boundary's schedule and not the electoral one. It also has no phone field and a null `PersonTitle` on all four. Those uneven dates are printed by every build rather than pinned, because pinning one would fail on every ordinary election | 2026-08-28 (first build; all 4 in-band names agreed with the council page, used as a witness) |
| Iowa specifically, yearly (after the odd-November city elections seat new councils) | **Re-run the municipal candidate sweep before concluding anything has changed.** A `no-source` record ages in one direction only, and this one is now MEASURED rather than assumed: on 2026-09-04 all 532 Iowa cities that publish a website were swept, and 16 (3.0%) yielded a roster a machine could read — against 407 of the state's 939 cities that publish no site at all, which is the ceiling before any markup question. The method is recorded in full in the `ia-municipal-officeholders` blocker so the next pass repeats it rather than redoing it. **Two calibrations that must survive the re-run**: a plausible seat count alone does NOT validate a parse (Waterloo's own page yields eight plausible council members, two of them bio-link artifacts), and a third of extracted addresses are not on a city domain — which is a per-city read, not an automatable one. **Files:** `docs/DATA_LAYER_GUIDEBOOK.md` gaps block, `docs/ASK_DRAFTS.md` Ask 8. **You do:** Re-sweep; if the yield has not moved, the statewide ask is still the only route that closes this. | 2026-09-04 |
| Iowa specifically, on any Waterloo annexation or ward redraw | **Waterloo is the second city on `city-ward` (2026-09-04), and its tiling gate asks a question Des Moines's does not: is any uncovered fragment COMPACT?** Erasing the five wards from the city's own City Limits layer leaves 0.0216% of 62.98 sq mi in 156 fragments, the largest 7,909 m² — bigger than Des Moines's 3,482, which an area-only ceiling copied across would read as a regression. It is not one: 36 m by 3,156 m at Polsby-Popper 0.0025, and NOT ONE of the 156 scores above 0.30 (median 0.0016). A hole is COMPACT; a seam is thin. `build_waterloo_wards.py --check` runs this weekly. **Files:** `ia/scripts/build_waterloo_wards.py`, `update-ia-waterloo-council-roster.yml`. **You do:** Read WHERE a compact fragment is before shipping anything — it is ground inside the city no ward claims, where the card would answer nothing. | 2026-09-04 |
| Iowa specifically, no fixed cadence | **Waterloo's council page names every member TWICE and the second naming is wrong for two of them.** Each member has an authoritative line (`NAME, SEAT Through MM/DD/YYYY`) and a bio-link anchor repeating `NAME, SEAT` with no term — and the anchors read "Steve Simons" for Steve Simon and drop Hector Salamanca-Arroyo's middle initial, while Belinda Creighton-Smith has no anchor at all. The scrape keys on the TERM line and keeps the anchors as its control, failing if they vanish. The page is hand-pasted WYSIWYG HTML with no per-member container, so there is nothing structural to key on instead. **Files:** `ia/scripts/waterloo_council_scraper.py`. **You do:** If a shipped name ever matches an anchor's spelling, the parse has slipped — read the page. | 2026-09-04 |
| Iowa specifically, weekly (Fri 16:30 UTC) and yearly after the odd-November city elections | **Five cities out of 939 name their officials on the City card, and the ratio is the thing to keep true.** `ia-city-officials.json` carries Moravia, Norwalk, Palo, Riverside and Tiffin — a mayor and five council members apiece, 30 people, 30 e-mails, 18 phones — and every one of the five is a HAND-BUILT PAGE at a small city, which is the fragile end of the fleet's source spectrum. Three scrape gates decide each run and each exists because a plausible answer would otherwise ship: the seat count the city's own page publishes, NO NAME TWICE (the Waterloo lesson, earned on a city not in this list), and an e-mail for every official. **Never let this file be described as a statewide roster** — it is 0.5% of Iowa's cities, `ia-municipal-officeholders` stays open for the other 932, and the card's absence note is what those 932 readers get. **Files:** `ia/scripts/ia_city_officials_scraper.py`, `ia/scripts/build_ia_city_officials.py`, `update-ia-city-officials-roster.yml`. **You do:** A city that stops parsing is a page rewrite, not a city that stopped publishing — read the page before dropping it, and drop it rather than loosening a gate. | 2026-09-04 |
| Iowa specifically, on any change to what these five cities publish | **Six of the 30 addresses on this card are not on a municipal domain, and that is correct.** Moravia publishes consumer webmail, an internet-provider account and the contracting business one of its councilmen runs; that is what a town of a few hundred people has, and the city publishes them as the way to reach its officials. **The domain is never what decides an address here** — consulting it was measured and errs in BOTH directions, reading Riverside's and Waterloo's own municipal mail as third-party. What decides is `build_ia_county_officers.py`'s test applied unchanged: the officeholder's own name in the local part, or an office-mailbox form (27 and 3 of the 30). It is re-run every build against the name actually SHIPPED, not the one the scrape matched. **Files:** `ia/scripts/build_ia_city_officials.py`. **You do:** If a city ever starts publishing a HOME address, the parser must keep not reading one — only name, role, e-mail and phone are read, and that is by construction rather than by filtering. | 2026-09-04 |
| Iowa specifically, on any Linn County GIS change | **Cedar Rapids's districts are keyed on an opaque code in ANOTHER government's service, and the only warning available is a control.** The boundary is Linn County's `ElectionsCityCouncilDistrict`, one layer holding Cedar Rapids's five districts and Marion's four, separated solely by `POLITICAL_TWP` — '27' and '21' — with no name field, no domain and no description anywhere in the service. Nothing published says which is which. A swap would be INVISIBLE to any single point test, and would put Marion's districts on Cedar Rapids readers' cards under Cedar Rapids labels. So `build_cedar_rapids_wards.py --check` asserts both halves weekly: 27 must tile Cedar Rapids (ratio 1.00323) and 21 must fail to (99.996% of the city left uncovered). **Files:** `ia/scripts/build_cedar_rapids_wards.py`, `update-ia-cedar-rapids-council-roster.yml`. **You do:** If the cross-control ever fires, do NOT re-key on the other code and ship — read Linn's service and its own page first, because a code that has moved once can move again and the labels are the part a reader trusts. | 2026-09-04 |
| Iowa specifically, next city on `city-ward` | **MARION IS MEASURED, PASSES, AND IS STILL NOT SHIPPED — now for a reason rather than a sequencing choice.** The same Linn service carries Marion's four wards under `POLITICAL_TWP='21'`, and they tile the City of Marion on the same test Cedar Rapids passes (area ratio 1.00866, 0.246% uncovered in 78 fragments, none compact). This row said on 2026-09-04: build the roster FIRST, and if Marion's page will not parse, leave the geometry unshipped and record why. **THE ROSTER WAS ATTEMPTED 2026-09-05 AND THE PAGE CANNOT BE READ AT ALL.** cityofmarion.org denies this client at the Akamai edge on every path including robots.txt, and its Granicus Vision origin answers `/api` with `ErrorCode 900` naming this client's IP address as not authorized — an ACCESS CONTROL, which nothing here tries to defeat. Six further routes are measured shut in the gap record `marion-council-roster`, among them a `marion.legistar.com` that resolves, returns 200 in NINETEEN BYTES and is provisioned to no city at all. **THE ONE REACHABLE SURFACE IS THE FIND, and it is where a next pass starts:** `cityofmarion.civicweb.net` is the city's CivicWeb agenda portal, is not behind the deny, and its identity is proven four ways (a 319 phone one digit off the office number already shipped, the Marion postcode, an outbound link to the city domain, city-specific social accounts) rather than assumed from its name. It carries an Attendance & Voting Records page and 105 meetings; what it does NOT carry, anywhere observed, is a WARD against a name — and the attendance grid is a WebForms postback with no data endpoint in the page. **You do:** develop the CivicWeb route (the postback cycle, or the attached agenda documents) ONLY together with a source that attaches a ward to each member; a ward card naming people it cannot place is what this row has forbidden from the start. **ASK 15 IS DRAFTED (2026-09-05, `docs/ASK_DRAFTS.md`) AND NOT SENT** — to `cityclerk@cityofmarion.org`, an office mailbox the city publishes on every page of that portal, asking only for four names against four wards. On send, change `NOT YET ASKED — DRAFTED` to `ASKED <date>` HERE and in the `marion-council-roster` blocker. | 2026-09-05 |
| Iowa specifically, weekly with the roster, and by hand whenever a refused county is worth re-reading | **NINE COUNTIES PUBLISH THE OFFICIALS OF EVERY CITY INSIDE THEM, AND THREE MORE PUBLISH THE SAME PAGE WITH STALE DATES.** `ia-county-city-officials.json` is 98 cities and 710 officials from the route the gap record had recorded as NOT YET PROBED — the county auditors, Iowa Code 47.2. What to watch is not a county going down; it is the CURRENCY GATE moving in either direction. Iowa city officers are elected in November of ODD years, so `build_ia_county_city_officials.py` refuses any county still publishing a term that has already ended: Sac (29 of 61 officials), Shelby (38 of 77) and Winnebago (22 of 51) fail it today, all three with their four-year seats split across 2025 and 2027 where the shipped nine straddle 2027 and 2029. **A REFUSED COUNTY RE-ENTERS BY ITSELF** the week it updates its page, and a shipped one drops out the same way — so a county count that moves is the gate working, not a scrape breaking, and MIN_COUNTIES TRACKS THE CURRENT COUNT (9 now that Jasper ships) and is what makes it loud. **EXPECT RED, PERIODICALLY, BY DESIGN**: with the floor equal to the count, ONE county going stale fails the build — not a collapse, one county — and every EVEN-YEAR JANUARY, when terms elected the previous November begin, all nine briefly publish expired terms until each page updates, so this workflow is expected to be red for days or weeks each cycle. The next November of an odd year is 2027: **every shipped county's terms go expired that January unless it updates**, so expect movement then and do NOT relax the gate to absorb it. **Files:** `ia/scripts/ia_county_city_officials_scraper.py`, `ia/scripts/build_ia_county_city_officials.py`, `update-ia-county-city-officials-roster.yml`, and twelve rows in `ia/scripts/validate_sources.py` (the three refused counties are registered there on purpose, because that monthly check is the only surface that asks about them). **You do:** if a county leaves, read its page before touching anything — a county that genuinely re-elected everybody to fresh terms looks nothing like a page that stopped being maintained. Never widen the gate to keep a county. | 2026-09-05 (first build; 12 counties swept, 9 current, 98 cities, 710 officials) |
| Iowa specifically, whenever this route is re-swept | **THE SEVEN "PROXY CASUALTIES" ARE NOW ALL MEASURED, AND NONE OF THEM IS UNMEASURED.** The 2026-09-05 sweep reached 71 of 99 county sites; the 28 it did not are classified by WHO refused — 9 Cloudflare, 5 TLS certificate name mismatch, 4 connection reset, 2 TLS handshake refused, 1 Akamai, and 7 that were this sandbox's own egress proxy answering 502 to CONNECT. Re-probed on the domains those seven counties ACTUALLY use: **JASPER publishes and now SHIPS** — its city officials live on its ELECTION AUTHORITY's own host, `jaspercountyelections.iowa.gov`, linked from `jasperia.org`, a domain sitting in three of this app's own data files while every host permuted from the auditor's mail domain failed; **Fayette, Fremont, Ida, Winneshiek and Webster are MEASURED ABSENT** (none publishes an officials page reachable from its own city links; Webster answers on `www.` where the bare domain 403s; Winneshiek's `/cities/` is 44 KB whose visible content is the one word "Cities"); **Osceola is MEASURED BLOCKED** (sgcaptcha 202, an access control). **You do:** never record a county unreachable on the strength of hostnames you invented for it — read the domain this app already ships for that county, follow it, and only then permute. Two claims of the first re-sweep are withdrawn: the Internet Archive rung IS triable from here, and a certificate cannot be read from this sandbox at all, because the egress gateway substitutes its own leaf on every TLS connection — so no certificate claim made here is about the site. | 2026-09-05 (28 unreachable classified; the seven proxy cases resolved to 1 shipping, 5 absent, 1 blocked) |
| Iowa specifically, on any Jones County or state-aggregate change | **Jones is the one Iowa county with no supervisor-district card, and the route its own record named does not exist.** The state's aggregate covers 98 of 99; the county publishes its adopted plan only as a PDF whose 554 vector curves are **all stroke-only, zero filled**, over 22 raster strips — so the Jackson-style extraction the gap record called "plausible" is measured CLOSED, and reading it would be pixel sampling. Its text layer does give all five compositions, and they close the composition route too: every district takes PART of a township, and the `Castle Grove/Lovell/Wayne` precinct spans three districts. **Ask 14 (Jones County Auditor, for the county's own GIS file): NOT YET ASKED — DRAFTED 2026-09-04.** On send, change that to `ASKED <date>` HERE and in the blocker. **Files:** `docs/DATA_LAYER_GUIDEBOOK.md` gaps block (`jones-county-supervisor`), `docs/ASK_DRAFTS.md` Ask 14. **You do:** Re-check the state aggregate for a Jones row first — it is one query and it would close this outright. Do NOT re-attempt the PDF; if the county answers Ask 14 with a file, gate it against its own five published populations. | 2026-09-04 |
| Fleet-wide, before the next instance needs a dispatcher | **`registerCountyLayer` now exists in TWO instances and belongs in ONE.** It was Illinois-only fork-level code until Iowa's second ward city needed it (2026-09-04), and it was ported rather than promoted because promoting it means refactoring Illinois's 18 dispatched concepts — not a change to make inside a PR about Iowa's cities. The honest home is `engine/`, spliced by `compose_app.py`, which is what makes parity hold by construction rather than by two copies staying accidentally identical. **Files:** `ia/index.html`, `il/index.html`, `engine/`. **You do:** Promote it to `engine/` as its own change, with `compose_app.py --check` green on all six instances, BEFORE a third instance copies it a third time. | not started |
| Iowa specifically, on any change to which counties this instance answers for | **The coverage wash was a claim nothing checked, and `check_county_coverage_list` was a vacuum reporting a result.** That check walks `registerCountyLayer` dispatch entries; Iowa registers none — every county concept is one statewide layer — so it returned 0 on every run and printed "0 dispatched counties all inside the coverage ring", carrying an unedited copy of the reference instance's docstring about Illinois counties. Wisconsin, the same shape, found this the hard way: seven counties sat greyed out for two days while their cards named every supervisor and every gate in the repo was green. `check_coverage_ring_tracks_counties()` now holds `METRO_COUNTY_FIPS` to `ia-county-officers.json` in both directions. **The comparand is deliberately NOT `ia-supervisor-members.json`**: the wash claims where the STATEWIDE layers answer, which is all 99 counties, while the supervisor roster covers 17 — a known roster gap, not a coverage lie — so comparing against it would fail a correct instance and read as 82 counties wrongly washed | 2026-09-02 (check added; 99 ring counties == 99 officers entries) |


---
| Monthly, with the fleet's e-mail deliverability check | **The ISSDA sheriff directory's e-mail join glued the NEXT field onto four addresses, and the trap was predicted in the scraper's own docstring before it happened.** `_issda_email` joined up to two continuation lines and then ran a greedy `EMAIL_RE.search` over the lot, so Clay got a WEBSITE (`…iowa.govclaycountysheriffsoffice.com`), Delaware a STREET ADDRESS, Jones a STATE ID and Crawford an office label — four addresses that look alive, pass every count guard and cannot receive mail. `ISSDA_STOP` was the guard written for exactly this and **a label list cannot be the guard**, because the line after the domain is usually not a label. The join now stops the moment what it has IS a whole address; verified old-vs-new on the same PDF, 4 rows moved and the address count unchanged at 84. It also declines correctly where the SOURCE is at fault — Monroe publishes `Email: @` with no local part, so that county ships without one | 2026-09-04 (fixed; `ia/scripts/ia_county_officer_sources_scraper.py`) |
| Monthly, same check | **`iowatreasurers.org` can put a DIFFERENT PERSON in an href than in its own link text.** Monroe's page publishes `<a href="mailto:cchambers@…">fpowless@…</a>` under a photo captioned Faith Powless, so an href-first parser ships Chambers's address for Powless's office with every guard green. `classify()` settles it correctly today for the right reason — the local part carries the OFFICEHOLDER's name — and that property must survive any move to parsing links | 2026-09-04 (recorded in `ia_county_officer_email_scraper.py`) |
| Monthly, same check | `auditor@harrisoncountyia.org` is REPORTED, not withheld: the domain resolves and publishes an MX, but that MX names `mail.harrisoncountyia.org`, which is NXDOMAIN on both resolvers — so the declared route points nowhere and RFC 5321's implicit-MX fallback does not apply. It is not in `scripts/undeliverable.py` on purpose: that list is for MISTYPED ADDRESSES, which retire themselves when the source publishes the right one. A correct address behind a broken route would never retire and would go on withholding a contact the county can fix tomorrow | 2026-09-04 (measured; watch the monthly issue) |

## Unresolved — confirm at the next legislature roster refresh

| What | Why it's here |
|---|---|
| Whether `legis.iowa.gov/legislators/{senate,house}`'s per-legislator profile-page URLs (`…/legislator?personID=<id>`) are session-scoped like Wisconsin's biennium-versioned `docs.legis.wisconsin.gov/<biennium>/…` path, or evergreen | Not confirmed in this PR's research — `ia_legislature_scraper.py` sources every personID fresh from the Open States CSV on each run rather than caching IDs, which sidesteps the question for now, but if the state ever moves to session-scoped profile URLs the scraper's floor (45/93) would start silently failing rather than reading a frozen roster the way Wisconsin's BIENNIUM constant would. Watch the weekly workflow's floor-failure behavior, not a calendar date |
| Whether the "Business Address" field's two observed ZIP variants (50319 and 50311, both "1007 E Grand Ave, Des Moines, IA") are both currently correct, or one is a site error | Low-stakes (both identify the Capitol) — `build_ia_legislature_roster.py`'s honesty assertion checks the street/city/state prefix only, deliberately tolerant of this |

---

## Grow this file

Every new layer ships with a row here naming when its source moves (school-year datasets
rotate every summer; county precinct files change per election; rosters change per election
and per resignation). A source with no row is a source nobody is watching.
