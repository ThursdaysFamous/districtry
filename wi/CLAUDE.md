# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

districtry Wisconsin: a single-file, dependency-light web app. Click a point in Wisconsin (or search an address) and it reports every civic district containing that point and who represents you there. It serves at **districtry.com/wi/** as a folder of the consolidated districtry repo — the first state to expand **in place** rather than as a fork (the retired template route proved this tree end-to-end on the archived `DistrictExplorer-WI` repo before the fleet consolidated; `docs/EXPANSION_GUIDE.md` §2.8 is the decision record). It ships thirty-one layers — phase 1's twelve, phase 2's seven (`docs/WI_PHASE2_PLAN.md`, delivered 2026-08-26), phase 3's ten (fully delivered 2026-08-26), and phase 4's Madison city tier and Technical College District layer (2026-08-27). **Elected School Board District** carries BOTH of the school boards Wisconsin elects by district, on one toggle — statute names them exactly and there are two, MPS and Racine Unified; every other board in the state is at large and has no geometry to draw. Milwaukee's eight come from the city's own layer witnessed against its open-data shapefile, with all nine directors from the district's own page under a committee-list witness. **RACINE UNIFIED JOINED 2026-09-03 AND CLOSED gap `rusd-school-board` BY A ROUTE THAT GAP HAD NOT LISTED.** That record named two ways out and both were about the MAP — a Jackson-style vector-path extraction from RUSD's ArcMap PDFs, or an ask to the district. **RUSD's districts are unions of WHOLE WARDS and RUSD publishes the composition as a table**, so no map was needed: its own board-election page carries "Election District by Municipality", naming every ward in each of the nine, and the shapes are a dissolve of the LTSB ward layer this instance already fetches live. **THE MAPS SAY SO IN THEIR OWN CAPTION** — "District Boundaries generated from WSL LTSB's WISE-LR software" over Census 2020 blocks — which this project had read and not used: A SOURCE'S OWN CREDIT LINE NAMES THE UPSTREAM YOU MAY ALREADY HAVE. The Jackson question is answered anyway so nobody re-runs it: all 21 district PDFs carry ZERO filled path objects (4,881 strokes over 73 raster tiles; the only fills are legend swatches). 116 wards over 7 municipalities, an exact partition joining LTSB 116 of 116 with one alias (RUSD's "Mt. Pleasant" for LTSB's "Mount Pleasant"), and the union reproduces the **Census's own** Racine School District on 99.98% of 4,000 points with identical bounding boxes — a different publisher agreeing, not the source checking itself. **THREE TRAPS**: the composition PDF is served as `Districts-Up-for-Election-2026.pdf`, which reads like one ballot's seat list and is the whole map (districts 2, 4, 5 and 6 are up in April 2027, so a four-district document under a near-identical name is a live risk — the builder discovers it BY CONTENT and the partition gate refuses a partial one); the page's own TITLE is "Election District by Municipality", so anchoring the positional parse on *Municipality* finds the title and reports one column group instead of three, and the anchor is *Ward*, which the title lacks; and A NEWSPAPER'S COMPOSITION IS NOT THE DISTRICT'S — the 2021 press account uses an older ward numbering (Racine to 36, where RUSD's table runs to 57) and building on it would have shipped nine wrong districts that each looked plausible. Its roster is RUSD's own board page: eight members with role, phone, e-mail and term, and District 2 VACANT since 2026-08-20 by the district's own notice — **the card SAYS the seat is empty**, because a card that merely named nobody reads as a layer that does not know. The seat's own "District N Schools" label must agree with the heading above it, so a reordered CMS page fails rather than filing every member under a neighbour's district (`wi/scripts/build_mps_school_board_districts.py`, `mps_school_board_scraper.py`, `wi/scripts/build_rusd_school_board_districts.py`, `rusd_school_board_scraper.py`). And **Aldermanic District**, 853 city and village council districts across 156 municipalities, dissolved from LTSB's coded wards on the statewide municipality key (never the county-qualified `ALDER_FIPS`, which splits cross-county cities), witnessed against the state's own BAS pre-dissolve edition key for key, with NINE measurably incomplete county filings still excluded on the record — Appleton was the tenth until 2026-09-05, when the city's own polling-place page supplied the ward composition Outagamie's filing withholds and `LOCAL_COMPOSITION` drew its fifteen districts under a gate that demands every ward or none, agreement with every LTSB-coded ward, and the polling file's own partition — and alderpersons and village trustees named in EIGHTEEN cities and villages (207 seats) from their own witnessed surfaces (`wi/scripts/build_wi_aldermanic_districts.py`, `wi_alderperson_scraper.py` — whose Kenosha canvass witness caught the city GIS stale on one seat at first build, and whose Madison read survives the index page's off-by-one flat-pairing trap around the vacant seat). Thirteen ride national publishers: U.S. House, WI Senate, WI Assembly, County, the three TIGER school-district tilings (Unified / Union High / Elementary), County Subdivision, City or Village, ZIP Code (ZCTA), Post Office, and the USGS-structures Police and Fire Station nearest-3 pair. **THE CITY OR VILLAGE CARD NAMES A CLERK IN ALL 608 MUNICIPALITIES AS OF 2026-09-03, AND THE SOURCE HAD BEEN SITTING IN THIS PROJECT'S OWN GAP RECORD FOR A DAY** — that record said on 2026-09-02 that the Elections Commission's statewide clerk directory "does mean the municipality card has a statewide officeholder source available for the first time", and the day's build went to Milwaukee County's nineteen executives instead. `WI Municipal Clerks Updated 8-4-2026.pdf` (395 pages, 1,848 municipalities) is committed under `wi/data/source/wec/` beside the polling workbook, and `wi/scripts/build_wi_municipal_clerks.py` parses it POSITIONALLY — Microsoft Reporting Services draws one ruling line between records, so a record is the band between two rules and its five fields are five x-ranges; flattening the page instead interleaves a long municipality name with the next record's clerk. The join is to the SAME TIGERweb Places layer the card reads, the only key the card can look up: **608 of TIGER's 608**, 605 by name and 3 through aliases that are THE SAME THREE the polling build already measured against LTSB (Mt. Sterling, LaValle, Fontana) — the same three missing against a THIRD publisher is what makes that table a fact about WEC's vocabulary rather than about LTSB's. **THE ONE CITY/VILLAGE THAT DOES NOT JOIN IS THIS INSTANCE'S OWN RECORDED GAP, reproduced independently**: French Island has no Census place at all (`french-island-census-lag`), so there is no card there to put a clerk row on, and the builder prints the unmatched list every run — an empty list means TIGER has caught up. **TWO FIELDS ARE WITHHELD AND BOTH ARE MEASUREMENTS.** No e-mail: the file has none (0 of 1,848 records contain an @, and its own PDF /Subject metadata says so) because the clerks asked the Commission to withhold them, which is a decision by the people named rather than an obstacle to route around. No address: the file cannot tell a village hall from a clerk's house and demonstrably carries both — Lori Opitz files ONE street address under the Town of Oconomowoc and the VILLAGE OF LAC LA BELLE, Tom Monacelli one under Warren and the VILLAGE OF LOHRVILLE, Amber Larson one across four towns — so `field_of()` RAISES on the two address labels rather than returning them, because a field that is never extracted cannot leak later. **A HYPHENATED SURNAME WRAPS MID-WORD and its continuation looks exactly like an address line** (Ashland's CHIANNE SCHWEITZER- / MONAHAN, Cudahy's MELISSA VENANCIO- / LEONARD), so the rule is that a CLERK line must be FOLLOWED by another label, gated twice — the continuation is joined AND no shipped name may end in a hyphen. **CURRENCY IS PER RECORD, 2017-07-26 (Lannon) to 2026-08-04, and the card states each row's own date** rather than stamping one 'as of' across 608. It is an OPERATOR build, not a weekly one, and that is the Commission's constraint rather than a choice: the directory IS published, at elections.wi.gov/clerks/directory, and every interior /clerks/* path re-challenges even a browser that has cleared the front page, so there is nothing to schedule and `--check` (which needs TIGERweb) stays out of CI exactly as the polling builder's does — `validate_index.py`'s roster floor is what guards the file. A CLERK IS NOT A COUNCIL: the block is labelled `Municipal clerk` and kept apart from the executive because in 589 of the 608 it is the only name on the card, and the governing-body half of `municipal-officers` stays open. **THE TOWNS FOLLOWED THE SAME DAY AND WISCONSIN IS NOW COMPLETE ON THIS OFFICER — all 1,847 municipalities.** TIGER's Places layer carries no towns, so the 1,239 of them ride the **County Subdivision** card, the only card that answers for a town; same script, same PDF parse, a SECOND join to TIGERweb subdivision layer 1 keyed by 10-digit cousub GEOID, because the two Census numbering systems are unrelated and each card can look up only the key its own layer hands it. **THE TOWN JOIN NEEDS THE COUNTY AND THE CITY JOIN DOES NOT**, measured rather than assumed from statute: there is a Town of Unity in CLARK and another in TREMPEALEAU, different towns with different clerks, so a name-only join files one's clerk under the other — the same collision `build_wi_polling_places.py` records for the same two publishers — while TIGER's 1,242 town records give 1,242 DISTINCT (county, name) keys and NO TOWN CROSSES A COUNTY LINE (0 of 1,239 under MULTIPLE COUNTIES against 58 cities and villages). The county side comes from the app's own `state-counties.json`, never a second TIGERweb call: a county this build cannot place is one the county card cannot draw. **TWO MORE ALIASES AND BOTH ARE THE POLLING BUILDER'S OWN** — Land O'Lakes, where WEC's `LAND O- LAKES` strips its hyphen to a SPACE the Census lacks, and St. Lawrence, character-for-character that table's existing entry; five aliases across both halves, every one a name WEC spells differently from everyone else. **THREE OF TIGER'S 1,242 TOWNS HAVE NO CLERK, all one phenomenon** — a town/village boundary the two publishers draw differently: Harrison (Calumet) and Bloomfield (Walworth), where a village incorporated out of the town and WEC files only the village (Harrison's residue is 24,739 m2, six acres, and the polling file has no reporting unit for it either), and Campbell (La Crosse), the Census's own older name for the Village of French Island — **whose clerk is DELIBERATELY NOT ATTACHED**, because that would assert an identity the Census does not make, which is exactly what `french-island-census-lag` refuses. All three are FUNCSTAT A, so it is a publisher disagreement and not a stale row, and **THE SET IS GATED BY NAME RATHER THAN BY COUNT** so a fourth losing its clerk fails the build and one of these three GAINING one fails it too. **SEVENTY-ONE COUNTY FILES, NOT SEVENTY-TWO**: Milwaukee has no towns at all, which is also why it is the one county publishing its own executives. Per county because the set is ~582 KB against a ~3.4 KB median, and the card builds the filename from the GEOID it already holds (`55` + county + subdivision) so — unlike the polling files' slug key — there is no name-to-slug table on either side to drift. A city or village gets NO clerk row on the subdivision card by design: that card already tells the reader their municipality also answers on City or Village, where the clerk is, and one row on two cards at one point reads as two answers. **THAT LAST PAIR OF GEOGRAPHY LAYERS WAS RENAMED AND RE-NESTED 2026-08-27**, because the app had been contradicting itself in two cards at once: Wis. Stat. 5.02(11) defines a municipality as "city, town or village", the ward card and the Elections Commission's own file both use that sense, and TIGER's Places layer carries 608 Wisconsin records — 190 cities and 418 villages and NOT ONE of the state's 1,242 towns. So a reader at the Town of Adams was told "Municipality: Town of Adams" on one card and, on another labelled Municipality, that the point was "not inside any district in this layer". The layer id stays `municipality` (it is in shipped `layers=` permalinks) and the LABEL now says **City or Village**, with the empty state saying what being outside every place MEANS rather than reading as a failed lookup — the engine's new `emptyNote` hook, since "no result" is sometimes a fact about the place. County Subdivision states each subdivision's TYPE for the same reason: it is the layer that answers everywhere (1,925 records — 1,242 towns, 452 village and 219 city records, 12 Great-Lakes water), so a city answers on both cards and the type row is what tells a reader why. Its 671 city/village records answer for 608 municipalities because 58 cross a county line and get one record per county — the same 58 the Commission's polling file labels MULTIPLE COUNTIES, two publishers agreeing — and that per-county split is the measurement behind nesting County Subdivision `subOf` County, which the reference instance's township layer has always done. Six come from state sources, and each carries a lesson: **County Board District** — all 1,590 supervisory districts in one fetch, because Wis. Stat. 5.15(4)(br)1 makes every county file its boundaries with LTSB each 15 January and 15 July and LTSB publishes the aggregate (Trempealeau's 17 come from the county's own service, because LTSB's file merges two of them; `wi/scripts/build_wi_supervisory_districts.py` carries the measurement) — supervisors are NAMED in ALL 72 counties — Wisconsin complete as of 2026-09-02 (41 board pages + Clark's, Pierce's and Marathon's directories + St. Croix's own district table + Chippewa's board-page h-cards + Menominee's joint County/Town board + Langlade's, Barron's, Florence's and Ashland's board lists + Forest's supervisor cards + Sawyer's staff directory + Douglas's ordinal table + Milwaukee's, Racine's and Lincoln's own GIS layers + Fond du Lac through the Internet Archive + Dodge's constituent directory + Kenosha's and Adams's directory PDFs + Columbia's framed table + Iron's member pages + TEN dated documents — 1,590 seats, every supervisory seat the state files, 1,572 named, 17 the counties themselves mark vacant and ONE WITHHELD). **IRON, THE LAST (2026-09-02), WAS WRITTEN OFF HERE THE SAME DAY AND THE RECORD WAS HALF WRONG.** Its board page names fifteen supervisors with roles and no districts; its 832 KB aggregate directory contains the word district not once. Both true, both about PAGES — **every member's OWN directory page states it** (`District: 4`, `Township/Wards: 4 - Hurley Ward 4`), and the board page LINKS all fourteen, in the anchors their names were read out of. Sawyer, same CivicPlus template, had its member pages enumerated an hour earlier. **AN AGGREGATE THAT OMITS A FIELD IS NOT A COUNTY THAT OMITS IT.** Three traps: the labels carry a colon on some pages and not others, which failed one member outright; two of fourteen give the number only as the lead of their Township/Wards line, so reading the labelled field alone ships a thirteen-seat board; and the empty seat has no page, so its number is ELIMINATION — gated on the board page showing exactly one vacancy AND exactly one district unaccounted for, or a member the directory merely dropped would publish as a vacancy. Witnessed 9/9 on wards and 14/14 on municipalities, bare town names typed from LTSB's filing. **The coverage ring is now the whole state and `OUTSIDE` is empty for the first time.** A live-retry bug surfaced in the same run and is worth its own line: `document_county()` took `scrape_county()`'s whole `(districts, read_from)` tuple, so the day Lafayette's challenged page finally answered, the run died three counties later on `districts.values()` — **a retry path guarded by “this will probably keep failing” is exercised only on the day it stops failing, which is the day it must not break.** **SAWYER, ASHLAND AND DOUGLAS SHIPPED 2026-09-02 AND CLOSED BAYFIELD'S ISLAND** — all three border it, so `metro-outline.json` is ONE polygon with no holes and no islands for the first time, and Iron is a NOTCH rather than an enclave because it fronts the Michigan line. Read that from `--check`. **SAWYER IS THE FLEET'S FIRST COUNTY ON CIVICPLUS'S REWRITTEN DIRECTORY**: Door and Oconto are CivicPlus and share the h-card readers, and Sawyer's has ZERO h-cards — Bootstrap list-group rows — so **THE PLATFORM IS NOT THE MARKUP**; its `/directory.aspx` redirects into the newer `/m/` path, its job title is printed once per breakpoint and BOTH copies are that member's (unlike Forest's, which are neighbours'), and its composition is on a third page. **ASHLAND WAS STOPPED BY ITS OWN robots.txt BEFORE IT WAS EVER SCHEDULED** — the seventh county on that footing and the first found before the weekly job existed rather than after weeks of it: `ashlandcountywi.gov` publishes `User-agent: * / Disallow: /`, so its 21 seats ship as a dated document read 2026-09-02, never re-fetched, with the card saying the county ASKED. The reader is kept for the day the county says yes, and `validate_robots.py` gained a guard so a NOT_FETCHED spec cannot be quietly re-registered as a live carrier. That reader is what measured the rest: **its per-district header carries an ALDERMANIC annotation** that, parsed whole, glues onto the municipality and resolves the city as `ashlandaldermanic`; and its district 14 is the only header of twenty-one with a style attribute, which a bare-tag pattern read as a twenty-seat board. **ONE ASHLAND WARD IS FILED DIFFERENTLY BY THE TWO PUBLISHERS AND THE SEAT STILL SHIPS**: the county puts City of Ashland ward 18 in District 11, LTSB in District 8, and Lincoln's withholding rule does not reach it because the difference is MEASURED — Lincoln's disputed lobe was a majority of its district, ward 18 is 3.8% of District 8 by area. **DOUGLAS WRITES ITS DISTRICTS AS ORDINAL WORDS** (1st, 21st), so a search for “District n” finds NOTHING on the clearest such page in the state, and its rural rows carry composition in the same cell — prose about PARTS of municipalities that is deliberately not used as a witness, because a witness scoring junk is worse than none. **TWO SHARED WITNESS DEFECTS, BOTH SILENT, WERE FOUND HERE**: `COMP_WARDS` did not continue a ward run across the word “and”, so “Wards 5 and 6” dropped ward 6 while the log printed a confident ratio over the smaller number (fixing it raised Clark 55/55 → 57/57 and surfaced these disagreements); and the witness counted a ward LTSB does not have as a numbering disagreement, where there are THREE cases — filed elsewhere in this county, filed under a NEIGHBOURING county because the municipality crosses the line (Marathon's Marshfield wards sit in Wood), or in no county's filing at all. The first draft of that fix called Marathon's three “absent from LTSB”, which was wrong and would have printed a wrong explanation weekly. **IRON IS MEASURED SHUT**: it names all fifteen supervisors with roles and no districts, and its only district-keyed document is a PRE-ELECTION candidate list whose incumbent column names nine people no longer on the board and which says “Open Seat” for six districts — six of fifteen could be paired and nine could not. **FLORENCE, THE 68TH (2026-09-02), IS THE THIRD COUNTY ON THIS VENDOR CMS** (after Menominee and Langlade), which is why the readers they share are now named `DMI_*` rather than `LG_*` — the file's own convention when a helper stops being one county's. Its roster was again one link from a URL this repo already carried, the fifth county running. **ITS EMPTY SEAT IS FILED AS A MEMBER AND WOULD HAVE SHIPPED AS A PERSON**: the list row reads “Position, Vacant” and the member page behind it says Name: Vacant Position, District #: 1, so read the way Langlade's rows are read it flips to “Vacant Position”, passes `is_name()` — two capitalised words — and ships as District 1's supervisor. The vacancy is therefore tested BEFORE the name is built and on BOTH surfaces, so a list that still says vacant over a page that has started naming somebody fails rather than telling that district it has nobody. **NO WARD OR MUNICIPALITY COMPOSITION IS PUBLISHED ANYWHERE FOR THIS COUNTY AND THAT IS A MEASUREMENT** — Langlade's rows carry the towns after the district number and Florence's stop at the number — so the key rests on the county's two statements plus the seat set, which is weaker than Barron's 63/63 and is what this county publishes; composing a check out of LTSB's own file to check LTSB with is not a witness. A third county page listing the eleven filled seats must name exactly them, which is the check the vacancy would otherwise hide. **Six of the eleven mailboxes are PERSONAL addresses** (gmail, outlook, yahoo, chartermi.net, florwi.org) against five on `florencecountywi.gov` — note the site is `.com` and the mail is `.gov` — and each ships as published, so no county-domain gate and no surname-agreement gate applies here: `cskel@chartermi.net` is Charles Kellstrom. **The build's own correction is worth more than the county**: a first draft loosened the shared `is_name()` to accept district 8's “John (Jack) Bomberg”, and in doing so defined a second `NICKNAME` pattern that SHADOWED an existing one and broke `is_name` for every county. This file had already answered that exact question for Richland's “Melvin (Bob) Frank” — `_reads_as_name()`, which strips the nickname for the TEST and ships the county's own spelling — so the answer was to use it. **Before changing shared code, look for the shared answer.** **FOREST, THE 67TH (2026-09-02), WAS THE FOURTH COUNTY RUNNING WHOSE PAGE THIS PROJECT HAD NEVER OPENED, AND ITS TRAP IS THE MOST DANGEROUS THE FLEET HAS MET — because a wrong read here LOOKS RIGHT.** `co.forest.wi.gov/county-board-supervisors` answers 200 and names all 21 districts; the county HOME page, the URL this repo had on file, names nobody. **The direction is the OPPOSITE of Barron's** — here the BARE host answers and `www.` fails — so neither prefix is a safe default and the working one is a per-county measurement. The page is a GoDaddy Website Builder site that gives **every supervisor three `<h4>` headings, two of them somebody else**: one shown per breakpoint, the hidden two carrying NEIGHBOURING cards' names. District 4's card reads Scott Goode, Brian Piasini, Sam Augustin and only the first is District 4's, so a reader that takes the last, joins them, or searches the card for a name ships two thirds of this board WRONG — and it would look entirely plausible, because every name on the page is a real Forest supervisor. Only `h4[0]` is the seat's own; the `CONTENT_HEADLINE1_RENDERED` data-aid marks it on FOUR of twenty-one cards and nothing on the rest, so position is the selector. **THE SAME LEAK MAKES THE VACANCY TEST PER-HEADING**: four seats are empty and the county says so in the name itself, and those strings appear in other cards' hidden headings — District 11's third heading is District 12's vacancy — so a card-wide search for the word marks THREE sitting supervisors vacant. Chippewa's stale-notice trap in another vendor's markup, same answer: read the seat's own element, never the page around it. Two witnesses come from the page's own structure — a `districtN` mailbox on every filled seat matching its card's number (17/17), and a ward composition scoring 26/26 in LTSB's same-numbered district with 21/21 districts naming only municipalities LTSB files there. **One shared-code change earned its name here**: Forest writes its towns BARE and spells out only the name that is two municipalities (Town/City of Crandon), so `ward_number_witness()`'s `settle()` now accepts a type of `None` and takes it from LTSB's own filing where the state files that name under exactly one type — thirteen resolved, and an ambiguous bare name stays unresolved, strays and FAILS the county rather than being picked. Bare-means-town would have been correct here and is still a guess. That change also exposed a latent crash it would have caused: the stray note sorts these tuples and `None` against a letter raises `TypeError`, so the failure path would have died instead of reporting. Every supervisor's home address is on that page and none is read. The join left the ring at 2 — Florence has no unserved neighbour left but fronts the Michigan line, so it is a NOTCH rather than a hole. **BARRON, THE 66TH (2026-09-02), IS THE CHEAPEST COUNTY THIS INSTANCE HAS ADDED AND IT SAT UNSERVED BECAUSE A CORRECTION STOPPED AT THE FRONT DOOR.** On 2026-08-26 this repo fixed its own record about Barron — it had been filed as answering 503, the sweep concluded rightly that the county is not that host, and `build_wi_county_board_directory.py`'s table got `barroncountywi.gov`. NOBODY OPENED THE PAGE: it is 75 KB of prose naming TWO people, and it LINKS the roster twice as “Individual Contact Information for County Board Supervisors”. That roster is on the county's OTHER site, `www.co.barron.wi.us/board.cfm` — the older estate where its committee lists, department heads and municipal officers also still live — and it is a complete district-keyed table: all 29 districts with their wards, supervisor, phone and county mailbox. **FINDING THE HOST THAT SERVES A BOARD PAGE IS NOT READING IT**, the St. Croix shape a third time. Two sub-findings: the bare and `www` hosts are DIFFERENT ADDRESSES (…40 fails the handshake, …39 answers 200 reproducibly), so a claim about a host was read as a claim about a site; and WHY the bare one fails is not something this project can state, because the sandbox's egress gateway re-signs every certificate — a chain read from here is the proxy's, and so may be a reset. **THE PAGE'S TRAP IS THAT EACH SEAT IS TWO ROWS with the district number in the one ABOVE the name**, so a flattened read pairs every supervisor with nothing and a first-number read pairs them with a house number; rows are paired explicitly and an unpartnered row fails the county. Its address column is 28 home addresses and is never read. Witnessed 63/63 on wards and 29/29 on municipalities against LTSB, with its three officers stated twice on the page in two formats and gated to agree. **Two gate defects surfaced in the build, both the same shape.** `attach_unique_roles()` keys on DISTRICTS and was handed a NAME-keyed dict: every lookup missed silently, and the run printed three officers it had not attached while all 29 seats, both witnesses and every count guard stayed green. And the ward floor was GUESSED from St. Croix's 131 rather than measured — 63 is all this page can give, since nine districts name a municipality with no ward — so the guess failed the county on this project's own invention. **A FLOOR IS A MEASUREMENT OF WHAT A SOURCE PUBLISHES, NEVER A TARGET FOR IT.** The join left the ring at 2 (Bayfield's island is untouched) and enclosed nothing. **LINCOLN, THE 65TH (2026-09-02), WAS RECORDED HERE AS A COUNTY THAT REFUSES EVERY CLIENT AND WAS NEVER A BLOCKED COUNTY** — the same shape Knox, Johnson and Perry taught in Illinois, arriving a fourth time. What refuses is `www.co.lincoln.wi.us`, behind a Cloudflare MANAGED challenge (`cf-mitigated: challenge`, distinct from the flat denies recorded elsewhere in this file) whose own robots.txt answers 200 and PERMITS the `/media/*` document that prompted the look; the county runs a second host on a different address, off that edge and linked from nowhere on its site, `maps.co.lincoln.wi.us`, a public ArcGIS Server whose `WebMerc_Admin` service is titled “Administrative Layers for Lincoln County WI” and whose layer 5 is Supervisor Districts with a `SupervisorName` and `SupervisorPhone` on every one of the 22. **Its root is a stock IIS splash** — the hollow-page class this instance's directory builder already records — so a host-liveness sweep would have called it nothing. The Blue Book independently gives Lincoln 22 seats and names Jesse Boyd chair, which is the layer's district 10, ruling out the Coles case of a roster column left stale on a map. **AND ONE SEAT NAMES NOBODY, WHICH IS A THIRD CARD STATE**: sampling 4,000 random points against both maps puts 97.90% of the county in the same-numbered district on the county's own polygons and on the shipped LTSB filing, with the entire remainder one lobe — Town of Merrill ward 2, district 21 to LTSB and district 9 to the county. The card reads a reader's district from LTSB, so on that ground it would name district 21's supervisor against the county's own word; district 21 is therefore WITHHELD and the card says why, which is neither vacant (the county says the seat is empty) nor silence (nobody publishes a name). Two false measurements were caught before it shipped and both are recorded in `wi_county_board_scraper.py`: an even-odd ring test over ESRI `rings` read the second part of a MULTIPART district as a hole and reported the wrong district mismatched, and a one-probe-per-district test scored a confident 22/22 agreement that 40 probes per district disproved. Its join also CLOSED the one-day enclave its own absence had opened when Langlade shipped, taking the ring count back to 2 — read that from `build_metro_outline.py --check`, never from a map in your head. **THE NINE DATED DOCUMENTS ARE TWO DIFFERENT FACTS AND THE CARD SAYS WHICH.** Taylor, Lafayette and La Crosse are BLOCKED — a captcha or a Cloudflare challenge to every automated client. Jackson, Richland, Rusk, Polk, Dunn, Pepin and Ashland are the OPPOSITE case: each site serves this client perfectly well and each publishes a robots.txt whose `User-agent: *` group disallows the whole site. Five of the six were scraped weekly until 2026-08-31, when `wi/scripts/validate_robots.py` swept every host this instance fetches on a schedule against that host's own file and found SEVEN saying no. The crawl stopped and everything already read stayed, dated: robots.txt governs RETRIEVAL rather than what already-public information may be shown, so those 103 seats ship with an `asOfWhy` saying the county ASKED rather than refused, and the four counties whose officer CONTACT came off the same hosts (Ashland, Dunn, Pepin, Polk) ride `CARRIED_CONTACTS` under a WITNESS GUARD — each office records the name its page showed at capture, and the builder drops that contact the moment the shipped name stops matching, because a predecessor's number under a successor's name is not a stale row but a wrong one. That gate runs monthly beside `validate_sources.py`, reads each host's file through the fleet's one parser (`scripts/robots_policy.py`, since 2026-09-12: every group naming the fetching client, merged, else every `*` group, merged — its own parser was retired into it), and deliberately does NOT count a link the app merely shows or a carried entry — a transcription is not a fetch; **Municipal Ward** — LTSB's live 7,161-ward layer, the ballot sub-unit everything else is composed from (the card also names the ward's POLLING PLACE, from three sources in preference order: Milwaukee's own CC-BY pairing, Madison's own polling layer — the city runs the election, so a city that publishes stays first — and STATEWIDE the Wisconsin Elections Commission's own list, which the Commission sent on request after this project had spent a fortnight measuring its website's Cloudflare challenge and writing the measurement down as though it described the world. IT DESCRIBED A WEBSITE: a challenge on a site is not a refusal by the agency behind it, the same lesson Knox, Johnson and Perry taught county-side in Illinois, arriving here state-side. `wi/scripts/build_wi_polling_places.py` pairs 7,131 of LTSB's 7,161 wards, 99.58%, as 72 per-county files so a reader fetches ~9 KB rather than ~950 KB; the Commission's own coordinate places the map pin, so the engine skips the geocoder exactly where geocoding is worst — the rural "1104 COUNTY ROAD C" addresses that resolve to a road or to nothing. The file is PROVISIONAL until the Commission publishes its November edition, and all five conditions of the display contract are build gates rather than wishes: the election is named on the card, the word *provisional* is in the reader's own sentence, the pull is dated and attributed, MyVote stays the primaryLink, and THE CARD RETIRES THE PAIRING ONCE THE ELECTION DATE PASSES — a per-election list that outlives its election looks current and is not. Its build also found a shipped defect nothing else had: 26 Wisconsin wards carry a LETTER, `parseInt` was dropping it, and Wauwatosa — which has no plain wards 2, 4, 6, 7, 8, 12 or 24 at all — had seven pairs of distinct wards sharing one identifier. The 29 wards in 12 municipalities the Commission's file omits are the narrowed gap `ward-polling-places`); **Circuit Court** and **Court of Appeals District** — county unions under a double witness (Wis. Stat. 753.06 / 752.11 AND wicourts.gov agreeing), both benches scraped weekly from the court system itself; and the DPI-directory **School Location** and **Library** nearest-3 points, statewide where the reference instance's library card is city-scoped. The county card names each county's **clerk** from the Blue Book cross-gated against the clerks' own association (WEC's Cloudflare challenge is recorded, never defeated — and measured SANDBOX-SIDE on 2026-08-27 by the one-shot `wi-wec-probe.yml` run: from GitHub's runners a real browser clears it in seconds and MyVote answers plain clients, so the statewide polling/early-voting/municipal-clerk questions are queued as data hunts, not blocks) — and, since phase 4, the county's **other officers** too: sheriff, district attorney, executive/administrator (the book's own CE/CA/AC/CM code decides the elected-vs-appointed label), board chair, treasurer, clerk of circuit court, register of deeds, and coroner or medical examiner, shipped DATED to the book's April 2025 snapshot because no second publisher for those offices measures open (`wi/scripts/build_wi_county_officer_roster.py` — a layout-aware x-position parse whose chair-seats witness caught the Blue Book and the state's own supervisory aggregate disagreeing about Menominee, 7 seats against 5 districts, pinned with the card claiming neither; the book's own footnote marks the Menominee–Shawano shared prosecutorial unit, Wis. Stat. ch. 978) — except the **board chair**, the one officer who is also a supervisor: boards elect their chair each April organizational meeting, so the builder reconciles the book's chair weekly against `county-board-members.json` (a chair the county's own page marks supersedes the book's and the card says so; a book chair absent from a complete roster is withheld with the reason stated — first run measured six counties past the book, two chairs off their boards entirely) — and, since the per-county officer scrape (tranches 1-3), forty-four counties' officer rows carry office phone/e-mail/page links checked weekly against the counties' own pages (`wi_county_officer_contact_scraper.py`), whose first runs caught TEN offices the book no longer has right — two stale executives (Waukesha's, whose own page records the Blue Book's county executive died in office; Portage's, whose surfaces name Eric Olson where the book has John Pavelski), seven further turnovers, and Sawyer's administrator filled where the book's cell literally prints Vacant — the interim ships from the page, and the builder's STALE_EXEC pin withholds rather than ever regressing to the deceased name. Both chambers carry the legislature's own office data. Phase 3's next four are Milwaukee's own city tier, all on the same `milwaukeeCoverage` gate as the MPS board and all built by one script with the MPS build's two-surface machinery (`wi/scripts/build_milwaukee_city_layers.py` — the city's ArcGIS layer server-reprojected, the CKAN shapefile as the build-time area-share witness): **Police District (Milwaukee)**, seven MPD districts that revisit the safety matrix's statewide drop with the city-scoped frame Chicago's own layer uses — the captains are gap `mpd-district-leadership`, because city.milwaukee.gov's Cloudflare challenge is an access control and the card therefore links MPD's district pages and names no one (re-measured 2026-08-27 from CI by the WEC probe: the city answers plain clients there, so a weekly captains scrape is queued) — and **Neighborhood (Milwaukee)**, 190 identity-only polygons whose all-caps names ship title-cased with the raw value kept, and whose witness caught the city's two copies spelling one neighborhood apart (service MCGOVERN PARK, shapefile MC GOVERN PARK — the service spelling ships) — joined 2026-08-26 by **Milwaukee Police Squad Area**, the beat analog: 25 squads under the reference instance's exact police-beat shape (subOf the district, compact card, parent dropped to outline while squads carry the fill), whose NUMBERS encode their districts (hundreds digit) and whose builder sample-verifies that against the shipped district file rather than asserting it — and **Tax Incremental District (TID)**, the reference instance's tif-district concept under Wisconsin's own name (Wis. Stat. 66.1105): the city's 79 ACTIVE finance-overlay districts, the one city layer that does not tile the city (most of Milwaukee is honestly in no TID and the standard empty state says so), whose witness is SCOPED to the city's own STATUS flag because the CKAN shapefile keeps all 56 retired districts the live layer omits — and 15 of those carry no dissolve date, which is why the flag beats the date as the filter. MADISON JOINED THE CITY TIER 2026-08-27 (phase 4 PR 4, `wi/scripts/build_madison_city_layers.py`): the TID toggle now answers in BOTH cities — Madison's 14 under a THREE-SURFACE agreement, because the city's own layer mixes district polygons with half-mile planning buffers in one file (the HALFMILERULE flag separates the concepts; every row is status 'A', buffers included) and still draws two districts DOR's certified annual active list closed, so what ships is layer ∩ DOR with display names from the city's TIF program page, and the newest district (TID 55, Voit Farm) is active with NO published geometry — gap `madison-tid-undrawn`, pinned so the build fails the day the city draws it — and **Neighborhood Association (Madison)** is the city's registered-association fabric under its honest name: 116 Active registrations of six kinds that genuinely NEST (condo and resident associations sit wholly inside neighborhood associations), so the card collects EVERY association at the point rather than the first, each labeled with its classification and linked to its profile on the city's own site; `madisonCoverage` grounds both, the city's own 137-ward fabric dissolved to the corporate limits. And **Technical College District** (`wtcs-district`, phase 4's stretch item): DPI's own 16-district WTCS fabric, statewide and IDENTITY-ONLY BY STATUTE — the board is APPOINTED (Wis. Stat. 38.08), the card says so and names no one — shipped under five gates in `wi/scripts/build_wi_wtcs_districts.py`, including a SEAT WITNESS pinning each college's home city inside its own district and the rule that the union's one lawful interior hole is Lake Winnebago (honestly inside no district; two failed gate drafts that sampled water-bearing TIGER references are part of the record — test a tiling's integrity from its own union). Phase 3's fifth through eighth are the complete statewide **NG911 tier** — `fire-service` (1,046 dispatch areas), `law-service` (639), `ems-service` (580 — regional ambulance providers re-prove the pair key, filing under several counties' authorities and sometimes under Agency_IDs that are not county domains at all) and `psap-area` (95 answering points — which 911 center answers a call placed at the point, the fleet's first PSAP layer and a concept only a statewide instance can carry; its tiling is the one with FUTURE-dated Expire rows, which is why the builder drops expired rows by date, never by presence) from the state Office of Emergency Communications' aggregate of every county's own NG911 filing ("free and open for use by the public", refreshed roughly weekly), dissolved per agency by `wi/scripts/build_wi_ng911_service_areas.py` on the DsplayName+Agency_ID **pair** (bare names collide across counties — two unrelated Rome Fire Departments — while real agencies like Appleton Fire file under two counties' authorities), with plain `-dissolve` rather than `-dissolve2`, whose mosaic flattening silently deletes the law layer's real sheriff/PD concurrent-jurisdiction overlaps (measured: 98.750% name-set agreement before the swap, 100.000% after). These are response areas, never taxing districts and never electing bodies, so the cards name no officeholder; the five county authorities whose filings are absent or partial are re-measured inside their own provisioning polygons on every build and recorded as gaps `ng911-fire-filings` / `ng911-law-filings` / `ng911-psap-filings` / `ng911-ems-filings`, with the card explaining absence in-state instead of hiding (Langlade files no NG911 data at all). Everything beyond these is this instance's next growth per `docs/EXPANSION_GUIDE.md` (Part 3 for county-by-county and city-tier builds, §2.2.1 for the layer contract, Part 4 for a new concept); the phase-3 backlog is fully delivered, and what the state's publishers still withhold lives as the Data-gaps records rather than as queued work.

**There is no build step, no framework, and no server-side code.** The app — styles, engine, and layer modules — lives inline in `index.html`. `sw.js` is the service worker; `data/app/*.json` are runtime-fetched data files; `data/state/` is the bootstrap's state config (`build_congress_roster.py` reads its FIPS/USPS/seat count — it ships in the repo and is excluded from the Pages deploy). `sources.html` carries the generated per-layer provenance matrix; `faq.html` the ten common questions, mirrored exactly in its FAQPage JSON-LD. `history.html` is the instance's deployment history — GENERATED by the repo-root `scripts/build_history_page.py` from the worksheet's `history_page` key: the stat tiles are MEASURED from the shipped data files (so any change that moves a number regenerates the page or fails `--check` in CI, and the three weekly workflows whose outputs feed a tile regenerate it in the same run), while the changelog entries are DATED, newest-first, append-only snapshots edited only in the worksheet. The masthead's fleet-wide "Why this exists" link is deliberately unchanged — the maker's motivation and this instance's record are different questions, each with its own page.

<!-- ==== GENERATED:BEGIN metro-facts ==== -->
**Metro facts** (generated from `metro-worksheet.json` — edit the worksheet and run
`python3 scripts/generate_metro_files.py`; hand-edits here fail CI):

- Metro: Wisconsin (`wisconsin`) — https://districtry.com/wi/
- Geocoders: address Photon (Wisconsin-bounded type-ahead); unbounded Photon (whole-coverage, sibling-metro lookup); POI Nominatim (office-address pin lookup, Wisconsin-bounded, serial >=1s queue)
- Ground truth: 44.89804,-89.75782 (inside Marathon County) → county Marathon County; wi-circuit-court Marathon County Circuit Court; wi-court-of-appeals Court of Appeals District III; us-house 7; school-district-unified Marathon City School District; wi-senate 29; wi-assembly 86; county-board 35; wtcs-district Northcentral Technical College District. Negative point 47.39000,-92.97000 (off the northwest corner of Wisconsin — outside the state and every starter layer).
- Layers: 31 registered (political 7, safety 8, schools 6, geography 10); `registerLayer(` floor 19. Debug namespace `window.WisconsinExplorer`.
- Scheduled workflows: `update-wi-congress-roster.yml` (Mon 13:30 UTC); `update-wi-legislature-roster.yml` (Tue 13:30 UTC); `update-wi-county-board-roster.yml` (Thu 14:30 UTC); `update-wi-circuit-court-roster.yml` (Wed 15:30 UTC); `update-wi-court-of-appeals-roster.yml` (Wed 16:30 UTC); `update-wi-county-clerk-roster.yml` (Fri 14:30 UTC); `update-mps-school-board-roster.yml` (Mon 15:30 UTC); `update-rusd-school-board-roster.yml` (Mon 17:30 UTC); `update-wi-alderperson-roster.yml` (Thu 15:30 UTC); `update-mpd-captains-roster.yml` (Mon 16:30 UTC); `wi-validate-sources.yml` (1st of month 13:00 UTC); `update-wi-municipal-executive-roster.yml` (Thu 16:30 UTC).
- Source registry: `scripts/validate_sources.py` (machine-checked monthly)
<!-- ==== GENERATED:END metro-facts ==== -->

## Running & testing

```bash
# From the REPO ROOT — one server, every instance:
python3 -m http.server 8000    # then open http://localhost:8000/wi/

# Behaviour gate (real Chromium boot via Playwright) — the main test:
npm install playwright@1.56.1 && npx playwright install --with-deps chromium
BASE_URL=http://localhost:8000/wi/ node wi/scripts/smoke_test.mjs

# Static gate (run after any data/app regeneration or app edit):
python3 wi/scripts/validate_index.py wi/index.html

# Generated-region gate: per-instance facts live ONCE in wi/metro-worksheet.json;
# GENERATED regions are emitted from it. NEVER hand-edit a GENERATED region:
pip install -c scripts/requirements.txt jsonschema
python3 scripts/generate_metro_files.py            # regenerate in place (all instances)
python3 scripts/generate_metro_files.py --check    # the CI drift gate

# Engine parity: the ENGINE fences are composed from the repo-root engine/ —
# edit a block THERE and recompose, never inside an instance file:
python3 scripts/compose_app.py            # splice engine/ into every instance
python3 scripts/compose_app.py --check    # the CI drift gate
```

**Sandboxed environments (Claude Code web):** the headless browser cannot reach the Leaflet CDN. The repo root's `.claude/settings.json` SessionStart hook runs `scripts/vendor_leaflet.sh`, which vendors Leaflet into `wi/scripts/vendor/leaflet/` (gitignored); `wi/scripts/smoke_test.mjs` serves it same-origin. Production and GitHub Actions CI reach the CDN directly.

## Architecture: stable core + pluggable layer modules

The metro-agnostic engine inside `index.html` is fenced with `/* ==== ENGINE:BEGIN <name> ==== */ … ENGINE:END` markers and is **composed from the single copy under the repo root's `engine/`** by `scripts/compose_app.py` — there is no release channel and no per-fork copy to drift. **Never edit inside an ENGINE fence in this file** — edit the block under `engine/` (when the change is right for every instance) and recompose. Everything Wisconsin-specific lives in the `METRO:BEGIN config` block (worksheet-generated) and this instance's own module code.

A layer module is registered via `registerLayer({ id, group, label, overlay, query, render })`; families of similar layers use the fenced factory helpers (`registerPolygonLayer`, `registerIlgaChamber` — the generic chamber factory both WI chambers and the U.S. House card use — `registerNearestPointLayer`, `registerSchoolZone`). The modules section in `index.html` carries a commented crib of each. Two invariants pervade the code: the **stale-async guard** (`if (seq !== state.sequence) return;` after every await) and **per-layer failure isolation** (a layer's failure shows a Retry inside its own card, never breaks the others).

**Honesty rules (non-negotiable):** officeholder data is never guessed — where no verifiable roster source exists, cards link to the official body instead of inventing a name (both chamber cards degrade to the district number + the legislature's own directory on a roster miss). External strings always render through `sanitize()`/`textContent`. Roster refreshes always land as PRs for human review — the two root workflows `update-wi-congress-roster.yml` and `update-wi-legislature-roster.yml` follow the fleet pattern — never as direct commits to main.

## Data pipeline

Live layers fetch Census TIGERweb (county subdivisions, places, School layers 1/2, ZCTA by state envelope) and the USGS National Map (post offices) at runtime with the point-first `.atPoint` hook. Pre-built layers ship as same-origin `data/app/` files: `state-counties.json`, `school-districts-unified.json`, `congress-districts.json` (from the bootstrap), and `wi-senate-districts.json` / `wi-assembly-districts.json` (from `wi/scripts/build_legislative_boundaries.py` — statewide TIGERweb, mapshaper-simplified, refused unless the 2,000-random-point agreement gate passes; each carries TIGERweb's ZZ water pseudo-district). Rosters: `congress-roster.json` (congress-legislators) and `wi-{senate,assembly}-members.json` (Open States `wi.csv` via `build_wi_legislature_roster.py`, floors 31/94) — all count-guarded, all refreshed weekly by CI as reviewed PRs. `county-supervisory-districts.json` and its companion `county-board-directory.json` are OPERATOR builds, not weekly ones: re-run them after an LTSB filing window (WATCH.md). The directory is not a roster of people — it carries each county's board size, read back from the shipped geometry so the two cannot disagree, and the county's own official page for the card's footer link, because Wisconsin publishes no statewide roster of county supervisors. The PEOPLE, where a county publishes them, are `county-board-members.json` — ALL 72 counties since 2026-09-02, 1,590 district seats (1,572 named, 17 the counties themselves mark vacant, Lincoln 21 withheld), scraped weekly from each county's own page with that county's reading direction PINNED, because the five page shapes include two that are the same extraction shifted by one and a wrong guess files every supervisor under their neighbour's district. The fifth shape is Lafayette's (`same-line-lead`: the person first, the office next, the district last), which is also the only reading that recovers a ROLE from a seat's own row.

## Growing this instance

A new county or layer follows the repo's `docs/EXPANSION_GUIDE.md`. The working order that guide teaches: prove the source first (a live fetch you performed), ship the boundary and its officeholder sourcing in the same change, floor every scraped count, and record what a publisher does NOT publish in the Data gaps panel rather than guessing. When a layer ships, its row in `wi/metro-worksheet.json` (`layers[]`, with a `source` block) is what puts it on the sources page and in every gate — a layer cannot ship without a provenance row. Extend `LAYER_SIDEBAR_RANK` and `WATCH.md` in the same change.
