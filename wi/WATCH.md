# WATCH.md — redistricting watch calendar

The one place the dates live: *when to look* for boundary and roster changes in this
instance's sources. The repo's `docs/REDISTRICTING_RUNBOOK.md` is *what to do* when a
boundary changes. Update the "Last done" column each time you complete a row — a
checkpoint with a stale date is a checkpoint that didn't happen.

---

## Standing (automated — verify, don't perform)

| Cadence | What | Where | You do |
|---|---|---|---|
| Weekly (Mon 13:30 UTC) | U.S. House (WI) roster refresh | `.github/workflows/update-wi-congress-roster.yml` → PR on change | Review + merge the PR; a week with a surprise diff is worth a look at the source |
| Weekly (Tue 13:30 UTC) | WI Senate + Assembly roster refresh (Open States wi.csv) | `.github/workflows/update-wi-legislature-roster.yml` → PR on change | Review + merge the PR — especially after a Wisconsin general (November, even years) and each January seating |
| Any change to `metro-outline.json` | **The coverage ring is CACHE-FIRST.** A returning visitor keeps the copy they have until `CACHE_NAME` changes, so a county added without a cache bump is named on its card and still greyed out on the map — which happened to all seven counties added 2026-09-02. Bump `cache_name` in `wi/metro-worksheet.json` and regenerate; `scripts/check_cache_version.py` now fails the PR that forgets | `scripts/check_cache_version.py --base origin/main` (in CI) | Bump and regenerate |
| Weekly (Thu 14:30 UTC) | County board supervisors, ALL 72 counties by twenty-two routes (41 board pages + Milwaukee's, Racine's and Lincoln's own GIS layers + Fond du Lac through the Internet Archive + Dodge's constituent directory + Kenosha's and Adams's directory PDFs + Clark's Official Directory PDF (link discovered by anchor text; the Wix content hash changes on re-upload) + Pierce's annual directory PDF (URL PINNED by year, because that host's robots.txt allows documents and forbids the pages that link them; the fetch tries this year, next and last) + Marathon's staff-directory table plus one fetch per member (38 pages; the e-mails are NOT taken, because the county publishes every address only behind a JavaScript handler) + St. Croix's own five-column Districts & Supervisors table (its ADDRESS column is never read: it mixes the Government Center with home addresses and nothing separates them, and four supervisors ship no phone because the number printed for all four is the County Clerk's line) + Chippewa's board-page h-cards, every seat re-confirmed against the county's own staff directory (**its board page carries a STALE VACANCY NOTICE for District 6 above a card naming the supervisor who holds it — the vacancy test is per CARD, never per page, or the run ships a 20-seat board for a 21-seat county**) + Menominee's joint County/Town board, plus one fetch per member (7 pages; **the two AT-LARGE supervisors ride a county-keyed `55078-at-large` row and are rendered on every district card in the county — a run that files only the five ward seats ships five of a seven-member board with the Vice-Chair missing**) + Langlade's board page, plus one fetch per member (21 pages; every one prints a HOME ADDRESS that is never read, and its composition is witnessed on municipality NAMES rather than ward numbers because the county writes ordinal ward words for its city districts) + Barron's `board.cfm` table on **`www.co.barron.wi.us`, the county's OTHER site** — its canonical board page on `barroncountywi.gov` names two people and LINKS this one twice; **each seat is TWO table rows with the district number in the row ABOVE the supervisor, so a flattened read pairs every one of the 29 with nothing and a first-number-in-the-row read pairs them with a house number**; the address column is 28 home addresses and is never read + Iron's board page plus one fetch per member (14 pages; **the district is on each member's OWN page and on NO other county surface — the board page lists fifteen names with no districts and the 832 KB aggregate directory says the word not once**; the labels carry a colon on some pages and not others, two members give the number only as the lead of their Township/Wards line, and the one empty seat has no page so its district is ELIMINATION, gated on the vacancy count agreeing) + Sawyer's staff directory on `sawyercounty.gov`, plus one fetch per member (15 pages; **CivicPlus's NEWER responsive template — NOT the h-card widget Door and Oconto use, so a reader assuming the platform implies the markup measures this county as publishing nothing**; the job title is printed once per breakpoint and both copies are that member's, so they are required to agree; ward composition comes from a THIRD page, `/264/Supervisory-Districts`) + Ashland as a DATED DOCUMENT (**read once 2026-09-02 and never re-fetched: `ashlandcountywi.gov` publishes `User-agent: * / Disallow: /`, so the crawl stopped before it started; the reader is kept for the day the county says yes**, and it is what measured that its per-district header carries an ALDERMANIC annotation that must be cut before the composition parses, or the city resolves as `ashlandaldermanic`; district 14's header is the only one of 21 with a style attribute**; one ward — City of Ashland 18 — is filed in District 11 by the county and District 8 by the state, and the seat ships because that ward is 3.8% of the district) + Douglas's Members by District table (**its district column is ORDINAL WORDS — 1st, 21st — so a search for `District n` finds nothing; its eight rural rows carry composition in the same cell, which an end-anchored pattern read as a 13-seat board**) + Florence's board list on `florencecountywi.com`, plus one fetch per member (12 pages; **its EMPTY SEAT is filed as a member — the row reads `Position, Vacant` and flips to a plausible two-word name if read as a person, so the vacancy is tested before the name is built and on BOTH surfaces**; no ward or municipality composition is published anywhere for this county, so the district key rests on the list and each member's own page agreeing; six of eleven mailboxes are personal addresses the county publishes as the way to reach that supervisor, and every member page's home address is skipped) + Forest's supervisors page on `co.forest.wi.gov` (**the BARE host answers and `www.` does NOT — the opposite of Barron; and every card carries THREE `<h4>` headings of which only the FIRST is that seat's, the other two holding neighbouring supervisors' names, so a read that takes the last or joins them ships two thirds of the board wrong with every name on it real. The four `District N - Vacant` headings leak the same way, so the vacancy test is per HEADING and never per card**) + Lincoln's Supervisor Districts layer on `maps.co.lincoln.wi.us` — a SECOND county host, off the Cloudflare edge its website sits behind and linked from nowhere on it, whose ROOT is a stock IIS splash so a liveness sweep would call the host nothing (**the DISTRICT KEY is witnessed, not just the names: 4,000 sampled points compare the county's own polygons against the shipped LTSB filing, and district 21 — Town of Merrill ward 2, which the county draws in district 9 — is WITHHELD rather than resolved in either publisher's favour. A run that drops the witness ships a name on ground its own county assigns to someone else**) + Columbia's framed table + NINE dated documents: Taylor, Lafayette and La Crosse behind a captcha or a challenge, and Jackson, Richland, Rusk, Polk, Dunn and Pepin because their robots.txt disallows this client) | `.github/workflows/update-wi-county-board-roster.yml` → PR on change | Review + merge. **A county JOINING the roster must join `METRO_COUNTY_FIPS` in `wi/scripts/build_metro_outline.py` in the same change** (INSIDE anchor added, OUTSIDE anchor deleted, `metro-outline.json` rebuilt) or the scope wash greys it out while its card answers — seven counties sat that way for two days on 2026-09-01. `validate_index.py` now fails the merge gate on it, so this is a reminder rather than the only guard. **A county DISAPPEARING from the roster is the signal that matters**: the scraper reads each county with its direction pinned, so a page that has reshaped fails its count guard and is skipped rather than shipping a shifted roster. Go and re-read that county's page. **And when a county is recorded as publishing nothing, re-read that too**: nine of the fifty were publishing a complete district-keyed roster on 2026-08-27, three in a district COLUMN this scraper could not see and Dane on the BOARD's own host rather than the county's |
| Monthly, with `validate-sources.yml` | **RESOLVED 2026-09-04, and one half of the old row was wrong.** Both of this roster's undeliverable addresses are now withheld by `scripts/undeliverable.py` — Clark District 25's `jeromekremp@yahoo.ocm` (`.ocm` is not a TLD) and Florence's `jmills@florwi.org`. **The previous row asserted that `florwi.org` "routes mail perfectly well"; it does not, and that claim was never measured** — the domain is NXDOMAIN on both Google and Cloudflare DoH, with no SOA, NS, A or MX. What WAS right is the rule it was written to record: the 2026-09-03 check used `socket.getaddrinfo` and condemned Sauk County's entire 30-seat board on `saukcountywi.gov`, which has MX and takes mail fine. **RESOLVE MX, NEVER A** — and never the reverse either: a domain with an A record but no MX has an implicit MX (RFC 5321 §5.1), so `marinettecountywi.gov` is reported UNVERIFIED rather than dead. Only sending settles that one, and it is the county clerk's own address | `scripts/undeliverable.py`, and the mail section of `scripts/validate_card_links.py` | Read the monthly issue's E-mail deliverability section. A RETIRE line means a county fixed its page — delete the entry |
| Monthly (1st, 13:00 UTC) | **Source freshness + the robots.txt audit** — `wi/scripts/validate_robots.py` re-reads every host this instance fetches on a schedule and checks each scheduled path against that host's own `*` group | `.github/workflows/wi-validate-sources.yml` → one standing issue on any WARN/FAIL | Read the report before merging anything that adds a county. **Two things it got wrong for two days, both fixed 2026-09-02 and both worth re-testing if this file is ever refactored.** (1) It found the board scraper's URLs from a HAND-KEPT LIST OF SIX TABLES, so the three carriers written after it — Clark's, Pierce's and Marathon's directories — were outside the audit entirely, and Clark's host reported green only because the OFFICER scraper reads four other paths there. It sweeps every upper-case attribute of both scrapers now, which also swept in `web.archive.org`, `archive.org` and the LTSB ward-witness query that nobody had listed. (2) It read each robots.txt with its own bare User-Agent while the scraper sends three pinned header sets, so SEVEN counties whose policies are perfectly readable — Marathon, Sheboygan, Outagamie, Monroe, Rock, Fond du Lac, Winnebago — were filed as “unreadable, not assumed”, which reads like caution and means the rules were never read. Every one of them permits what is fetched. **Reading a policy with a weaker client than the crawl is the one asymmetry this gate must not have** — and reaching for a STRONGER client than the crawl uses would be defeating a control, which is why the header set is the scraper's own, host for host, with nothing retried up a ladder **Last run: 2026-09-02 (72 hosts, 208 scheduled URLs, all permitted; 4 hosts still will not serve robots.txt at all — `services1`/`services2.arcgis.com`, `web.archive.org` and `www.washburnsheriff.org` — and are recorded unknown rather than assumed)** |

---

## Semiannual — the county supervisory filing windows

| When | What | Last done |
|---|---|---|
| Shortly after **15 January** and **15 July** | Wis. Stat. 5.15(4)(br)1 makes every county file its current supervisory district boundaries with LTSB on those two dates, and LTSB republishes the statewide layer. Re-run `wi/scripts/build_wi_supervisory_districts.py`: its gates (1,589 LTSB features raw — the SHIPPED file is 1,590, −16 LTSB Trempealeau +17 from the county's own service — 72 counties, 1..n numbering per county, ward reconciliation) are what catch a county whose plan changed or whose filing broke. A count change is expected news, not a failure — read it, then move the expected number | 2026-08-25 (July 2026 filing; 1,590 districts shipped) |
| Same windows | **Kenosha's geometry has a second, county-authored witness** and it is worth re-running when its filing moves: `wi/scripts/verify_kenosha_supervisory_map.py` converts each "DISTRICT n" label on the county's own adopted map (an ArcMap export carrying a real PDF georeferencing viewport, printing its own authority — County Board Ordinance 12 of 8 November 2021) to a coordinate and point-tests it against the shipped LTSB polygons. It discovers the map from the county's maps page rather than pinning an edition id, warns if that ordinance line stops appearing, and fails if any district's labels sit inside some other district's polygon — the two publishers numbering the same ground differently. It witnesses the NINE districts the sheet labels (15-23); 1-14 are the City of Kenosha's, drawn at a scale that carries ward numbers instead | 2026-08-29 (86 of 99 labels inside the district they name; all nine districts pass on plurality, the weakest being district 15 at ten against at most three) |
| Same windows, and after any RUSD redistricting | **Racine Unified's nine board election districts ride the ward filings too** — they are a DISSOLVE of LTSB's ward layer on the composition RUSD publishes, so they move when Racine, Mount Pleasant, Caledonia, Sturtevant, Elmwood Park, North Bay or Wind Point re-wards, and when RUSD itself redraws. Re-run `wi/scripts/build_rusd_school_board_districts.py`; its gates each fail on a different thing — an exact 116-ward partition over the seven municipalities, a TOTAL 116-of-116 join to LTSB's current filing with no unclaimed ward, and a 99% floor on point agreement with the Census's own Racine School District. **The join gate is the one to expect**: it fails the day a municipality re-wards, which is exactly when a composition drawn against the old fabric must stop shipping. The builder finds its input BY CONTENT (a PDF whose first page is titled "Election District by Municipality") rather than by filename, because RUSD serves it as `Districts-Up-for-Election-2026.pdf` and districts 2, 4, 5 and 6 are up in April 2027 — a four-district document under a near-identical name would otherwise ship a third of a map | 2026-09-03 (9 districts, 116 wards, 7 municipalities; witness 99.98% of 4,000 points; one alias, Mt. Pleasant to Mount Pleasant) |
| Same windows | **The aldermanic dissolve rides the same filings**: re-run `wi/scripts/build_wi_aldermanic_districts.py`. Its gates (2,580 coded wards, 867 statewide keys, 50 locally composed, the pinned NINE-municipality exclusion list, four pinned sliver holes, the BAS pre-dissolve witness against the state's own keys, ≥99.9% point agreement) each catch a different way a filing can move — and the EXCLUDED list is the one to hope changes: a county completing one of the nine's ward coding is how a city leaves the `aldermanic-incomplete-filings` gap. **`LOCAL_COMPOSITION` NEEDS RE-READING AT THE SAME WINDOWS AND FOR A DIFFERENT REASON**: Appleton's fifteen districts are composed from its city clerk's polling-locations page, so a re-warding moves the ward numbers under a table this repo pins by hand. The builder fails rather than guesses — every ward must be composed, the composition must agree with every ward the state does code, and the Elections Commission's own polling file must partition the wards the same way — but the fix is to re-read the clerk's page, not to move a constant. If Outagamie ever does code Appleton's wards, gate 2 turns the county's filing into the check on this table. Bump the sw cache when the geometry file changes | 2026-09-05 (July 2026 filing; 853 districts, 156 municipalities shipped — Appleton's 15 locally composed) |
| Same run | **The 72 county containment outlines** are sliced from the shipped `state-counties.json` (`wi/scripts/build_wi_county_outlines.py`), and they are what lets a Data gaps record say "this applies where you clicked". They only move if the county fabric does — a decennial-scale event — but a rebuild of state-counties.json without a rebuild of these would leave the two disagreeing silently, which is why `--check` runs in CI on every PR. Re-run the builder in the SAME change as any county-fabric rebuild | 2026-08-27 (72 outlines, 156 KB, every county's interior point inside its own outline and no other) |
| Same run | Re-check **Trempealeau**, the one county whose geometry comes from its own service rather than LTSB's. If a future LTSB filing restores its district 15, drop the override and ship the statewide file whole — the builder will tell you, because the override's own guards fail if the county's layer stops publishing 17 districts numbered 1..17 | 2026-08-25 (LTSB still merges 15 into 17) |
| 2026-09-17, then two weeks later — **OUTAGAMIE ONLY** | **The ask ledger's follow-up is now one letter, not nine** (guidebook, "The Wisconsin ask ledger"). Nine went out 2026-08-27; WEC answered in 22 minutes and none of the eight county clerks has replied at all — and every one of the eight is answered anyway, because all 72 counties have named their supervisors since 2026-09-02. Chasing those seven would ask a clerk to hand over what the card already shows. **What survives is Outagamie's SECOND ask**, about coding its uncoded city wards at a 15 Jan / 15 Jul filing. **ITS SUBJECT CHANGED ON 2026-09-05 AND IT IS STILL WORTH SENDING**: Appleton no longer needs it — its clerk's own polling-locations page composed the fifteen districts and the city ships — but Outagamie files ALL 131 of its city and village wards uncoded, Kaukauna (17 wards, 99.8%) and New London (3, 42.9%) are still excluded for exactly that, and neither has Appleton's way out: both consolidate their polling places, so no ward-to-place grouping can name a district there. Ask about the COUNTY's filing rather than about Appleton. Silence is NOT a refusal — Clay County (IL) answered only on the third attempt because its Clerk's spam folder ate the first two — so follow up once at three weeks, once more two weeks after that, and only then record the route as UNRESPONSIVE (which is a different claim from "no source exists"). A reply is a build: a supervisor pairing is a pinned scraper plus a weekly workflow, and Outagamie's ward codes need nothing at this end — the existing dissolve picks Kaukauna and New London up at the next filing window | 2026-08-27 (all nine sent 13:30-13:36 UTC. WEC ANSWERED IN 22 MINUTES: the statewide polling-place list EXISTS and publishes per election, the 2026 General edition "closer to September 17th"; the in-person absentee list exists on the same terms; the county and municipal clerk directories arrived attached and were measured. **SEPTEMBER 17 IS NOW A HARD DATE FOR THIS INSTANCE, not just a follow-up**: it is when the final polling file lands, and a provisional edition shown before it must be re-pulled and its flag cleared — see the ward-polling-places blocker's five-condition display contract. All eight county clerks still silent as of 2026-09-03 — verified in the operator's own sent mail that all eight DID go, 13:31-13:36 UTC; an earlier version of the guidebook entry said they had never been sent, which was false) |
| Odd-year January | **The legislature's biennium path**: wi_legislature_scraper.py reads docs.legis.wisconsin.gov/<biennium>/legislators/… and the unversioned path 404s, so the BIENNIUM constant must be bumped (2025 → 2027 → …) or the office enrichment silently reads a frozen session. The scraper's floors catch a page reshape; only this calendar row catches the frozen path | 2026-08-25 (2025-26 biennium; offices shipped 132/132) |
| November, even years | **County clerks** elect on the fall partisan ballot (Wis. Stat. 7.20); appointments fill vacancies year-round and arrive as roster-PR diffs from the weekly two-source scrape. The BIENNIAL drift is the Blue Book itself: the 2027-28 edition will publish under a new docs.legis path, and wi_county_clerk_scraper.py's URL follows it BY HAND — the monthly source report warns when the old excerpt goes stale. **The OTHER county officers (phase 4)** — sheriff, DA, treasurer, clerk of circuit court, register of deeds, chair, executive, coroner/ME — ship from the same book's officer tables DATED to its April 2025 snapshot, and turn over on the same partisan ballots (DAs in presidential years, most others in midterms): the honest refresh for them IS the next Blue Book edition, so the biennium bump is when their currency actually moves. The builder's chair-seats witness (against the shipped supervisory geometry) and the Menominee 7-vs-5 pin re-measure on every weekly run. THE BOARD CHAIR IS THE DATED RULE'S ONE EXCEPTION: boards elect their chair each April organizational meeting, so the builder reconciles the book's chair against the weekly county-board-members.json — a chair the county's page marks supersedes the book's, a book chair absent from a complete roster is withheld with the reason on the card — and BOTH the Thursday board workflow and the Friday clerk workflow rebuild wi-county-officers.json so the decision ships the same week it moves. The per-county officer scrape (tranches 1-3, 2026-08-26; Green Lake added 2026-08-29) widened that weekly check to FORTY-FIVE counties' own officer pages (all but 27 whose surfaces measure shut or unwitnessable — the scraper docstring characterizes each): contact + name witness per office, pinned page shapes with per-county floors (a reshaped page SKIPS its county loudly, never ships partial), and the STALE_EXEC pin holding Waukesha's executive honest — its own page records the Blue Book executive died in office; the interim ships from the page, and extraction failure withholds rather than regressing to the deceased name. Tranche 2's first run caught a SECOND stale executive: Portage's own surfaces name County Executive Eric Olson where the book has John Pavelski, and the divergence machinery shipped the county's name with the book's party withheld. Tranche 3's sweep added seven more turnovers (Calumet and St. Croix clerks of court, Iron's administrator and register of deeds, Oconto's treasurer, Price's medical examiner, Sawyer's administrator filled over a literal Vacant cell) and taught the builder that the book prints Vacant in empty cells — never ship it as a name (Forest's and Oneida's coroner rows now honestly render nothing) | 2026-08-26 (officers shipped: 72 counties x 7 offices; Menominee pinned. Chair reconciliation measured: 6 superseded — Bayfield, Dunn, Eau Claire, Grant, Milwaukee, Polk — 2 withheld — Portage, Winnebago — 14 confirmed, 50 without rosters) |
| Quarterly, or when a TIGER vintage lands | **French Island in TIGER** (gap `french-island-census-lag`): the Village of French Island, La Crosse County, is carried by LTSB and by the Elections Commission and is still 'Campbell town' in TIGERweb's county-subdivision layer and absent from its Places layer, so the ward card and the two municipal cards disagree on French Island. Nothing here is patched — both municipal layers are live Census reads with no build step, and substituting a name into one would put this project's words in a card that claims to quote the Census. THE CHECK IS ONE REQUEST: ask TIGERweb's Places layer (layer 4) for `BASENAME='French Island'`; a non-empty answer means the Census has caught up and this gap record is retired in that change. LTSB's own `MCD_FIPS` for the village ends 99999 — the Census placeholder — so that value gaining a real county-subdivision code is the same signal from the other side | 2026-08-27 (TIGER CouSub 'Campbell town' 5506312300; TIGER Places: neither name; 4 of 5 LTSB French Island wards probed, all inside Campbell town and inside no place) |
| **2026-09-17, then before each election (STATEWIDE)** | **The Wisconsin Elections Commission's polling-place list** — the ward card's statewide pairing, shipped 2026-08-27 from the workbook the Commission sent directly (`wi/data/source/wec/`, no URL exists for the provisional edition). IT IS PROVISIONAL AND THE CARD SAYS SO. On or after 17 September the Commission publishes the final November edition at elections.wi.gov/elections (its own words for where it will be); pull it, replace the committed workbook, re-run `wi/scripts/build_wi_polling_places.py`, and set `PROVISIONAL = False` — that one flag clears the wording on all 72 county files at once. THE CARD RETIRES ITSELF past `electionDate` (2026-11-03) whether or not anyone acts, so the failure mode of forgetting is a card that shows no polling place, never one that shows last year's. Watch the gates rather than the diff: the builder floors the pairing at 99% of LTSB's wards, refuses a ward claimed by two places, and prints every unpaired municipality — the 29 wards in 12 municipalities missing today are the list the final edition should shrink. A Jan/Jul LTSB filing window moves ward counts and the builder says so | 2026-08-27 (3,623 reporting units -> 2,475 places; 7,131 of 7,161 wards paired, 99.58%; 2026 General Election, PROVISIONAL) |
| When the Commission's clerk directory becomes fetchable, or on the next ask (STATEWIDE) | **The Wisconsin Elections Commission's municipal clerk directory** — the City or Village card's only statewide officeholder source, naming the clerk in all 608 cities and villages. It ships from a COMMITTED PDF (`wi/data/source/wec/WI Municipal Clerks Updated 8-4-2026.pdf`) because the published copy at elections.wi.gov/clerks/directory re-challenges automation on every interior path — measured over 17 attempts in two CI runs — so unlike the polling file there is no date to wait for, only a condition. THE TRIGGER IS THE HOST OPENING: if any interior /clerks/* path ever answers a plain client, this becomes a schedulable weekly job and the committed PDF stops being the input. Short of that, ask again when the ledger's next WEC round goes out. Replacing the file is one command — `python3 wi/scripts/build_wi_municipal_clerks.py` — and its gates do the checking, on BOTH halves it writes: 1,848 records, 58 MULTIPLE COUNTIES, a TOTAL join to TIGER's 608 places, a refusal to ship any name ending in a hyphen, and on the town side 1,242 subdivision town records against 1,239 shipped across 71 counties with the three clerkless towns gated BY NAME. Note the town half also re-reads TIGERweb's subdivision fabric, so a Census vintage roll shows up here first. NOTE THE CURRENCY SPREAD rather than the file date: the Commission stamps each municipality separately and they run 2017 to 2026, so a re-pull's value is the rows it moves, not the header | 2026-09-03 (395 pages, 1,848 municipalities; 608 of 608 places joined; clerk 608, deputy 361, phone 608, website 545; oldest record Lannon 2017-07-26) |
| Before each election (spring primary/general, fall partisan; Milwaukee) | **Milwaukee polling places**: the ward card's pairing is DATED to the city's voting-wards dataset edition, and polling places move election to election. Re-run `wi/scripts/build_mke_polling_places.py` when the CKAN resource's last_modified date moves (the monthly source report's row going stale is the backstop signal); its gates — ward set equal to LTSB's Milwaukee wards, every pair witnessed against the city's REST layer, the pinned one-place null-geometry tolerance — are what catch a redraw or a desynchronized surface | 2026-08-26 (356 wards -> 176 places, edition 2026-08-26) |
| Before each election (spring primary/general, fall partisan; Madison) | **Madison polling places**: the second city's pairing (`wi/scripts/build_madison_polling_places.py`) reads the city's open-data polling layer — one point per ward — which publishes NO edition date (no editingInfo, measured 2026-08-27), so the card dates the pairing as the day it was read and there is no last_modified signal to watch: this calendar row IS the refresh trigger. Re-run before each election; the gates — ward set equal to LTSB's Madison wards AND the city's own ward layer, every point bbox-gated inside Madison, one point per ward — are what catch a redraw, a desynchronized city surface, or a ward filing moving at a Jan/Jul window | 2026-08-27 (137 wards -> 108 places, read 2026-08-27) |
| Spring elections (April) | **Court of Appeals judgeships** elect one per district per year at most (Wis. Stat. 752.03), staggered six-year terms — the weekly wicourts scrape carries turnover as a roster-PR diff. A FAILED run means a seat count moved off 4/4/3/5 (a vacancy — read it) or the district composition itself changed (752.11 — rebuild the geometry in the same change) | 2026-08-25 (layer shipped; 16 judges) |
| Spring elections (April, odd and even years) | **Circuit judgeships** elect every April on staggered six-year terms, and vacancies fill by appointment year-round — the weekly wicourts scrape carries both without ceremony (a new name is a diff on the roster PR). What deserves a read is a FAILED weekly run: the scrape asserts the statutory circuit composition (Wis. Stat. 753.06 — the three two-county circuits), so a failure can mean the circuit map itself moved, which needs `wi/scripts/build_wi_circuit_courts.py` re-run in the same change | 2026-08-25 (layer shipped; 261 judges, 234 with direct phones) |
| USGS republication (roughly annual, unannounced) | The **police-station / fire-station / post-office** layers all ride the USGS National Map structures service live, which is republished wholesale without a schedule. Nothing to rebuild — the monthly source report's envelope returnCountOnly rows are where a count change (or the service going dark) surfaces. The known data caveat travels with the source: ghost records happen (a fire department defunct since 2020 is the verified example), which is why the cards present proximity and never a service claim | 2026-08-25 (July 2026 republication; 807 police / 1,743 fire / 1,244 post offices in the envelope) |
| Around each school year (late summer) and the annual library-system data (spring) | The **school-site / library** layers are PRE-BUILT from DPI's own AGO org, which rotates the school directory around each school year and refreshes the library directory from the annual public library system data. Re-run `wi/scripts/build_wi_school_sites.py` and `build_wi_libraries.py`, read the diff, ship it as a PR — the builders' gates (paging asserted against the layer's own count, the placeless-virtual-program skip, the mercator-meters LAT/LONG trap, state-bbox and floor checks) are what catch a rotation that changed shape rather than content. The monthly source report also watches DPI's catalog item (d383fe81275e46f2a5a5c4f1a0c2eb85) for the successor-item pattern | 2026-08-26 (initial build: 2,966 school sites — 2,138 public + 828 private, 152 placeless virtual rows skipped — and 482 library outlets, 481 with phones) |
| Unscheduled — the city republishes when the facts move | The **Milwaukee city quartet** (`mpd-district`, `mpd-squad-area`, `milwaukee-neighborhoods`, `tid-district`) is PRE-BUILT by `wi/scripts/build_milwaukee_city_layers.py` from city surfaces that change without a calendar: MPD redraws districts and squads when it reorganizes (not on the census cycle), the neighborhood map moves rarely if ever, and TIDs are CREATED AND CLOSED by Common Council action — the 79 will move, and the exact worksheet count moves with the rebuild. Nothing weekly to review — the monthly source report watches the ArcGIS doors and CKAN dataset pages, and a count or reachability change there is the trigger to re-run the builder, read the diff, and bump the sw cache. The builder's witnesses (exact key sets 1-7 / 25 / 190, area shares within 0.5%, the space-folded neighborhood key, and each squad sample-verified inside its hundreds-digit district) are what catch a republication that changed shape rather than content — an MPD redraw that breaks the hundreds-digit rule fails the squad gate loudly | 2026-08-26 (first build: 7 districts at 0.04% max share difference, 25 squads at 0.02% all verified in-district, 190 neighborhoods at 0.007% — one spelling divergence on the record) |
| Whenever any Wisconsin builder that fetches ArcGIS directly is next run | **THE ARCGIS GEOJSON EXPORTER SILENTLY UNNESTS HOLES, AND EIGHT BUILDERS STILL ASK IT TO.** Measured 2026-09-05: `f=geojson` returns a feature's interior rings as separate SHELLS instead of holes, so an area the publisher cut out becomes an area the layer claims. `fetch_layer` in `build_wi_supervisory_districts.py` was switched to `f=json` with a containment-aware converter, which fixes every caller of it — the NG911 quartet, the aldermanic build and the supervisory build. **These eight still fetch `f=geojson` themselves and are NOT covered**: `build_legislative_boundaries.py`, `build_madison_city_layers.py`, `build_metro_outline.py`, `build_milwaukee_city_layers.py`, `build_mps_school_board_districts.py`, `build_rusd_school_board_districts.py`, `build_wi_wtcs_districts.py` and `wi_county_board_scraper.py` (plus one probe in `validate_sources.py`). **`build_wi_wtcs_districts.py` WAS AFFECTED AND IS NOW REBUILT** (2026-09-05, the same day): its DPI layer returned 0 holes as geojson and **109** as Esri json, and the shipped file filled all of them; it now fetches `f=json` through the same converter and ships 109. `build_metro_outline.py` is MEASURED NOT affected, because TIGERweb's county layer returns 0 holes either way. **FIVE OF THE OTHER SIX WERE THEN MEASURED AND ARE CLEAN** (2026-09-05, ten layers over four hosts): `build_madison_city_layers.py` (wards 44 rings, associations 14, TIF 4 — identical both ways), `build_mps_school_board_districts.py` (2), `build_rusd_school_board_districts.py` (1) and `build_milwaukee_city_layers.py` (TID 1; its other three layers carry no interior rings) all keep every ring through the GeoJSON exporter, and `build_legislative_boundaries.py` is unaffected the weaker way — TIGERweb's legislative layer has no interior rings at all, so it says nothing about the exporter. **A CLEAN READING ON A LAYER WITH RINGS IS EVIDENCE; ONE ON A LAYER WITHOUT THEM IS ONLY AN ABSENCE.** **THE SIXTH IS MEASURED TOO, AND THE REASON IT WAS FIRST RECORDED AS UNMEASURABLE WAS WRONG.** `wi_county_board_scraper.py` was said to hit a different county-owned host per county; it carries only THREE layer specs, and line 4000 — its only exposed fetch, the other five setting `returnGeometry=false` — runs solely for a county with a `district_witness`, which is Lincoln alone. Lincoln's layer is on-premises (`maps.co.lincoln.wi.us`) and CLEAN, keeping both its interior rings through either format; the other two specs are read attributes-only. So all six Wisconsin builders are now measured. The one probe in `validate_sources.py` is unmeasured and its exposure runs the SAFE way: it tests ward dispatch entries for DISJOINTNESS, and an unnested hole only makes a geometry claim MORE ground, so the defect could produce a spurious overlap FAIL — loud — and never a missed overlap. **AND THE MEASUREMENT FOUND A PATTERN WORTH MORE THAN THE FIVE RESULTS**: every affected layer is hosted on ARCGIS ONLINE (`services*.arcgis.com`) and every clean one on an on-premises ArcGIS Server or TIGERweb — with LTSB as its own control, since the same publisher's on-prem ward layer keeps its ring while its ArcGIS Online supervisory layer drops one on Marathon alone. Twelve layers over six hosts is a strong signal and not a proof, so the per-layer test stays the rule; but it says WHERE TO LOOK FIRST. **AND THE EXPOSURE IS NOT WISCONSIN'S**: measured 2026-09-05, **35 files under `scripts/` (Illinois)** request GeoJSON from a server, plus 15 in `ia/`, 7 in `mi/` and 1 each in `ny/` and `ca/`. COUNT BOTH SPELLINGS — `f=geojson` inside a URL string AND the dict form `"f": "geojson"`, which is the commoner one; grepping only the first reports Illinois as 3 files instead of 35, which is exactly the mistake made here first. A layer with no interior rings is unaffected no matter how it is fetched, so the test is per LAYER and cheap: fetch both ways and compare hole counts | 2026-09-05 (supervisory districts rebuilt through the fixed path: 193 -> 327 holes in the shipped file, 68 of 1,590 features changed. **MEASURE AT AN INTERIOR POINT OF THE HOLE, NEVER ITS CENTROID** — a centroid falls outside a non-convex hole, lands in the owner's shell and reads as agreement, which under-counts the defect. At an interior point of each of the 277 source-restored holes, against the source as truth: **133 were wrong on main and are right now**, 34 were already right, and **108 ARE STILL WRONG** — every one of them smaller than 0.0073 km2, dropped by the 9% simplification in both versions, 98 of them where the source names no district at all. That residual is the price of shipping a simplified file and is not fixed by the fetch change; it is recorded rather than closed. **GEOMETRY AND CARD ARE DIFFERENT COUNTS AND THE CARD'S IS SMALLER**: the app renders the FIRST containing feature, so where main's geometry held two districts at one point the card already showed one of them by file order. Of the 134 holes the simplification keeps in the shipped file, the card's answer CHANGES at 63 — every one of the 63 moving off the cutout's own owner, none onto it, 2 of them onto 'no district' where the county files none — and at the other 71 file order had already put the true district ahead of the owner, so a reader saw the right answer from a wrong geometry. QUOTE 63 TO A READER AND 133 TO A BUILDER. **WTCS WAS REBUILT THE SAME DAY AND LEFT NO RESIDUAL**, because it ships the server's geometry as answered where this file is mapshaper-simplified at 9% retain: all 103 of its real holes are fixed, 0 points still name two colleges, and the card's answer changed at 59. A residual is a property of SIMPLIFICATION, not of the fetch fix) |
| Unscheduled — the OEC refreshes roughly weekly | The **NG911 quartet** (`fire-service`, `law-service`, `ems-service`, `psap-area`) is PRE-BUILT by `wi/scripts/build_wi_ng911_service_areas.py` from the OEC's statewide aggregate, which republishes as counties file — no calendar. **CORRECTED 2026-09-05 — THAT TRIGGER DID NOT EXIST.** This row used to say the monthly report's four returnCountOnly rows WERE the trigger. They were not: those rows asked the service for a count and `validate_sources.py` read only whether the endpoint answered, throwing the number away, and nothing anywhere held last month's figure — so no count change could be seen, by this row or by anybody. Three surfaces described the mechanism (this row, that file's own comment, the builder's docstring) and none implemented it. It exists now: the builder writes `wi/data/source/ng911/built-rows.json` on every run and the monthly check compares the live counts against it, WARNing when the shipped files have fallen behind; `build_wi_ng911_service_areas.py --check` is the same comparison on demand. When it fires, that is expected news — re-run the builder, read the diff, move the worksheet's exact feature counts and bump `cache_name` (these four are cache-first). What it CANNOT see is a county redrawing a boundary without changing its row count, and the UNFILED gate failing is the GOOD news this row exists for: a dark county (Iowa, Langlade, Vilas, Walworth, Jefferson's fire+EMS, Polk's partial law) completed its filing, so retire its entry from UNFILED and from gaps `ng911-fire-filings` / `ng911-law-filings` / `ng911-psap-filings` / `ng911-ems-filings` together. PSAP's future-dated Expire rows (11 at first build) mean its counts can also move when a dated filing simply expires — the drop-by-date rule handles it, but read the diff | 2026-09-05 (rebuilt: 1,046 fire + 639 law + **580** EMS + 95 PSAP areas, all at 100.000% name-set agreement; the 5 pinned authorities and Langlade UNCHANGED since 2026-08-26. The one moved answer was EMS: Waushara filed the City of Berlin's own ambulance service over the city's Waushara half, 2.1 km2, where this app still said Poy Sippi. A 20,000-point statewide sample saw ZERO changed answers on all four layers and that reading was WRONG — the rebuild changed 138 fire, 161 law, 19 PSAP and 79 EMS features (per FEATURE; a first count said 136 and 74, keyed on the agency name, which collapses agencies filing under several authorities); a 2.1 km2 change in a 169,000 km2 state expects fewer than one hit in 20,000 points, so the sample had no power and its null proved nothing. The instruments are the row COUNT and the layer's own `editingInfo.dataLastEditDate`, both now pinned in the sidecar, because a REDRAW does not move a row count — which is exactly what the OEC's 2026-08-31 edit did) |
| Rare — WTCS reorganization or a boundary amendment | The **wtcs-district layer** is PRE-BUILT from DPI's own layer by `wi/scripts/build_wi_wtcs_districts.py`; district boundaries move only when school districts reorganize across a WTCS line (Wis. Stat. 38.06) and the system last renamed a college in 2021 (Northwood — the layer's "2019" title is a name, not a vintage). The monthly source report watches the DPI layer and its AGO catalog item (the successor pattern); a count or item change is the rebuild trigger, and the builder's seat witness + Lake-Winnebago hole rule fail loudly if the fabric was redrawn rather than merely refreshed. **THE LAKE-WINNEBAGO RULE NEVER WATCHED PER-FEATURE HOLES AND CANNOT** (2026-09-05): it runs at the UNION level on `make_valid` copies, and `make_valid` re-nests an unnested inner shell as a hole, so it reads the same on either fetch format — and these districts TILE the state, so the union fills a district's hole from its neighbour anyway. The two gates that DO watch it run on the geometry as SHIPPED, before any `make_valid`: a floor of 90 interior rings (109 measured) and a pin of 6 on parts nested inside another part of the same district. **READ A RISE IN THAT PIN, NEVER RE-PIN IT** — the 6 are 4-vertex micro-parts of the server generalization, and they are NOT separable from real holes by shape: 22 of the 103 real holes are smaller than the largest of them and 29 have the same 4 vertices, so an area or vertex threshold would blind the gate to real cutouts | 2026-09-05 (rebuilt through `f=json`: 109 interior rings where the first build shipped 0, seat witness still 16/16, overlap 2.1e-4 deg², Lake Winnebago 0.0598 deg² present. At an interior point of each of the 103 real holes — 6 of the 109 are zero-area artifacts — the old file named TWO colleges at 100 and named one where the source names none at 3, agreeing at NONE; the rebuild agrees with the source at 103 of 103, and the CARD's answer changed at 59, the other 44 having already shown the true district because the app returns the FIRST containing feature. 27.9 km² of holes, six over 1 km², largest 2.26. Gap `wtcs-district-holes` retired) |
| Annual (DOR edition, early each year) + unscheduled Council action | The **Madison city pair** (`tid-district`'s Madison entry, `madison-neighborhood-assoc`, plus `madison-outline.json`) is PRE-BUILT by `wi/scripts/build_madison_city_layers.py` under a three-surface TIF agreement: the city's GIS layer, the city's TIF program page, and DOR's certified annual Active-TID workbook (`tid100wi-<year>`) must reconcile before anything ships. A NEW DOR EDITION is the standing trigger — re-run the builder against it, and expect the pins to fail deliberately: `KNOWN_UNDRAWN` fails the moment the city draws TID 55 (retire gap `madison-tid-undrawn` in the same change), and `KNOWN_CLOSED_IN_LAYER` fails when the layer catches up with the state's closures or a new district closes. The association layer moves when the city registers or lapses one (the monthly report's returnCountOnly row is the signal); the outline moves only if the city re-wards, which the ward-count gate catches | 2026-08-27 (first build: 14 TIDs of DOR's 15, TID 39/47 dropped as state-closed, TID 55 undrawn; 116 Active associations of 141 rows; 137 wards dissolved to a 4-part outline) |
| Same windows | The **ward layer** rides the same filings LIVE — no rebuild to run, because both its point query and its paged overlay hit LTSB's Current service directly. What to do instead: read the count off the monthly source report (the returnCountOnly ENDPOINTS row) and treat a change as expected news (Jan 2026 filed 7,138, July 2026 filed 7,161 — the same total as July 2025, a verified coincidence); it is the layer going unreachable or answering zero that is a failure. The supervisory builder's ward-reconciliation gate independently re-witnesses the ward↔district pairing on its own re-run | 2026-08-25 (layer shipped against the July 2026 filing) |

---
| Weekly, with `update-wi-county-clerk-roster` (the officer scrape runs there and in the board workflow) | **A 200 THAT WITNESSES NOBODY IS A FACT ABOUT THE FETCH.** On 2026-09-04 all four of Oneida's department pages answered HTTP 200 to the Actions runner with bodies carrying none of the four officers' names — Cloudflare bot management, `__cf_bm`; a Python-urllib UA is refused 403 outright — while the same pages witness all four from another client and did from the runner on 09-03. The scraper's floor correctly skipped the county; the BUILDER then dropped every phone, e-mail and page URL it had, because this file is rebuilt from the Blue Book each run and a county missing from the intermediate simply gets no contact fields. Bot PR #709 was closed rather than merged for it. The builder now PRESERVES a missing county's contacts from the shipped file, under the same witness rule `CARRIED_CONTACTS` uses — a contact is kept only where the officeholder's NAME is unchanged, because a predecessor's phone under a successor's name is wrong rather than stale | `wi/scripts/build_wi_county_officer_roster.py`, `wi/scripts/wi_county_officer_contact_scraper.py` | 2026-09-04 (preservation added; watch for a county that stays preserved for weeks — that is a real block and wants a `CARRIED_CONTACTS` row or an ask) |
| Weekly, same job | **PRESERVATION IS NOT THE SAME CLAIM AS A CARRY, and the card now says which.** A `CARRIED_CONTACTS` county's pages are never re-read (its host disallows this crawler); a PRESERVED county is retried every week and merely did not answer usably. Telling a reader the rows "are not re-read" would understate a preserved county, so the builder sets `contactRetried` and the card swaps in "could not be re-read this week". Oneida was chosen as a preservation rather than a carry or a measured block deliberately: a permanent hand-maintained carry would freeze data a good run should refresh, and a recorded block would claim something the 09-03 run disproves | `wi/index.html` (County card), `wi/scripts/build_wi_county_officer_roster.py` | 2026-09-04 |
| Monthly, when a county's contact looks thin | **THE 350-CHARACTER WITNESS WINDOW IS LOAD-BEARING AND A BLANKET WIDENING IS WRONG.** Washington's sheriff publishes its main line inside the office's own address block, 392 characters after the name, so the window ended 42 short and the county shipped no phone (bot PR #714, closed). Widening the default was MEASURED before being refused: across all 25 page-mode counties at 450/550/700, thirteen of 125 (county, office) pairs move — six gain a plausible county contact and five are corrupted, worst of them MARQUETTE'S REGISTER OF DEEDS, which goes from its own number to a run of third-party e-recording vendors (`support@hopdox.com`, `eRecordSupport@Indecomm.net`, `erecording@cscglobal.com`). So `WINDOW_SPAN` widens per (county, office) and each row is read in context first. Six further measured gains are recorded in that table's comment and NOT taken — Langlade's coroner, Trempealeau's and Waupaca's clerks of circuit court, Clark's register of deeds — each wants its own reading | `wi/scripts/wi_county_officer_contact_scraper.py` (`WINDOW_SPAN`) | 2026-09-04 (Washington's sheriff and DA taken, both read in context) |
| Whenever the preservation branch changes | **THE PRESERVED-COUNTY CARD HAS NO AUTOMATED COVERAGE, and that absence is exactly how it shipped broken once.** The first version set `contactAsOf`/`contactRetried`/`contactAsOfWhy` and NOT `contactChecked` — which is what `wi/index.html` gates the whole as-of note on — so a preserved county rendered its phone and page rows with no disclosure at all and the card's own `contactRetried` branch was unreachable. The smoke test could not have caught it: it runs against the shipped data, which normally carries no preserved county. Verified instead by hand — build with a county's entry replaced by a `skipped` record, then render two points in a browser and compare: the preserved county must say "could not be re-read this week" and carry the scrape's reason, and a control county that WAS read must still say "checked weekly". **Pick a control that actually has contacts** — the first attempt used Marathon, which has none, so it correctly showed no note and the check failed on the harness rather than the code | `wi/scripts/build_wi_county_officer_roster.py`, `wi/index.html` | 2026-09-04 (verified by hand: Oneida preserved vs Dane weekly, both branches rendered) |
| Monthly, with `wi-validate-sources` | **THE THREE NEAREST-N LOADERS MAKE ONE REQUEST EACH AND IGNORE `exceededTransferLimit`**, so each is correct only while its envelope holds fewer records than the USGS service's `maxRecordCount`. Michigan measured that shape silently serving 2,000 of 2,820. Measured here 2026-09-04 against the app's exact URLs: post office 1,107, law enforcement 728, fire/EMS 1,548, cap 2,000 — **none truncating**, fire/EMS closest at 77%. So nothing was converted to paging; instead the count is now CHECKED rather than merely fetched. The two existing count endpoints asked for `returnCountOnly` and threw the number away, and post office had no count row at all. The cap is READ LIVE from the layer, not pinned, because a service that LOWERS its cap breaks this the same way and a hardcoded 2,000 would never notice | `wi/scripts/validate_sources.py` (`_check_single_request_count`), `wi/index.html` | 2026-09-04 (FAIL/WARN/OK bands all witnessed against the live service; a FAIL means page the loader with the engine's `loadArcGISPaged`) |
| Weekly, whenever a scraper gains a host | **THE ROBOTS SWEEP'S MODULE LIST IS DISCOVERED NOW, AND WIDENING IT FOUND A DISALLOW WE HAD BEEN CROSSING.** The gate named three modules by hand while EIGHT other scheduled scrapers sat outside it — its own comment called that "an open gap rather than a statement that their hosts permit anything", and it was right: `milwaukeemaps.milwaukee.gov` publishes `User-agent: * / Disallow: /` (Googlebot excepted) and `wi_alderperson_scraper` fetched its alderman layer every week as Milwaukee's PRIMARY source, six tries with backoff, under a comment calling it "the measured flaky host". It was not flaky. The subject is now every `wi/scripts/*_scraper.py` a workflow actually RUNS, so a scraper written tomorrow is swept the day its workflow lands; The subject is now every `wi/scripts/*_scraper.py` AND `build_*.py` a workflow runs, PLUS `validate_sources.py`'s own PROVENANCE and ENDPOINTS rows, which the monthly job requests on the 1st — globbing scrapers alone left the builders' fetches (Open States, the two RUSD ones) outside a contract that says "every address this instance requests on a schedule". The sweep refuses to pass having found fewer than two modules | `wi/scripts/validate_robots.py` (`scheduled_scraper_modules`) | 2026-09-05 (widened; the Disallow found and retired in SIX places, gate green) |
| Weekly, with `update-wi-alderperson-roster` | **Milwaukee's roster now comes from Legistar, which the scraper was ALREADY fetching as its currency witness.** No source was added and no data lost: `OfficeRecordTitle` carries the district ("3rd District") corroborated by `OfficeRecordSort`, and surnames matched the shipped roster 15/15, but SIX of fifteen DISPLAY names change on the next refresh — Pratt, Chambers Jr., Coggs, Moore, Perez and Stamper II. Three lose a middle initial ("Andrea M Pratt" to "Andrea Pratt"), two gain a period, and Stamper LOSES the generational suffix ("Russell W Stamper, II" to "Russell W. Stamper"). That is a real if small loss and it is not smoothed over: the alternative was preferring the CKAN spelling when CKAN answers, which would make a person's name flap run to run depending on whether a host replied. The CKAN shapefile on `data.milwaukee.gov` stays as corroboration rather than a fallback: its robots.txt permits the path, but it 403s from some clients, and a host that declines one client on one day must not fail a build whose data is already sound | `wi/scripts/wi_alderperson_scraper.py` (`scrape_milwaukee`) | 2026-09-05 |
| Whenever `METRO_BBOX` moves | The three record-count URLs hardcode the envelope `-92.94,42.44,-86.19,47.36`, which is a COPY of `index.html`'s `METRO_BBOX`. Widen the app's envelope and the gate would go on measuring the old rectangle and report comfortable headroom for a request that had started truncating — a gate measuring a different question than the app asks is worse than none, because it reports OK. `check_count_envelope_matches_index` fails when the two diverge | `wi/scripts/validate_sources.py` | 2026-09-05 (added; witnessed failing on a one-digit bbox change) |
| Monthly, with `wi-validate-sources` | **A `blocked` FLAG DOES NOT STOP A REQUEST, AND A ROBOTS DISALLOW NEEDS ONE THAT DOES.** `blocked` exists for a host that REFUSES us — Akamai, a captcha — where the request is how we learn the refusal still stands, so `check_provenance` fetches and only inverts the reading. A robots Disallow is the opposite situation: the host is ASKING us not to request. `robots_disallowed` skips the fetch entirely and reports the recorded policy, with the shipped data file still checked. It covers all SIX `milwaukeemaps.milwaukee.gov` provenance rows — the Disallow is `/`, so it is every path, and one of the six was `election/alderman/MapServer/0`, the exact URL the alderperson scraper stopped fetching in the same change. Retiring it there and leaving the monthly probe on it would have been the fix in name only. Nothing is lost by not asking: all six layers ship as committed files from an operator rebuild and the app never touches the host at runtime | `wi/scripts/validate_sources.py` (`ROBOTS_MILWAUKEEMAPS`) | 2026-09-05 (witnessed: 64 provenance probes, zero to that host) |
| Whenever a gate imports another gate | **`validate_sources` EXISTS IN EVERY INSTANCE AND AT THE ROOT, AND A BARE IMPORT PICKED THE WRONG ONE.** Sweeping the monthly manifest meant importing this instance's `validate_sources`, and by the time that ran the modules already swept had put the ROOT `scripts/` at the front of `sys.path` — one of them imports the fleet-wide `undeliverable` from there. So `import validate_sources` resolved to ILLINOIS's, and a Wisconsin run began reporting Kane and Coles County hosts as Wisconsin's scheduled fetches. Caught only because two Illinois hostnames appeared in the output (389 URLs where 311 was right). It is loaded BY PATH now, with a guard that refuses a module from any other directory. `validate_workflow_deps.py` already carries this lesson for its own closure walk — "a name that exists in both cannot be walked into the wrong tree" — and it applies to any cross-gate import | `wi/scripts/validate_robots.py` | 2026-09-05 (fixed; 0 Illinois hosts in a Wisconsin sweep) |
| Whenever a builder gains a fetching subcommand | **ROBOTS GOVERNS CRAWLING, NOT LINKING.** Widening the sweep to `build_*.py` immediately reported two counties as disallowed — Jackson and Polk, from `build_wi_county_board_directory.COUNTY_SITES` — and the report was WRONG: that table is 72 card-link targets, and only the operator's `--probe` fetches them, while CI runs `--check`, whose own step comment says "and fetches nothing". A gate that cannot tell a link from a crawl ends up arguing the app must stop linking a county to its own website. `NOT_FETCHED_BY_MODULE` records the exclusion and CHECKS it: every workflow running that module must invoke `--check` and never `--probe`, so putting the fetching flag in CI fires the gate | `wi/scripts/validate_robots.py` (`NOT_FETCHED_BY_MODULE`) | 2026-09-05 |

## Per-election — the seats above the boundaries

| When | What | Last done |
|---|---|---|
| After each Wisconsin general (November, even years) and any special election | The chamber rosters turn over; the weekly Open States refresh picks it up, but verify the first post-election PR against the Legislature's own directory before merging | 2026-08-25 (initial build: 33/33 + 99/99) |
| After each **spring election (April)** | **MPS school board directors** elect on staggered four-year terms (the shipped roster's expirations run April 2027 and April 2029), and the weekly scrape carries turnover with the committee-list witness. The GEOMETRY moves once a decade: the districts were adopted 2022-02-25 after the 2020 census, so re-run `wi/scripts/build_mps_school_board_districts.py` after the post-2030 reapportionment — its CKAN-shapefile witness fails loudly if the two city surfaces ever disagree mid-decade | 2026-08-26 (first build: 9 directors, all with terms; area shares witnessed at 0.04% max difference) |
| After each **spring election (April)** | **Racine Unified Board of Education members** elect on staggered three-year terms (the shipped roster's expirations run April 2027, 2028 and 2029), and the weekly scrape carries turnover as a roster-PR diff. **DISTRICT 2 IS VACANT** as of 2026-08-20 by RUSD's own notice and the card says so; the seat filling is real news, and the roster refuses a SECOND vacancy so somebody reads the page rather than the diff. The GEOMETRY does not move at an election — it moves when RUSD redraws or a municipality re-wards, which is the semiannual row below | 2026-09-03 (9 seats: 8 named with role, phone, e-mail and term; District 2 vacant) |
| After each **spring election (April, even years — and odd years for the cities that stagger)** | **Alderpersons** in the 18 rostered municipalities elect at spring elections, and the weekly scrape carries turnover — but the Kenosha witness pins THIS canvass: after each spring election, point `KENOSHA_CANVASS` in `wi_alderperson_scraper.py` at the county's new certified results document and re-verify, and re-read any `KENOSHA_CANVASS_WINS` pin (each is self-retiring — the build fails the day the GIS catches up). Madison's vacant-seat list changes at elections and appointments; the per-district pages carry it without ceremony | 2026-09-05 (207 named + Madison D1 vacant across 18 municipalities; the Kenosha D14 pin self-retired 2026-09-03. TWO OF THE TWELVE NUMBER THEIR SEATS BY WARD — Menomonie and Viroqua — and ward-is-district is witnessed against LTSB on every run, so a re-warding fails those two loudly instead of moving every name one seat) |
| **Next county-officer run after 2026-08-29** | **EXPECT CHAIRS TO BE WITHHELD, AND DO NOT RAISE THE CAP TO GET PAST IT.** The union of PRs 617-636 took `county-board-members.json` from 33 counties to 50, and the officer builder reconciles the Blue Book's board chair against that file every run: a book chair absent from a COMPLETE roster is withheld with the reason stated. Seventeen counties just gained a complete roster, so seventeen more can now contradict the book — Manitowoc already does (the book names Tyler Martell; the county's own page names 25 supervisors and he is not among them). `build_wi_county_officer_roster.py` refuses to write when more than SIX chairs are withheld, on the measured grounds that the state was 2 and a spike is likelier a name-matching defect than a mass eviction. That reasoning was written against 33 counties. If the next run trips it, the cap is not the thing to change first: re-measure the withheld counties one by one against their own pages, and only then raise it deliberately with the new measured state written down. Runs from `.github/workflows/update-wi-county-clerk-roster.yml` (Fri 14:30 UTC); read the withheld list in the run log before merging the PR. | 2026-08-29 (predicted, not yet observed — the officer builder needs a Blue Book cache the union could not run) |
| After each **spring election (April, even years)** | County supervisors are elected to two-year terms then, so all 72 shipped rosters — 1,590 seats — turn over at once. NINE ARE THE ONES THE WEEKLY RUN CANNOT SIMPLY CATCH, and they need different things. SIX OF THE NINE ARE THE ROBOTS SET — Jackson, Richland, Rusk, Polk, Dunn and Pepin — whose sites serve this client fine and whose robots.txt tells it not to crawl, so their 116 seats can ONLY be refreshed by a person re-reading each county's own board page in a browser and updating `DOCUMENT_ROSTERS`; nothing automated will ever catch an April turnover for them, and their cards will visibly age until somebody does. The same applies to the office contact of Ashland, Dunn, Pepin and Polk in `CARRIED_CONTACTS`, with one safety net the seats do not have: each carried office records the name its page showed at capture, so if the Blue Book renames that officeholder the builder DROPS the contact rather than showing a predecessor's number — watch the build log for a `carried contact DROPPED` line, which is the signal to go re-read that page by hand. THE OTHER THREE ARE BLOCKED RATHER THAN ASKING: TAYLOR's host answers a captcha and its other hosts do not resolve, so its seventeen ride a dated document and only a human re-reading co.taylor.wi.us/directory/county-board/ can refresh them. LAFAYETTE's and LA CROSSE's hosts answer a Cloudflare challenge but its page PARSES under a pinned reading, so the weekly run re-tries it LIVE every time — check the log for an `ok Lafayette ... READ LIVE` line, which means the county can move from DOCUMENT_ROSTERS to COUNTIES and stop needing a capture at all. Both runs print their document's age every week, and after an April election that age is the thing to act on. The weekly run picks it up, but verify the first post-seating PR against a couple of counties' own pages before merging — this is also when counties reorganise their board pages, which is what breaks the pinned reading direction. The two county-GIS rosters (Milwaukee, Racine) need their layers' own edit dates re-checked PAST the election before names ship — Milwaukee's Legistar witness fails the county loudly if the layer lags | 2026-08-29 (50 counties, 1,139 seats: 1,129 named, 10 vacant. Seventeen counties joined in one merge of seventeen independently built changes — read the county-officials gap record before touching the scraper, because the union broke in four places none of the parts could see, and the guard against a county silently vanishing from the roster was one of them) |

---

## Per-decade — the census redistricting cycle

| When | What | Last done |
|---|---|---|
| After each decennial census (next: 2031–2032) | Congressional + legislative districts redraw: re-run `wi/scripts/build_legislative_boundaries.py` (all three targets), confirm the district counts against the apportioned delegation and the 33/99 chambers, and re-verify the smoke anchors against the rebuilt geometry | 2026-08-25 (chambers built at 100.00% agreement) |
| Mid-decade — Wisconsin specifically | Wisconsin's legislative maps have moved OFF-cycle before (the 2024 remap after Clarke v. WEC). A chamber-map change outside the census cycle is plausible here in a way it isn't in most states: any news of a court-ordered remap means re-running the chamber builds and anchors without waiting for the decade | _(watching)_ |
| Same cycle | County subdivisions / places / school districts shift in TIGERweb continuously; the live layers pick changes up on their own, but the PRE-BUILT files (`state-counties.json`, `school-districts-unified.json`) need a rebuild | _(never)_ |
| Before any new use of the Blue Book, and whenever its edition rolls | **THE BLUE BOOK'S REUSE TERMS ARE UNESTABLISHED, ON DATA ALREADY SHIPPING.** `wi-county-officers.json` (72 counties x 7 offices) and `wi-county-clerks.json` are built weekly from the volume's own county-officer tables, and its front matter reserves all rights to the Joint Committee on Legislative Organization while the Legislature sells the book through its Document Sales Unit. Measured 2026-09-02: there is **no recorded reasoning anywhere** for why that notice does not apply — nothing in `wi_county_clerk_scraper.py`, `build_wi_county_clerk_roster.py` or `build_wi_county_officer_roster.py`, nothing in `LICENSE-DATA.md`, and the worksheet calls it "a state publication", which is an assumption rather than a finding. This project's own rule cuts both ways — an All-rights-reserved string was a REQUIRED NOTICE for Des Moines's ward layer and a real block for Piatt's GIS — so it is asked, not inferred. **Ask 7** in `docs/ASK_DRAFTS.md` goes to the Bureau's published reference desk and covers the existing use plus one extension worth having: section `190` publishes each city's and village's year of incorporation, its county or counties, and the county seats, none of which ships today. A NO is a real outcome and the ask says so — the county card's officer rows would have to come out | 2026-09-03 (measured 2026-09-02; **ASKED 2026-09-03** to lrb-reference-services@legis.wisconsin.gov, sent by the operator. Asked is not answered: follow up 2026-09-24, then 2026-10-08, then record UNRESPONSIVE. It gates only the two Blue Book builds already shipping and the section 190 extension — nothing else in Wisconsin waits on it) |
| Before any purchase from the League, and whenever its annual Directory edition rolls | **THE LEAGUE'S MUNICIPAL DIRECTORY IS SOLD RATHER THAN MEMBER-GATED, AND ITS PRICES ARE MEASURED.** It is the only source found that names Wisconsin's municipal governing bodies together — recorded gap `municipal-officers`, whose per-municipality alternative was measured on 2026-09-05 and is a poor route rather than an untried one (444 of 608 city and village sites readable, 238 pairing an executive title with a name, and that 238 a triage whose names are not data). From the League's own public order page, read 2026-09-05: `Governing Bodies` ~3,500 contacts **$180.00**, and the annual `Directory of Cities and Villages` **$500.00** — whose scope line says **member** city and village elected officials, narrower than the `/1236/` page's "all city and village elected officials", so what the $500 actually covers is itself part of the question. **THE BLOCKER IS THE TERMS AND NOT THE PRICE**: the page grants a buyer "exclusive use" without saying what that excludes — whether it leaves room for naming an officeholder on a free public map is precisely what Ask 17 asks, and is not answered here — and only its Clerks list carries a phone or an e-mail at all — the one officer WEC already publishes for nothing. **Ask 17** in `docs/ASK_DRAFTS.md` puts the terms question first, which is the Jo Daviess rule (written permission BEFORE signing or paying). A NO is a real outcome: the gap record would call the League route CLOSED rather than unexplored | **DRAFTED 2026-09-05, NOT SENT.** The agent never sends, and the purchase is the operator's decision. Nothing waits on it — the clerk ships statewide today and the eighteen rostered municipalities need nothing from the League |
| **After each spring election (April, even years)**, and whenever a withheld municipality's site becomes readable | **Milwaukee County's 19 municipal executives ship WITNESSED, and the witness is the layer's only defence against its own age.** The county's `Milwaukee_County_Municipal_Executives` layer (FeatureServer **42**) is the only source in Wisconsin that names mayors and village presidents as data, and its `dataLastEditDate` is **2024-07-30** — before the April 2026 election that fills those offices. So `wi_municipal_executive_scraper.py` fetches each municipality's OWN page and a name ships only where that page names the person beside the word mayor or president. **At first build 9 of 19 witnessed**; Cudahy and West Milwaukee were CONTRADICTED (page reads fine, name absent — the election showing up), and eight were unfetchable (Bayside, Hales Corners, South Milwaukee and West Allis 404 on the layer's OWN link; Milwaukee, Oak Creek, River Hills and Wauwatosa refuse this client 403 — an access control, not defeated). **What the weekly run is for**: a 403 or 404 clearing turns a withheld name into a shown one with no code change, and a name that STOPS witnessing is an election or a resignation. After an April election expect several to flip at once — read the diff against the municipalities' own pages before merging, and never lower `MIN_WITNESSED` to get a green build Refreshed weekly by `.github/workflows/update-wi-municipal-executive-roster.yml` (Thu 16:30 UTC). | 2026-09-03 (first build; 19 municipalities, 9 witnessed, 10 withheld with the reason) |

---

## Grow this file

Every new layer ships with a row here naming when its source moves (school-year datasets
rotate every summer; county precinct files change per election; rosters change per election
and per resignation). A source with no row is a source nobody is watching.
