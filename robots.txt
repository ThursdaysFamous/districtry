# As a condition of accessing this website, you agree to abide by the following
# content signals:

# (a)  If a Content-Signal = yes, you may collect content for the corresponding
#      use.
# (b)  If a Content-Signal = no, you may not collect content for the
#      corresponding use.
# (c)  If the website operator does not include a Content-Signal for a
#      corresponding use, the website operator neither grants nor restricts
#      permission via Content-Signal with respect to the corresponding use.

# The content signals and their meanings are:

# search:   building a search index and providing search results (e.g., returning
#           hyperlinks and short excerpts from your website's contents). Search does not
#           include providing AI-generated search summaries.
# ai-input: inputting content into one or more AI models (e.g., retrieval
#           augmented generation, grounding, or other real-time taking of content for
#           generative AI search answers).
# ai-train: training or fine-tuning AI models.
# use:      how AI systems may consume the content (immediate, reference, or full).

# ANY RESTRICTIONS EXPRESSED VIA CONTENT SIGNALS ARE EXPRESS RESERVATIONS OF
# RIGHTS UNDER ARTICLE 4 OF THE EUROPEAN UNION DIRECTIVE 2019/790 ON COPYRIGHT
# AND RELATED RIGHTS IN THE DIGITAL SINGLE MARKET.

# What this site asks for, and why. Every district boundary and every
# officeholder name here comes from a government publisher and is cited to it,
# so there is nothing secret to withhold and every reason to want the answers
# found: a reader typing "who is on the LaSalle County board" should get the
# answer. So search is welcome, and so is grounding an answer in these pages —
# ai-input=yes, use=reference — on the terms the signal already names: by
# reference, with a link back to the page that names the person, so the reader
# can check the county's own source. Training is reserved.
#
# This is the same reading districtry applies to OTHER people's sites. Sixteen
# of the 386 hosts its scrapers read carry this line; one of them is
# sheboyganwi.gov's search=yes,ai-train=no,use=reference, read on 2026-09-05,
# and the fetch was kept because nothing here trains on a page and naming an
# officeholder with a link back is the reference use the signal permits.
# scripts/robots_policy.py is the one reader that decides it, and its
# --selftest asserts that reader parses THIS file too.
#
# No crawler is disallowed by name. A site that wants its answers found does
# not gain anything by refusing the clients that find them, and the training
# reservation above is a statement of rights rather than an access control.
#
# /llms.txt (GENERATED by scripts/build_llms_txt.py) is the short guide to what
# is here and where each answer comes from.

User-agent: *
Content-Signal: search=yes, ai-input=yes, ai-train=no, use=reference
Allow: /

Sitemap: https://districtry.com/sitemap.xml
