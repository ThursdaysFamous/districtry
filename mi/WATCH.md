# WATCH.md — redistricting watch calendar

The one place the dates live: *when to look* for boundary and roster changes in this
instance's sources. The repo's `docs/REDISTRICTING_RUNBOOK.md` is *what to do* when a
boundary changes. Update the "Last done" column each time you complete a row — a
checkpoint with a stale date is a checkpoint that didn't happen.

---

## Standing (automated — verify, don't perform)

| Cadence | What | Where | You do |
|---|---|---|---|
| Weekly (Mon 15:30 UTC) | U.S. House (MI) roster refresh | `.github/workflows/update-mi-congress-roster.yml` → PR on change | Review + merge the PR; a week with a surprise diff is worth a look at the source |
| Weekly (Tue 15:30 UTC) | Michigan Senate + House roster refresh (Open States mi.csv + the Senate's own all-senators directory) | `.github/workflows/update-mi-legislature-roster.yml` → PR on change | Review + merge the PR — especially after a Michigan general (November, even years) and each January seating |
| Monthly (1st, 16:00 UTC) | Source freshness for every dataset this instance depends on | `.github/workflows/mi-validate-sources.yml` → tracking issue on WARN/FAIL | Read the issue; the job stays green on purpose |
| Monthly, with the run above | **The commissioner layer's own republish signal, which its endpoint cannot give.** The service carries no `editingInfo`, no `lastEditDate` and no date field, so a republish is invisible there | `arcgis.com/sharing/rest/content/items/4c8d0d854ac04d8787cb3cf6dab7fbec?f=json` — compare `modified` against **1764783222000** (2025-12-03) and the title's own `vNN` suffix against **v25** | If either moved, re-run `mi/scripts/build_mi_commissioner_districts.py` and diff: the count guard (619/83) fails loudly on an apportionment, and a quiet change is a correction |
| Monthly, with the run above | **Detroit's four council-district services, and which of them is in force.** The city publishes `Council_Districts` (titled "Current", live since 2016), `city_council_districts_2026` (what ships), `city_council_districts_2013` (created December 2025 — an archive) and `NewDistrictBoundariesOption6` (reads like a rejected draft; measured 2026-09-05 it is the SAME plan redigitised, 99.575% point agreement, and it is the witness naming the adoption date). Their NAMES do not settle it; the builder does, and gates it both ways: 'Current' must match what ships, the 2013 archive must differ, the shipped plan must balance on Census 2020 (4.28% measured) and the archive must not (13.51%). The plan's own dates are in the shipped service's `description`: adopted 2024-02-06, effective 2026-01-01. | `python3 mi/scripts/build_mi_detroit_council.py` | Re-run the builder. If it refuses because the archive has come into balance too, the test has stopped discriminating and the answer is a human reading the city's own adoption record — never a loosened threshold. Last done 2026-09-05. |
| Monthly, with the run above | **Grand Rapids's ward geometry is a 2018 file, and its currency is a MEASUREMENT rather than a claim.** `CGR_Wards` was last edited 2018-01-24 — before the census it balances on — while its item description says it is "maintained to reflect the most current adopted ward configuration". The gate is Michigan's OWN 2026 precinct layer, which carries a WARD column assigning the city's 59 precincts 20/20/19; dissolved by it, it agrees with the city's polygons on 99.575% of 4,000 points. | `python3 mi/scripts/build_mi_grand_rapids_wards.py` | Re-run the builder. It refuses if that agreement drops below 99% or the per-ward precinct counts move — which is what a ward redraw looks like from here. On refusal, find the city's adopted plan before touching the threshold; the threshold is not the thing to move. It also refuses if the city's duplicate ward service diverges from the one that ships — ONE of SEVEN siblings in the org (eight ward services in total, measured 2026-09-05 by sweeping all 681 feature services); the other six are deliberately ungated because three are single-ward layers and two already differ at the city edge. Last done 2026-09-05. |
| Weekly, by CI | **Grand Rapids's City Commission — six of seven seats.** The city elects two commissioners per ward plus a mayor and publishes one Ward 1 commissioner. THAT SEAT IS VACANT — the city's news post of 2026-04-17 names the resignation that created it — and no city source states how it was resolved, so the card names the vacancy and stops. WHEN A SUCCESSOR APPEARS the roster count goes 6 -> 7 on its own and the card's shortfall row disappears with it; nothing needs editing in the card — its shortfall row and the cause it states both render from the roster — but `VACANCY_SOURCES` in the scraper, the two script docstrings, the worksheet `people` note and the expansion plan all describe a resolved situation at that point and should be retired. `update-mi-grand-rapids-council-roster.yml` refreshes it; the roster carries `seatsPerWard` and a per-ward `vacancies` entry, so the card states the shortfall and its cause from the DATA rather than from a literal. UNLIKE DETROIT THIS SOURCE IS DIRECTLY READABLE, so a refusal here is a real change, not a known block. | `python3 mi/scripts/mi_grand_rapids_council_scraper.py && python3 mi/scripts/build_mi_grand_rapids_council.py` | If the run fails: check whether the city has started challenging this client (the scraper says so explicitly) — and if it has, the Detroit scraper next door is the Archive-rung pattern to copy. If Ward 1's second seat appears, nothing needs changing: the card counts what ships. Last done 2026-09-05. |
| Monthly, with the run above | **Michigan's precinct layer has a sibling that dates itself to 2002.** The org's `Proposed_Precincts` self-describes as adopted 2002 and updated 2025; the shipped `2026_Voting_Precincts` is the one this app uses and the one Grand Rapids's ward-currency gate reads. Recorded so a future sweep does not mistake the older service for a newer plan, which is the trap that made Detroit's four services worth measuring. | `python3 mi/scripts/build_mi_precincts.py --check` | If a future cycle's precinct service appears, WATCH THE ORG rather than the URL — a new cycle arrives as a NEW SERVICE. Last done 2026-09-05. |
| On the next fleet-wide refactor | **`registerCountyLayer` now exists in THREE instances** — Illinois, Iowa and (since 2026-09-05) Michigan — and that is a real debt, not a decision this repo is happy with. Its honest home is `engine/`, spliced by `compose_app.py`. | `python3 scripts/compose_app.py --check` | Promote it to an engine fence in a PR whose subject is the engine, not a city: doing it inside a city PR means refactoring Illinois's 18 dispatched concepts as a side effect. Until then, a fix to one copy must be ported to the other two by hand. |
| Weekly, by CI | **Detroit's nine council members, read through the Internet Archive.** detroitmi.gov refuses this client (Cloudflare managed challenge, both rungs), so `update-mi-detroit-council-roster.yml` reads a public archive of the city's own council page. THE FAILURE MODE THIS ROW EXISTS FOR IS A SILENT ONE: if the Archive stops capturing that page, the newest snapshot ages past the scraper's 45-day ceiling and the run FAILS rather than shipping stale officeholders — which is correct, and is a human's cue to check whether the city has started answering directly again. | `python3 mi/scripts/mi_detroit_council_scraper.py && python3 mi/scripts/build_mi_detroit_council_roster.py` | If the ceiling refuses: check whether detroitmi.gov now answers (the scraper's first rung tries it every run, so a passing direct rung retires the archive hop and its `EXPECTED_UNREACHABLE` entry in `scripts/validate_card_links.py`). If it does not, request a fresh capture of the page rather than raising the ceiling. Last done 2026-09-05. |
| Monthly, with the run above | **Lansing's ward geometry is BUILT AND UNSHIPPED, on a licence question.** The city publishes the current plan three times; the only clean four-feature copy (`VotingWards`) states no licence, while the same plan one item away states CC BY-NC 4.0. Silence from one publisher is not a grant, so the operative licence is NC and the call is the operator's. Currency is NOT the issue: against the state's 2026 precinct WARD column (27 precincts, 7/7/6/7) the wards agree on 99.893% of sampled points. | `python3 mi/scripts/build_mi_lansing_wards.py` | Re-run it. It re-reads the city's stated licence EVERY RUN and fails if the clause has moved in either direction — a relicence to open terms is exactly the news that would unblock this. It measures and refuses to write without `--licence-approved`; the write path is deliberately unimplemented so the attribution the licence requires gets written in the same change as the geometry. Last done 2026-09-05. |
| Monthly, with the run above | **Ann Arbor has two unexhausted routes and neither was reachable from here.** The readable ward layer is superseded (47 features against the state's 41), the same org's `City Council Districts` is Los Angeles, and that org is not the city's publisher. The city's own portal answers but is a JavaScript application, and `gis.a2gov.org` fails at this environment's egress proxy, which says nothing about the host. | read `data.a2gov.org` in an ordinary browser on an ordinary network; probe `gis.a2gov.org` from a network whose proxy can tunnel it | BOTH WERE TRIED ON 2026-09-06 AND BOTH FAILED ON THIS ENVIRONMENT, not on the city: headless Chromium cannot reach the portal at all (it does not use the agent proxy), and the GIS host still 502s at that proxy. Every documented catalog API for the portal's platform 404s too, so the browser is genuinely the missing piece. Still not a measured shut — if either route yields a current five-ward boundary, Ann Arbor becomes a `city-ward` dispatch entry with no new layer. Last done 2026-09-06. |
| Per election cycle | **Sterling Heights' council form is unsettled and every route tried is named.** Four signals point at at-large — the state's own precinct fabric assigns it no ward numbers where the other three cities carry them — but an absence in one publisher's column is a fact about that publisher. | see the gap record `sterling-heights-council-form` | The charter is the first witness (its own site's charter link serves a water-billing portal, and Municode needs a browser); a certified Macomb canvass is the second, and Clarity carries the county slot but publishes an EMPTY election list. If the council is at large there is no gap to close — only a record to correct, because an at-large body never gets a polygon. Last done 2026-09-05. |
| Monthly, with the run above | **Warren's five wards ship, and the builder refuses if the city ever states terms.** The layer sits at SERVICE INDEX 32, not 0 — a hardcoded `/0` returns HTTP 200 with no `features` key and dies with a KeyError that reads like an outage. Currency is measured against the state's 2026 precinct WARD column (53 precincts, 10/10/11/11/11) at 99.550%, and the five wards tile the city exactly: 139,387 against the Census place's 139,387, zero blocks on the wrong side. | `python3 mi/scripts/build_mi_warren_wards.py` | Re-run it. It re-reads the item's terms FIRST and fails if the city has added a licence, so geometry is never republished under terms nobody has read; it also asserts the service's single layer is still named 'Council Wards 2023'. On a currency failure find the city's adopted plan — the threshold is not the thing to move. Last done 2026-09-06. |
| Per election cycle | **Dearborn is the second city measured as probably at-large**, after Sterling Heights, and by the same test: the state records no ward number for any of its 42 precincts where every other covered city carries them, and the catalogue holds no ward service. | see the gap record `dearborn-council-form` | The charter or a certified Wayne County canvass settles it. If the council is at large there is no gap to close, only a record to correct — an at-large body never gets a polygon. Last done 2026-09-06. |
| Monthly, with the run above | **Flint's nine wards ship, and NOTHING about them is decided by a name.** The plan in force is service `Wards22` (AGO item titled "Wards23"); `Wards2022` is the 2012 plan and `Wards_Outline`, from the city's own account, is also 2012. All three carry nine features called Ward 1..Ward 9 and the currency gate separates them by twenty-three points. | `python3 mi/scripts/build_mi_flint_wards.py` | Re-run it. It re-reads the item's terms first and refuses if a licence has appeared, reads the layer index from the FeatureServer root, and gates currency at 99% (shipped layer 99.525%, both old plans 76.9%). If a future service name looks newer, MEASURE it — do not switch on the name. Last done 2026-09-06. |

---

## Open questions this instance owes an answer to

| Question | Why it is open | What would close it |
|---|---|---|
| **Who are Michigan's 619 county commissioners?** | The `county-commissioner` layer names the district and not the person, because the only statewide name source — the Commissioner/Party columns on the boundary layer itself — is a list of the certified **November 2024 election winners**, not a maintained roster. Measured 2026-09-03: 93.5% right, every miss the same direction, and Wayne District 5 still names a commissioner who died on 10 June 2025. Recorded as gap `mi-commissioner-roster` | A county-by-county roster build against each county's own board page, weekly and count-guarded — the route every other instance uses. Ten of the twelve counties sampled publish a readable one. Oakland answers only through its CMS origin (`www.oakgov.com` is an Akamai 403 to this client); Ottawa is behind an sgcaptcha gate, which is an access control this project does not route around |
| **Four western-UP counties still hand off to Wisconsin.** | Michigan's TIGERweb county fabric is WATER-INCLUSIVE, so the state's own bbox runs west across Lake Michigan and contains both Chicago's centre (41.8781, -87.6298) and Wisconsin's (44.9, -89.565) — `validate_index.py` hard-fails `il` and `wi` on that, since a metro must never claim a sibling's centre. The shipped fleet bbox is therefore the county fabric clipped to `lng >= -87.60`. Front-door routing was moved off the bbox tie-break on 2026-09-04 and now settles a CONTESTED point against each instance's own published coverage ring, which took misroutes from 7 of 28 real places to 4 — Marquette and Escanaba MI, Sister Bay WI, Dubuque IA and Rock Island IL all resolve correctly now. **The remaining four (Ironwood, Houghton, Iron Mountain, Menominee) are not a tie-break failure**: they sit WEST of the clipped bbox, and the bbox is still the cheap first pass, so Michigan is never offered them and no ring is ever consulted. Michigan's app still ANSWERS there — `metro_bbox` is untouched and full-state. **CORRECTION, measured 2026-09-04:** an earlier version of this row named SMALLEST BBOX AREA as the fix, at "5 misroutes of 26". That is wrong. Measured over 37 places, smallest-area is 7 wrong — the same count as nearest-centre, just wrong in different places (it fixes Sister Bay and Dubuque, and breaks Rock Island and Escanaba). No rectangle rule separates states that interlock across a lake | Restore Michigan's honest full-state fleet bbox, which needs two things this project should decide together: `validate_index.py`'s "a bbox must not contain a sibling metro's centre" rule relaxed — it guards the nearest-centre tie-break, which the front door no longer uses — and the IN-APP `metro-portal` engine block moved onto the same ring test. That block's `siblingMetroAt` is called from `moveend`, so making it fetch requires care: keep the bbox pre-filter, resolve the ring only for a contested point, and never let a fetch block a pan. Ring-based resolution answers all 37 probes correctly, so the ceiling is 0 |
| **Is `house.mi.gov` actually unreachable, or is this sandbox?** | The Michigan House site fails TLS from the build environment with "unable to get local issuer certificate" even with the egress proxy's CA bundle supplied, while `senate.michigan.gov` answers 200 on identical flags and the proxy logs no relay failure. That is the incomplete-chain shape this repo documents for Coles, Gallatin and Vermilion — **but TLS is re-terminated at the proxy here, so this environment cannot see the site's real chain at all.** A measurement is not a conclusion (`docs/EXPANSION_GUIDE.md` §0.4). | ONE CI-side probe, from a GitHub Actions runner rather than this sandbox. If the site answers there, the House card gains the same capitol contact block the Senate card already has, from the House's own directory. If it fails there too with a leaf-only chain, `scripts/probe_incomplete_tls_chains.py` reports the intermediate's SHA-256 and a scraper PINS it — never by disabling verification. |
| ~~When does the Bureau of Elections republish the commissioner-district layer?~~ **ANSWERED 2026-09-03** | It has not, and there is no successor: all 294 `michigan_admin` AGO items were enumerated and exactly one is a commissioner-district item, while the state's own current Election District Viewer (touched 2026-07-31) still wires this exact endpoint. Michigan counties reapportion on the decennial census (MCL 46.404), so the next plan is ~2031. | Closed. The republish WATCH now has a concrete mechanism — the standing monthly row above, against the AGO item's `modified` epoch, because the service itself carries no date field of any kind. |

---

## Per-election — the seats above the boundaries

| When | What | Last done |
|---|---|---|
| After each Michigan general (November, even years) and any special election | The chamber rosters turn over; the weekly Open States refresh picks it up, but verify the first post-election PR against each chamber's own directory before merging | 2026-09-03 (initial build: 38/38 Senate seats, 110/110 House seats — no vacancies at arrival) |
| After each U.S. general (November, even years) | The delegation turns over; the weekly congress-legislators refresh picks it up | 2026-09-03 (initial build: 13/13) |

---

## Per-decade — the census redistricting cycle

| When | What | Last done |
|---|---|---|
| Before each Michigan general (November, even years), and whenever the precinct card looks stale | **A new precinct cycle appears as a NEW SERVICE ON A DIFFERENT HOST, and nothing on the shipped one announces it.** The 2026 map is at `services3.arcgis.com/dxRQUfTDNtfqZ301/.../2026_Voting_Precincts` (AGO item `c349bc2bb6c2468594d341a09f520ea8`), while the `OpenData/boundaries` MapServer that carries the commissioner layer still stops at 2024 — so THE MAPSERVER IS THE WRONG PLACE TO CHECK. Run `arcgis.com/sharing/rest/search?q=owner:michigan_admin AND precinct` and look for a cycle newer than 2026; that one query is the whole check. Michigan went 4,340 -> 3,895 precincts between 2024 and 2026, so a new cycle is a real remap, not a refresh | 2026-09-04 (built against the 2026 cycle; 2014-2026 enumerated, none newer) |
| Whenever TIGERweb rolls a vintage | **The five live TIGERweb layers rebuild themselves — what a roll can break is silent.** (0) `zip-code` is fetched by an Esri `{xmin,ymin,xmax,ymax}` ENVELOPE rather than `STATE='26'`, because ZCTAs carry no STATE field; passing the instance's `{minLng,...}` `METRO_BBOX` instead yields HTTP 200 with a JSON error envelope and zero features rather than an error, which is how `ia/` shipped a dead ZIP overlay for weeks with a working card. `validate_sources.py` watches that count. (1) The Township or City and City or Village cards derive their type row from TIGER's own NAME-minus-BASENAME suffix; an unseen suffix renders NO type row rather than a wrong one, which is safe and invisible, so re-measure the LSADC/suffix distribution (measured 2026-09-04: 44 township 1,122 · 49 charter township 118 · 25 city 301 · 00 blank 40; places 25 city 281 · 47 village 252). (2) The two school layers tile Michigan only because School layer 1 (secondary) measures ZERO and the 27 elementary districts fill exactly the gaps the 514 unified ones leave — re-run the disjointness probe at each layer's `INTPT*` interior points (never `CENT*`, which reports a false overlap), and if layer 1 stops measuring zero, the recorded drop becomes a layer to ship | 2026-09-04 (measured at the phase-3 build) |
| After each decennial census (P.L. 94-171 release, spring of the year after) | Every boundary this instance ships is redrawn. Michigan's congressional and legislative lines are drawn by the **Independent Citizens Redistricting Commission** (2018 Prop 2), not the legislature; county commissioner districts are redrawn by each county's own apportionment commission under MCL 46.404/46.405. Rebuild all three chamber files and the county file, then re-anchor the smoke test | 2026-09-03 (built against the 2020-cycle maps) |
| After each decennial census, ~2031 | **County commissioner districts are reapportioned** by each county's own apportionment commission under MCL 46.404, and the Bureau of Elections recompiles the statewide layer. The 2021 plans shipped here run the 2020-census cycle | 2026-09-03 (built against the 2021 plans, 619 districts / 83 counties) |
| After each Michigan general seating commissioners (November 2028, then every 4 years) | Terms went to FOUR years under PA 121-122 of 2021, so the boards seated January 2025 run to December 2028 — there is NO 2026 commissioner election, and a re-verify scheduled for then would be wasted. The live risk between generals is mid-term vacancies | 2026-09-03 (no roster shipped; see the open question above) |
| Whenever TIGERweb rolls a vintage | **The congressional layer's district field is versioned and the old one is REMOVED, not merely stale.** It rolled to `CD120` for the 120th Congress; a query naming the retired `CD119` returns HTTP 400, "Failed to execute query" — measured 2026-09-03. `mi/scripts/build_legislative_boundaries.py` names `CD120`; the app's `CONGRESS_DISTRICT_FIELDS` lists it first with `cd119` behind it. On the next roll, both need the new name | 2026-09-03 (CD120) |
