# WATCH.md — redistricting watch calendar

The one place the dates live: *when to look* for boundary and roster changes in this
instance's sources. The repo's `docs/REDISTRICTING_RUNBOOK.md` is *what to do* when a
boundary changes. Update the "Last done" column each time you complete a row — a
checkpoint with a stale date is a checkpoint that didn't happen.

---

## Standing (automated — verify, don't perform)

| Cadence | What | Where | You do |
|---|---|---|---|
| Weekly (Mon 15:30 UTC) | U.S. House (MI) roster refresh | `.github/workflows/update-mi-congress-roster.yml` → PR on change | Review + merge the PR; a week with a surprise diff is worth a look at the source |
| Weekly (Tue 15:30 UTC) | Michigan Senate + House roster refresh (Open States mi.csv + the Senate's own all-senators directory) | `.github/workflows/update-mi-legislature-roster.yml` → PR on change | Review + merge the PR — especially after a Michigan general (November, even years) and each January seating |
| Monthly (1st, 16:00 UTC) | Source freshness for every dataset this instance depends on | `.github/workflows/mi-validate-sources.yml` → tracking issue on WARN/FAIL | Read the issue; the job stays green on purpose |
| Monthly, with the run above | **The commissioner layer's own republish signal, which its endpoint cannot give.** The service carries no `editingInfo`, no `lastEditDate` and no date field, so a republish is invisible there | `arcgis.com/sharing/rest/content/items/4c8d0d854ac04d8787cb3cf6dab7fbec?f=json` — compare `modified` against **1764783222000** (2025-12-03) and the title's own `vNN` suffix against **v25** | If either moved, re-run `mi/scripts/build_mi_commissioner_districts.py` and diff: the count guard (619/83) fails loudly on an apportionment, and a quiet change is a correction |
| Monthly, with the run above | **The certified-returns roster, which is the same service one column over.** `data/app/mi-commissioner-returns.json` carries commissioner NAMES for the 35 counties no tranche reaches, from the `Commissioner`/`Party` columns `build_mi_commissioner_districts.py` strips off the boundary file. Adam opened that route on 2026-09-22 on one condition -- the row must always say the names are the winners the state certified on 5 November 2024 -- and every record carries its own provenance sentence for that reason. THE COHORT SHRINKS ON ITS OWN: a county that gains a scraped roster leaves this file, and `--check` FAILS if the two ever overlap or if the cohort stops matching the counties the gap records name. | `python3 mi/scripts/build_mi_returns_roster.py` then `--check` | Re-run it when the republish signal above moves, and read the run's own report: it prints the fill rate, the surnames one edit apart (three candidates today, one of them Wexford District 2's `Jason L. Nelso`, none of them corrected) and how far the column has drifted from the counties' own pages. Last done 2026-09-22. |
| Monthly, with the run above | **Detroit's four council-district services, and which of them is in force.** The city publishes `Council_Districts` (titled "Current", live since 2016), `city_council_districts_2026` (what ships), `city_council_districts_2013` (created December 2025 — an archive) and `NewDistrictBoundariesOption6` (reads like a rejected draft; measured 2026-09-05 it is the SAME plan redigitised, 99.575% point agreement, and it is the witness naming the adoption date). Their NAMES do not settle it; the builder does, and gates it both ways: 'Current' must match what ships, the 2013 archive must differ, the shipped plan must balance on Census 2020 (4.28% measured) and the archive must not (13.51%). The plan's own dates are in the shipped service's `description`: adopted 2024-02-06, effective 2026-01-01. | `python3 mi/scripts/build_mi_detroit_council.py` | Re-run the builder. If it refuses because the archive has come into balance too, the test has stopped discriminating and the answer is a human reading the city's own adoption record — never a loosened threshold. Last done 2026-09-05. |
| Monthly, with the run above | **Grand Rapids's ward geometry is a 2018 file, and its currency is a MEASUREMENT rather than a claim.** `CGR_Wards` was last edited 2018-01-24 — before the census it balances on — while its item description says it is "maintained to reflect the most current adopted ward configuration". The gate is Michigan's OWN 2026 precinct layer, which carries a WARD column assigning the city's 59 precincts 20/20/19; dissolved by it, it agrees with the city's polygons on 99.575% of 4,000 points. | `python3 mi/scripts/build_mi_grand_rapids_wards.py` | Re-run the builder. It refuses if that agreement drops below 99% or the per-ward precinct counts move — which is what a ward redraw looks like from here. On refusal, find the city's adopted plan before touching the threshold; the threshold is not the thing to move. It also refuses if the city's duplicate ward service diverges from the one that ships — ONE of SEVEN siblings in the org (eight ward services in total, measured 2026-09-05 by sweeping all 681 feature services); the other six are deliberately ungated because three are single-ward layers and two already differ at the city edge. Last done 2026-09-05. |
| Weekly, by CI | **Grand Rapids's City Commission — six of seven seats.** The city elects two commissioners per ward plus a mayor and publishes one Ward 1 commissioner. THAT SEAT IS VACANT — the city's news post of 2026-04-17 names the resignation that created it — and no city source states how it was resolved, so the card names the vacancy and stops. WHEN A SUCCESSOR APPEARS the roster count goes 6 -> 7 on its own and the card's shortfall row disappears with it; nothing needs editing in the card — its shortfall row and the cause it states both render from the roster — but `VACANCY_SOURCES` in the scraper, the two script docstrings, the worksheet `people` note and the expansion plan all describe a resolved situation at that point and should be retired. `update-mi-grand-rapids-council-roster.yml` refreshes it; the roster carries `seatsPerWard` and a per-ward `vacancies` entry, so the card states the shortfall and its cause from the DATA rather than from a literal. UNLIKE DETROIT THIS SOURCE IS DIRECTLY READABLE, so a refusal here is a real change, not a known block. | `python3 mi/scripts/mi_grand_rapids_council_scraper.py && python3 mi/scripts/build_mi_grand_rapids_council.py` | If the run fails: check whether the city has started challenging this client (the scraper says so explicitly) — and if it has, the Detroit scraper next door is the Archive-rung pattern to copy. If Ward 1's second seat appears, nothing needs changing: the card counts what ships. Last done 2026-09-05. |
| Monthly, with the run above | **Michigan's precinct layer has a sibling that dates itself to 2002.** The org's `Proposed_Precincts` self-describes as adopted 2002 and updated 2025; the shipped `2026_Voting_Precincts` is the one this app uses and the one Grand Rapids's ward-currency gate reads. Recorded so a future sweep does not mistake the older service for a newer plan, which is the trap that made Detroit's four services worth measuring. | `python3 mi/scripts/build_mi_precincts.py --check` | If a future cycle's precinct service appears, WATCH THE ORG rather than the URL — a new cycle arrives as a NEW SERVICE. Last done 2026-09-05. |
| Every PR, by CI | **`registerCountyLayer` is one copy now** — `engine/index.html/county-layer-dispatcher.txt`, spliced into il, ia and mi (#903). ca, ny and wi register no county-dispatched concept, carry no fence for it, and are untouched; `--fleet` states that three-of-six shape rather than leaving a reader to infer it from a fence count. | `python3 scripts/compose_app.py --check`, `python3 scripts/check_engine_parity.py --fleet` | Nothing. Edit the block under `engine/` and recompose; never a fence inside an instance file. |
| Weekly, by CI | **Detroit's nine council members, read directly when the city answers and through the Internet Archive when it does not.** CORRECTED 2026-09-19: this row said detroitmi.gov "refuses this client (Cloudflare managed challenge, both rungs)" as a permanent property, and the roster's own committed history disproves it. Across the three commits that have ever written the file, `archivedAt` reads 20260831152830 (2026-09-05, archive), **null (2026-09-09 — the DIRECT rung served)** and 20260912152333 (2026-09-16, archive). The challenge is INTERMITTENT, `update-mi-detroit-council-roster.yml` tries the direct rung first every run, and the archive is the fallback. Nobody noticed the 2026-09-09 success for ten days because that workflow's PR body was one static paragraph; it now states which rung answered, in the PR title. THE FAILURE MODE THIS ROW EXISTS FOR IS A SILENT ONE: if the Archive stops capturing that page, the newest snapshot ages past the scraper's 45-day ceiling and the run FAILS rather than shipping stale officeholders — which is correct, and is a human's cue to check whether the city has started answering directly again. THAT CUE IS NO LONGER THE ONLY ONE and was never the first to fire: the weekly PR now says which rung answered, and a run where the direct rung serves says so in its title. `EXPECTED_UNREACHABLE` in `scripts/validate_card_links.py` carries the same inversion on a MONTHLY cadence and did not surface 2026-09-09 either. | `python3 mi/scripts/mi_detroit_council_scraper.py && python3 mi/scripts/build_mi_detroit_council_roster.py` | If the ceiling refuses: check whether detroitmi.gov now answers (the scraper's first rung tries it every run, so a passing direct rung retires the archive hop and its `EXPECTED_UNREACHABLE` entry in `scripts/validate_card_links.py`). If it does not, request a fresh capture of the page rather than raising the ceiling. Last done 2026-09-05. |
| Monthly, with the run above | **Lansing's ward geometry is BUILT AND UNSHIPPED, on a licence question.** The city publishes the current plan three times; the only clean four-feature copy (`VotingWards`) states no licence, while the same plan one item away states CC BY-NC 4.0. Silence from one publisher is not a grant, so the operative licence is NC and the call is the operator's. Currency is NOT the issue: against the state's 2026 precinct WARD column (27 precincts, 7/7/6/7) the wards agree on 99.893% of sampled points. | `python3 mi/scripts/build_mi_lansing_wards.py` | Re-run it. It re-reads the city's stated licence EVERY RUN and fails if the clause has moved in either direction — a relicence to open terms is exactly the news that would unblock this. It measures and refuses to write without `--licence-approved`; the write path is deliberately unimplemented so the attribution the licence requires gets written in the same change as the geometry. Last done 2026-09-05. |
| Monthly, with the run above | **Ann Arbor has two unexhausted routes and neither was reachable from here.** The readable ward layer is superseded (47 features against the state's 41), the same org's `City Council Districts` is Los Angeles, and that org is not the city's publisher. The city's own portal answers but is a JavaScript application, and `gis.a2gov.org` fails at this environment's egress proxy, which says nothing about the host. | read `data.a2gov.org` in an ordinary browser on an ordinary network; probe `gis.a2gov.org` from a network whose proxy can tunnel it | BOTH WERE TRIED ON 2026-09-06 AND BOTH FAILED ON THIS ENVIRONMENT, not on the city: headless Chromium cannot reach the portal at all (it does not use the agent proxy), and the GIS host still 502s at that proxy. Every documented catalog API for the portal's platform 404s too, so the browser is genuinely the missing piece. Still not a measured shut — if either route yields a current five-ward boundary, Ann Arbor becomes a `city-ward` dispatch entry with no new layer. Last done 2026-09-06. |
| Per election cycle | **Sterling Heights' council form is unsettled and every route tried is named.** Four signals point at at-large — the state's own precinct fabric assigns it no ward numbers where the other three cities carry them — but an absence in one publisher's column is a fact about that publisher. | see the gap record `sterling-heights-council-form` | The charter is the first witness (its own site's charter link serves a water-billing portal, and Municode needs a browser); a certified Macomb canvass is the second, and Clarity carries the county slot but publishes an EMPTY election list. If the council is at large there is no gap to close — only a record to correct, because an at-large body never gets a polygon. Last done 2026-09-05. |
| Monthly, with the run above | **Warren's five wards ship, and the builder refuses if the city ever states terms.** The layer sits at SERVICE INDEX 32, not 0 — a hardcoded `/0` returns HTTP 200 with no `features` key and dies with a KeyError that reads like an outage. Currency is measured against the state's 2026 precinct WARD column (53 precincts, 10/10/11/11/11) at 99.550%, and the five wards tile the city exactly: 139,387 against the Census place's 139,387, zero blocks on the wrong side. | `python3 mi/scripts/build_mi_warren_wards.py` | Re-run it. It re-reads the item's terms FIRST and fails if the city has added a licence, so geometry is never republished under terms nobody has read; it also asserts the service's single layer is still named 'Council Wards 2023'. On a currency failure find the city's adopted plan — the threshold is not the thing to move. Last done 2026-09-06. |
| Per election cycle | **Dearborn is the second city measured as probably at-large**, after Sterling Heights, and by the same test: the state records no ward number for any of its 42 precincts where every other covered city carries them, and the catalogue holds no ward service. | see the gap record `dearborn-council-form` | The charter or a certified Wayne County canvass settles it. If the council is at large there is no gap to close, only a record to correct — an at-large body never gets a polygon. Last done 2026-09-06. |
| Monthly, with the run above | **Flint's nine wards ship, and NOTHING about them is decided by a name.** The plan in force is service `Wards22` (AGO item titled "Wards23"); `Wards2022` is the 2012 plan and `Wards_Outline`, from the city's own account, is also 2012. All three carry nine features called Ward 1..Ward 9 and the currency gate separates them by twenty-three points. | `python3 mi/scripts/build_mi_flint_wards.py` | Re-run it. It re-reads the item's terms first and refuses if a licence has appeared, reads the layer index from the FeatureServer root, and gates currency at 99% (shipped layer 99.525%, both old plans 76.9%). If a future service name looks newer, MEASURE it — do not switch on the name. Last done 2026-09-06. |
| Weekly, by CI | **Kalamazoo, Kent and Berrien went behind a Cloudflare managed challenge on 2026-09-19** — `Cf-Mitigated: challenge`, a 5.7 KB "Just a moment..." body, measured on all three the same evening from three unrelated domains, with the egress proxy's own CONNECT returning 200 so it is the sites and not this sandbox. That is an access control and is never worked around. Their 42 seats (Kent 21, Berrien 12, Kalamazoo 9) are CARRIED FORWARD on their last good read rather than deleted, per Adam's 2026-09-19 ruling, and `build_mi_commissioner_roster.py` prints each one every run. **The 45-day ceiling turns that into a failure around 2026-11-03** if the challenge has not lifted. | `python3 mi/scripts/mi_commissioner_scraper.py --county Kent` | If it still challenges: the counties are not readable and the ceiling is the cue to decide between asking the county and recording a gap — never to raise the ceiling or send a browser string at a challenge. If it answers, the next weekly run clears `preservedSince` on its own and nothing needs editing. Last done 2026-09-19. |
| Before any new Michigan city | **Run the state's WARD column FIRST — it settles a whole batch in one query.** 23 cities were settled this way on 2026-09-06: ten districted (Wyoming, Rochester Hills, Pontiac, Kentwood, Battle Creek, Midland, Muskegon, Jackson, Bay City, Holland), thirteen at-large-shaped. `WARD='00'` on every precinct means stop — an at-large council has no district to draw. | one query on the state's 2026 precinct layer filtered to the cities of interest | **WYOMING IS BLOCKED BY ITS OWN robots.txt, which names ClaudeBot and disallows the whole site**; its precinct maps are under `/Portals/`, disallowed to `*` too. Do not fetch it with a browser user-agent. The state's fabric would dissolve to 6/6/6 across three wards without touching the city, but that would make the state the SOURCE rather than the currency CHECK — and **on 2026-09-21 the operator ruled against it**: the state's fabric may only ever CHECK a city-published ward boundary, never stand in for one, because a state precinct tiling carries no statement by the city about its own boundary and aggregates to a ward only where the city's lines happen to nest. So a districted city whose own publisher has nothing stays a recorded gap, and Wyoming is now a measured shut rather than a pending decision. ONE QUESTION SURVIVES THAT RULING AND IS A DIFFERENT ONE: Jackson's proposed source is JACKSON COUNTY's precinct layer, a separate publisher, which leaves the state's fabric as a genuinely independent check — see gap `jackson-mi-ward-boundary`. Last done 2026-09-21. |
| Monthly, with the run above | **Rochester Hills ships GEOMETRY ONLY, and the split is between two HOSTS.** `gis.rochesterhills.org` (no robots.txt, AGO item shared public, no stated terms) publishes the four council districts and they ship; `www.rochesterhills.org` allows five named bots and then disallows `*` site-wide, so its maintained council page is never read and no name ships. Currency 99.725% against the state's 2026 precinct WARD column (21 precincts, 6/5/5/5). | `python3 mi/scripts/build_mi_rochester_hills_wards.py` | Re-run it; it re-reads the AGO item's terms first and refuses if any appear. **THE POPULATION CEILING IS 0.22% HERE, NOT THE FLEET'S 0.2%**, raised for this city alone on a measurement — the whole -162 is ONE block whose Census interior point sits 26 m inside the city line — with a new 100 m edge test beside it that strictly tightens the build, so the raise cannot hide a hole. If that edge test ever fires, the answer is not to widen it. **If the city's robots.txt ever opens, or the city publishes a roster endpoint, gap `rochester-hills-council-roster` becomes closeable — and you will be TOLD: the host sits in `ROBOTS_DECLINED` in `scripts/validate_card_links.py`, whose monthly run never fetches it but DOES re-read its robots.txt and WARNs the day the `Disallow: /` goes.** The card links the bare origin only, because every deeper path this project knows for that host came from a fetch it should not have made. Last done 2026-09-06. |
| Before any new Michigan city | **READ robots.txt THROUGH ITS REDIRECT.** `www.rochesterhills.org/robots.txt` answers with an "Object Moved" stub pointing at the city's CMS host; a sweep that scanned the STUB found no AI-agent lines and recorded the site as open, and two pages were fetched before the real policy (`User-agent: *` / `Disallow: /`) was read. A REDIRECT BODY IS NOT A POLICY — the same shape as the Battle Creek 403 that came from a guessed URL. Of the seven Michigan city sites checked this way, only Rochester Hills bars general crawlers; the rest disallow admin and search paths only. | `curl -sSL <site>/robots.txt` — note the `-L` | Follow redirects, and read the file rather than grepping it for agent names. A host with NO robots.txt (404) is permitted; a separate GIS host is governed separately (the Knox precedent). Last done 2026-09-06. |
| Monthly, with the run above | **Battle Creek's five wards ship and its commissioner NAMES come from the city's page, not from the ward layer.** The layer carries a COMMISSIONER per ward and four of five records were last edited in March 2023 — a name field is not a roster, and Michigan cities elect in odd Novembers — so the build strips the column and the shipped geometry carries the ward number and nothing else. Currency 99.450% against the state's 2026 precinct WARD column (13 precincts, 2/2/3/3/3). | `python3 mi/scripts/build_mi_battle_creek_wards.py` | Re-run it; it re-reads the item's terms first and refuses if a licence appears. **CORRECTED 2026-09-06: an earlier version of this row said battlecreekmi.gov/165/City-Commission answers 403 and that reading it was what would close gap `battle-creek-commission-roster`.** That page id had been GUESSED. The real page is /380/City-Commission, it answers 200, the roster now ships from it and the gap is retired. A 403 from a path you invented is a fact about your guess. Last done 2026-09-06. |
| Weekly, by CI | **Battle Creek's City Commission — all nine seats.** Five ward commissioners, three at large and the Mayor, the split the city states in its own prose on the page the roster is read from; the scraper checks that prose against the cards it publishes and refuses to write unless they agree, so a charter change fails the run rather than reshaping the card. `update-mi-battle-creek-commission-roster.yml` refreshes it. THE PAGE RENDERS ITS WARDS 5, 2, 1, 3, 4, so the parser keys off each member's own h-card block and never document order; the smoke test selects points in two wards and asserts neither names the other's commissioner. No per-member e-mail exists — one shared contact form serves all nine and is hoisted to the body. | `python3 mi/scripts/mi_battle_creek_commission_scraper.py && python3 mi/scripts/build_mi_battle_creek_commission.py` | If the run fails: check whether the page has been rebuilt out of h-card markup (the scraper says so explicitly) or whether the city has restated its composition. Neither is a constant to edit past — the first needs a new parser, the second a human. Last done 2026-09-06. |
| Per election cycle | **Bay City and Pontiac were measured and REFUSED, and the floor was not moved.** Bay City's own 9-ward layer scores 97.608% against the state's 2026 fabric with the disagreement spread across twelve ward pairs — two plans, not one bad edge. Pontiac publishes a 2020 PRECINCT layer (21 features, 45.969%), not districts. | see gaps `bay-city-ward-boundary` and `pontiac-ward-boundary` | Re-measure when either city republishes. Bay City needs only to bring its ward map into agreement with the precincts the state records; Pontiac needs a district polygon to exist at all. Last done 2026-09-06. |

---

## Open questions this instance owes an answer to

| Question | Why it is open | What would close it |
|---|---|---|
| **Who are the other 253 Michigan county commissioners?** | Six shipping tranches: tranche 1 on 2026-09-13 (Kalamazoo 9, Kent 21, Macomb 13, Muskegon 7, Saginaw 11, Wayne 15), tranche 2 on 2026-09-15 (St. Clair 7, Monroe 8 of 9, Berrien 12, Jackson 9, Calhoun 7), tranche 3 the same day (Eaton 15, Grand Traverse 9, Lapeer 7, Lenawee 8 of 9, Midland 7) tranche 5 on 2026-09-19 (Barry 8, Cheboygan 7, Dickinson 5, Hillsdale 5, Ionia 6 of 7, Kalkaska 7, Leelanau 7, Oceana 5, Osceola 7, Sanilac 7) and tranche 6 the same day (Alcona 5, Alpena 8, Arenac 5, Chippewa 5, Clare 9, Clinton 6 of 7, Delta 5, Emmet 7, Houghton 5, Isabella 7, Lake 7, Luce 5, Mackinac 5, Menominee 9, Montcalm 7, Ontonagon 5, Oscoda 5, Otsego 9, Presque Isle 5, Roscommon 5, Schoolcraft 5), every one of tranche 6 read from a page saved during tranche 5's sweep with NO HOST FETCHED to write a parser, and tranche 7 the same day (Cass 8), the one candidate that needed a fetch because the probe had scored its board's COMMITTEES page rather than its roster. TRANCHE 4 WAS THE PROBE and shipped no county: population had stopped discriminating, so the order became whether a county publishes a district-keyed board page at all, and `mi/scripts/probe_mi_county_boards.py` measured that for all 59 untried counties at once. `mi-commissioner-members.json` now names 366 of the 619 districts from forty-eight counties' own board pages — 6,184,606 of Michigan's 10,077,331 people, 61.4% — refreshed weekly by `update-mi-commissioner-roster.yml`, whose first run ever was a manual dispatch on 2026-09-19 (run 35415178348, success, no change: the sixteen counties shipped before this tranche still name the same people). The other 35 counties still name the district and not the person, because Michigan publishes no maintained statewide roster: the Commissioner/Party columns on the boundary layer are the certified **November 2024 election winners**, and re-measured against all 366 seats on 2026-09-19 they name a different person in 18 and write 114 more differently — 65 dropping a middle initial, 40 a different form of a first name or a suffix or an accent, 9 a plain typo in the state column. **FOUR COUNTIES ARE SHORT AND FOR THREE DIFFERENT REASONS, each stated in its own words on its own card.** Ionia's and Clinton's board pages call District 3 and District 4 vacant — Clinton carries its own Notice of Vacancy for that seat at the top of the same page — which is neither of the other two cases and gets its own sentence. Monroe's directory lists eleven rows for a nine-seat board and one is malformed — labelled District 2, naming "Commissioner Vensel", who holds District 6 — so that row names no District 2 commissioner. **LENAWEE DISTRICT 5 IS THE FLEET'S FIRST SEAT WITHHELD BECAUSE A PUBLISHER CONTRADICTS ITSELF:** its directory lists Jim Daly there while the county's own News Flash of 10 September 2026 announces his death and says he represented District 5. Shipping the name would repeat the error this project criticises the state column for; calling it vacant would assert an appointment nothing published shows. `CONTRADICTED` in `build_mi_commissioner_roster.py` withholds it with the reason and the source on the card, and RETIRES ITSELF — it binds only while the roster still carries the recorded name, so a county that updates its page ships the new name with a printed line saying to delete the entry, and an entry naming a county that has left the roster fails the build. **THE ONE THING NO WEEKLY RUN CHECKS is whether the county has since filled the seat by appointment without saying so on either page.** Gap `mi-commissioner-roster` | More tranches, by the same route. **Ten counties have been tried and refused, each measured rather than guessed:** Genesee (a single `*` group `Disallow: /` on geneseecountymi.gov line 18 AND on gc4me.com line 22, which allows only document extensions and so cannot be navigated to one), Ingham (the same, line 18), Ottawa and Livingston (robots.txt ITSELF answering HTTP 202 in 169 bytes — the sgcaptcha shape, an access control this project does not route around), Oakland and Allegan (HTTP 403 from each site's own Akamai edge to the districtry token AND to Chrome 126 with client hints, so the stack is refused rather than the token and a browser string buys nothing; Oakland's earlier "answers only through its CMS origin" note is not a route anybody here has made work), Washtenaw (200, and still not keyable: its commissioners are named only inside free-prose biographies in seven sentence shapes, one of which does not begin with the name), and BAY (200 and allowed, and still not keyable: its board's own Members page names nobody, its board index names only three OFFICERS with no district for any of them on a seven-seat board, its districts page is seven map links, its 1.4 MB meeting-results page names none of the three and carries no roll call, `co.bay.mi.us` resolves to a different server whose robots.txt is UNREACHABLE and so was never fetched, and its `boc.`/`commissioners.` subdomains are a DNS WILDCARD — a name that cannot exist resolves identically — whose own robots.txt disallows; three names with no district key cannot go on a district card). GOGEBIC and MARQUETTE joined them on 2026-09-19, each serving a 210-byte robots.txt that allows five named crawlers and then disallows `*` on line 18, byte-identical to each other and to Genesee's and Ingham's, read three times in a row — and both had been recorded as candidates by the probe the day before, so either the files changed inside a day or that read differed, which is not established and cannot be, because the probe records a robots status only for the hosts it rejects. The table of record is `PROBES` in `mi/scripts/mi_commissioner_scraper.py`; the refused counties stay in it so the weekly run re-asks and a county that changes its file re-enters by itself. CASS CLOSED THE CANDIDATE LIST on 2026-09-19, and the reason it was last is worth keeping: the probe had scored `casscountymi.org/1500/BOC-Committees`, which is the board's COMMITTEES page, so the probe's own verdict pointed at a page that names all eight districts and is not a roster — a parser built on it would have lost any commissioner who sits on no committee. The county's board page is a different URL, linked from that one, and names all eight; the committees page is kept for the seven addresses it carries and for nothing else. ZERO candidates remain. **THE 25 SHUT COUNTIES WERE RE-EXAMINED ON 2026-09-24 AND FIVE OF THEM PUBLISH A BOARD PAGE.** The budget was one request per county to an address the record does not name, nothing at all to the two challenge hosts, and the addresses came from each county's own English Wikipedia infobox rather than from permutation — en.wikipedia.org is not a county host, so finding them cost the counties nothing and could not trip the WAF a repeated sweep tripped on Tuscola. Ten addresses no sweep had asked for; nine answered. **Shiawassee** (shiawassee.net) and **Montmorency** (montcounty.org) were recorded `no-confirmed-host` and each has a host and a Board of Commissioners page; **Ogemaw** (ocmi.us), **Keweenaw** (keweenawcountyonline.org) and **Gratiot** (gratiotmi.com) were `no-board-page` and each links one. **Iron** (ironmi.com) gains a live county host and still links no board page. **Gladwin** (gladwincounty-mi.gov) and **Iosco** (iosco.net) answer HTTP 202 on robots.txt at the new address too, so both move from "no board page" to shut by an access control, which is the more accurate verdict. **Baraga's infobox is wrong** — keweenawbay.org titles itself "Keweenaw Bay - The Heart of Baraga County" and is a tourism site, so that address is recorded rejected rather than tried again. **Benzie and Mason did not move**: their `no-districts` verdict was reached by reading a board page, and benzieco.net and masoncounty.net serve the same CMS path as the hosts already recorded, so a second domain says nothing about a page's content. **NO BOARD PAGE HAS BEEN READ at any of the nine** — that is a second request per county and a separate budget, and it is what stands between these five counties and a roster. The root cause is recorded in code rather than here: six of the ten addresses are FORMS `_forms()` could not produce, so `no-board-page` was a fact about the candidate list exactly as that file's own docstring warns; the six are added, the nine hosts are in `KNOWN_HOSTS`/`CHALLENGE_HOSTS` so the generator gate keeps them, and the nine artifact rows carry a dated `superseded` block that `--check` fails on if it is dropped, if it goes stale, or if a sweep catches up with it. The remaining probe gap is closed: it recorded a robots verdict only for the hosts it REJECTS and never for the one it ACCEPTS |
| **Four western-UP counties still hand off to Wisconsin.** | Michigan's TIGERweb county fabric is WATER-INCLUSIVE, so the state's own bbox runs west across Lake Michigan and contains both Chicago's centre (41.8781, -87.6298) and Wisconsin's (44.9, -89.565) — `validate_index.py` hard-fails `il` and `wi` on that, since a metro must never claim a sibling's centre. The shipped fleet bbox is therefore the county fabric clipped to `lng >= -87.60`. Front-door routing was moved off the bbox tie-break on 2026-09-04 and now settles a CONTESTED point against each instance's own published coverage ring, which took misroutes from 7 of 28 real places to 4 — Marquette and Escanaba MI, Sister Bay WI, Dubuque IA and Rock Island IL all resolve correctly now. **The remaining four (Ironwood, Houghton, Iron Mountain, Menominee) are not a tie-break failure**: they sit WEST of the clipped bbox, and the bbox is still the cheap first pass, so Michigan is never offered them and no ring is ever consulted. Michigan's app still ANSWERS there — `metro_bbox` is untouched and full-state. **CORRECTION, measured 2026-09-04:** an earlier version of this row named SMALLEST BBOX AREA as the fix, at "5 misroutes of 26". That is wrong. Measured over 37 places, smallest-area is 7 wrong — the same count as nearest-centre, just wrong in different places (it fixes Sister Bay and Dubuque, and breaks Rock Island and Escanaba). No rectangle rule separates states that interlock across a lake | Restore Michigan's honest full-state fleet bbox, which needs two things this project should decide together: `validate_index.py`'s "a bbox must not contain a sibling metro's centre" rule relaxed — it guards the nearest-centre tie-break, which the front door no longer uses — and the IN-APP `metro-portal` engine block moved onto the same ring test. That block's `siblingMetroAt` is called from `moveend`, so making it fetch requires care: keep the bbox pre-filter, resolve the ring only for a contested point, and never let a fetch block a pan. Ring-based resolution answers all 37 probes correctly, so the ceiling is 0 |
| **Is `house.mi.gov` actually unreachable, or is this sandbox?** | The Michigan House site fails TLS from the build environment with "unable to get local issuer certificate" even with the egress proxy's CA bundle supplied, while `senate.michigan.gov` answers 200 on identical flags and the proxy logs no relay failure. That is the incomplete-chain shape this repo documents for Coles, Gallatin and Vermilion — **but TLS is re-terminated at the proxy here, so this environment cannot see the site's real chain at all.** A measurement is not a conclusion (`docs/EXPANSION_GUIDE.md` §0.4). | ONE CI-side probe, from a GitHub Actions runner rather than this sandbox. If the site answers there, the House card gains the same capitol contact block the Senate card already has, from the House's own directory. If it fails there too with a leaf-only chain, `scripts/probe_incomplete_tls_chains.py` reports the intermediate's SHA-256 and a scraper PINS it — never by disabling verification. |
| ~~When does the Bureau of Elections republish the commissioner-district layer?~~ **ANSWERED 2026-09-03** | It has not, and there is no successor: all 294 `michigan_admin` AGO items were enumerated and exactly one is a commissioner-district item, while the state's own current Election District Viewer (touched 2026-07-31) still wires this exact endpoint. Michigan counties reapportion on the decennial census (MCL 46.404), so the next plan is ~2031. | Closed. The republish WATCH now has a concrete mechanism — the standing monthly row above, against the AGO item's `modified` epoch, because the service itself carries no date field of any kind. |

---

## Per-election — the seats above the boundaries

| When | What | Last done |
|---|---|---|
| After each Michigan general (November, even years) and any special election | The chamber rosters turn over; the weekly Open States refresh picks it up, but verify the first post-election PR against each chamber's own directory before merging | 2026-09-03 (initial build: 38/38 Senate seats, 110/110 House seats — no vacancies at arrival) |
| After each U.S. general (November, even years) | The delegation turns over; the weekly congress-legislators refresh picks it up | 2026-09-03 (initial build: 13/13) |

---

## Per-decade — the census redistricting cycle

| When | What | Last done |
|---|---|---|
| Before each Michigan general (November, even years), and whenever the precinct card looks stale | **A new precinct cycle appears as a NEW SERVICE ON A DIFFERENT HOST, and nothing on the shipped one announces it.** The 2026 map is at `services3.arcgis.com/dxRQUfTDNtfqZ301/.../2026_Voting_Precincts` (AGO item `c349bc2bb6c2468594d341a09f520ea8`), while the `OpenData/boundaries` MapServer that carries the commissioner layer still stops at 2024 — so THE MAPSERVER IS THE WRONG PLACE TO CHECK. Run `arcgis.com/sharing/rest/search?q=owner:michigan_admin AND precinct` and look for a cycle newer than 2026; that one query is the whole check. Michigan went 4,340 -> 3,895 precincts between 2024 and 2026, so a new cycle is a real remap, not a refresh | 2026-09-04 (built against the 2026 cycle; 2014-2026 enumerated, none newer) |
| Whenever TIGERweb rolls a vintage | **The five live TIGERweb layers rebuild themselves — what a roll can break is silent.** (0) `zip-code` is fetched by an Esri `{xmin,ymin,xmax,ymax}` ENVELOPE rather than `STATE='26'`, because ZCTAs carry no STATE field; passing the instance's `{minLng,...}` `METRO_BBOX` instead yields HTTP 200 with a JSON error envelope and zero features rather than an error, which is how `ia/` shipped a dead ZIP overlay for weeks with a working card. `validate_sources.py` watches that count. (1) The Township or City and City or Village cards derive their type row from TIGER's own NAME-minus-BASENAME suffix; an unseen suffix renders NO type row rather than a wrong one, which is safe and invisible, so re-measure the LSADC/suffix distribution (measured 2026-09-04: 44 township 1,122 · 49 charter township 118 · 25 city 301 · 00 blank 40; places 25 city 281 · 47 village 252). (2) The two school layers tile Michigan only because School layer 1 (secondary) measures ZERO and the 27 elementary districts fill exactly the gaps the 514 unified ones leave — re-run the disjointness probe at each layer's `INTPT*` interior points (never `CENT*`, which reports a false overlap), and if layer 1 stops measuring zero, the recorded drop becomes a layer to ship | 2026-09-04 (measured at the phase-3 build) |
| After each decennial census (P.L. 94-171 release, spring of the year after) | Every boundary this instance ships is redrawn. Michigan's congressional and legislative lines are drawn by the **Independent Citizens Redistricting Commission** (2018 Prop 2), not the legislature; county commissioner districts are redrawn by each county's own apportionment commission under MCL 46.404/46.405. Rebuild all three chamber files and the county file, then re-anchor the smoke test | 2026-09-03 (built against the 2020-cycle maps) |
| After each decennial census, ~2031 | **County commissioner districts are reapportioned** by each county's own apportionment commission under MCL 46.404, and the Bureau of Elections recompiles the statewide layer. The 2021 plans shipped here run the 2020-census cycle | 2026-09-03 (built against the 2021 plans, 619 districts / 83 counties) |
| After each Michigan general seating commissioners (November 2028, then every 4 years) | Terms went to FOUR years under PA 121-122 of 2021, so the boards seated January 2025 run to December 2028 — there is NO 2026 commissioner election, and a re-verify scheduled for then would be wasted. The live risk between generals is mid-term vacancies | 2026-09-03 (no roster shipped; see the open question above) |
| Whenever TIGERweb rolls a vintage | **The congressional layer's district field is versioned and the old one is REMOVED, not merely stale.** It rolled to `CD120` for the 120th Congress; a query naming the retired `CD119` returns HTTP 400, "Failed to execute query" — measured 2026-09-03. `mi/scripts/build_legislative_boundaries.py` names `CD120`; the app's `CONGRESS_DISTRICT_FIELDS` lists it first with `cd119` behind it. On the next roll, both need the new name | 2026-09-03 (CD120) |
