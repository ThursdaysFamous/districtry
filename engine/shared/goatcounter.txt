<!-- THE ONE COPY OF THE COUNTER TAG. Every surface on this site reports to the
     same GoatCounter site and the PATH is what tells them apart (/, /il/, /ny/,
     /il/county-board/warren.html), so there is nothing per-instance about it and
     nothing to parameterise. It was seven hand-written copies until 2026-09-15 —
     in each app's index.html, the question pages, the landing page — and 193 of
     the site's 245 pages carried no copy at all, including all 183 per-county
     pages. The traffic report's subject is what this fleet gets read, and it had
     never counted the largest page set on it.

     Read from here by every GENERATED page's builder (build_county_pages.py,
     build_history_page.py, build_privacy_page.py's shared root-page shell) and
     spliced as an ENGINE fence into the authored ones, the same two mechanisms
     engine/shared/footer-byline.txt already uses for the same reason.

     scripts/validate_analytics.py holds every worksheet's
     brand.analytics.goatcounter_url to the URL in this file, so a rename here
     cannot leave an instance reporting somewhere else.

     `async` and a protocol-relative src are GoatCounter's own documented
     snippet. It sets no cookie and stores no identifier; what it sends is
     recorded on /privacy.html, which is generated by MEASURING these pages. -->
<script data-goatcounter="https://districtry.goatcounter.com/count"
        async src="//gc.zgo.at/count.js"></script>
